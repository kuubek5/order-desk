# Звіт: живі файли зі старого верстата (RemiCORE)

Складено 13.09.26 за набором, знятим 12.09.26 о 17:10 (Kyiv), і доповнено
**другим знімком того самого верстата 13.09.26 о 20:54** — розділ «Другий
знімок» наприкінці. Це виконання кроку 1 з `OLD_MACHINE_FILES_REQUEST.md`.
Копії файлів лежать у `tests/data/old_machine/` (другий знімок — у підтеці
`snapshot2/`); вони ж стануть фікстурою тесту парсера.

Джерело: один старий верстат, **ім'я ПК ще не назване**. Файли прийшли з трьох
різних тек (префікс у назві копії): `Bin\`, `TchUni\`, `Windows\`.

## Короткий підсумок

Три з чотирьох відкритих питань закрито. Головне рішення липня («знос =
`ActRunTime / MaxRunTime`») **підтверджується живими даними**. Понад те,
знайшлось те, чого специфікація не обіцяла: **дата останнього вимірювання
фрези** (`RefPosZTimeStamp`, unix epoch UTC) і **довічний лічильник годин по
типу фрези** в `Spindle.LOG`, який переживає заміни. Разом вони дають історію
гнізда вже з ПЕРШОГО знімка, не чекаючи місяців накопичення.

Частина полів зі специфікації на цьому верстаті **порожня назавжди** —
`Diameter`, `CuttingVelocity`, `AvgTimePerUnit`, `MaxCuttingTime`.

## Відповіді на відкриті питання

### 1. Чи рядки `ActRunTime` відповідають гніздам один-до-одного — ТАК

Структура виявилась на один шар глибша, ніж припускала специфікація:

- `[ToolProperties_ToolNNN]` — **NNN це номер ГНІЗДА** (1..20), не номер фрези.
- Поле `Type` усередині — **тип фрези** (наскрізний каталог виробника).
- `ToolCfg.INI` `[ToolChange_ToolTable]` індексується **типом**:
  `Tool033.000=0,16/0.3mm_CONICAL_Zr_PMMA_WAX` = тип 33, магазин 0, **гніздо 16**.

Тобто номер типу й номер гнізда збігаються не завжди: тип 16 стоїть у гнізді 10,
тип 33 — у 16, тип 63 — у 9, тип 64 — у 20. Решта збігаються. Звірка по всіх 20
гніздах зійшлась без винятків: `ToolProperties_ToolNNN.Type` → `ToolCfg` →
`place == NNN`.

Наслідок для коду: **ключ історії — номер гнізда з назви секції**, а опис і
діаметр беруться через `Type` з `ToolCfg`. Поле `ToolPlace` для цього
непридатне (див. «Пастки»).

Живий зріз на 12.09.26 17:10:

| Гніздо | Тип | Опис із `ToolCfg` | ActRunTime | MaxRunTime | Знос | Err | Востаннє міряна |
|---:|---:|---|---:|---:|---:|---:|---|
| 1 | 1 | 3.0mm RADIUS (l=15) Ti | 00:00:00 | 30:00:00 | 0 % | 1 | 06.09.23 |
| 2 | 2 | 2.0mm RADIUS (l=12) Ti | 09:49:44 | 10:00:00 | **98 %** | 0 | 19.08.25 |
| 3-8 | 3,4,11,6,7,8 | — | 00:00:00 | 6-10 год | 0 % | 1040 | немає |
| 9 | 63 | 1.5mm TORUS (r=0.08/l=7) Ti CoCr | 00:00:00 | 06:00:00 | 0 % | 1040 | немає |
| 10 | 16 | 1.5mm RADIUS (l=15) Zr PMMA WAX | 00:47:47 | 06:00:00 | 13 % | 0 | 15.11.24 |
| 11 | 11 | 2.5mm RADIUS PMMA WAX PEEK | 54:46:17 | 80:00:00 | 68 % | 0 | 11.09.26 |
| 12 | 12 | 1.0mm RADIUS PMMA WAX PEEK | 07:04:20 | 80:00:00 | 9 % | 0 | 11.09.26 |
| 13 | 13 | 2.5mm RADIUS Zr | 45:04:37 | 60:00:00 | 75 % | 0 | 12.09.26 |
| 14 | 14 | 1.0mm RADIUS Zr | 99:32:17 | 60:00:00 | **166 %** | **256** | 12.09.26 |
| 15 | 15 | 0.6mm CONICAL Zr PMMA WAX | 21:50:47 | 300:00:00 | 7 % | 0 | 12.09.26 |
| 16 | 33 | 0.3mm CONICAL Zr PMMA WAX | 92:40:51 | 300:00:00 | 31 % | 0 | 12.09.26 |
| 17 | 17 | 1.5mm SHAFT (l=15) Zr PMMA WAX | 271:36:28 | 300:00:00 | **91 %** | 0 | 12.09.26 |
| 18 | 18 | — | 00:00:00 | 02:00:00 | 0 % | 1040 | немає |
| 19 | 19 | 0.5mm SHAFT (l=4) UNIVERSAL | 00:19:58 | 02:00:00 | 17 % | 0 | 27.01.23 |
| 20 | 64 | 1.5mm TORUS (r=0.08/l=15) Ti CoCr | 02:37:51 | 03:00:00 | **88 %** | 0 | 29.03.24 |

**Гніздо 14 (1.0mm по цирконію) перепрацювало ресурс у 1.66 раза просто зараз.**
Гнізда 2, 17 і 20 — за 88 % і вище. Це вже готовий екран «час міняти», без
жодного накопичення історії.

### 2. Що насправді в `Spindle.LOG` — суми по ТИПАХ фрез, дат немає

```
[WorkTime_Spindle_1]
Total=577:25:32
Tool11000=1015:20:30
Tool12000=891:12:26
...
Tool_Unknown=00:00:01
```

- Ключ = `Tool` + **тип × 1000**. `Tool2000` = тип 2, `Tool64000` = тип 64.
- Значення = години **того самого лічильника, що й `ActRunTime`**, але
  **накопичені довічно, без скидання при заміні фрези**. Доведено збігом
  секунда-в-секунду там, де фрезу ще жодного разу не міняли:
  тип 2 = `09:49:44` = `ActRunTime` гнізда 2; тип 19 = `00:19:58`; тип 64 =
  `02:37:51`.
- **Дат у файлі немає** — лише суми. Питання закрите: календаря звідси не буде.
- `Total=577:25:32` **менше** за окремі типи (тип 11 — 1015 год), тож це **не
  сума** накопичених значень. Другий знімок показав, що це **той самий
  годинник**: за добу сума приростів по типах і приріст `Total` збіглися
  секунда в секунду (`+4:20:27`). Отже `Total` — мотогодини шпинделя від
  останнього скидання лічильника (заміна шпинделя), а лічильники типів тоді не
  скидали. Доведено.
- Типів, які жодного разу не різали (1 і 63), у файлі немає взагалі — рядок
  з'являється з першим різанням.

Що це дає безкоштовно: **скільки життів фрези вже прожило гніздо**.
Тип 12 — 891 год довічно при ресурсі 80 год = приблизно 11 замін.
Тип 17 — 818 год при 300 = приблизно 2.7. Тип 33 — 697 год при 300 = 2.3.

### 3. Скільки гнізд насправді — 20

`TchUni.INI` → `[TchUni_Options] NumTools=20`, і рівно 20 секцій
`[ToolInClampPos_1..20]` із реальними координатами (тисячні міліметра):
чотири ряди по п'ять, крок ~20 мм по Y, ~20 мм по X, спільний
`AxZ=-79060`. Окремо `NumToolsManualToolchange=12`.

Системні позиції на місці: `[ToolChangePos]`, `[EndPos]`, `[StartPosRef]`,
`[SecLevel]`, `[LastPos]`. У `ToolChangePos`, `EndPos`, `SecLevel`, `LastPos`
координати нульові або порожні — реально заповнений лише `StartPosRef`.

### 4. Чи всі п'ять машин однакові — ВІДПОВІДІ НЕМАЄ

Потрібен набір хоча б з другої машини. Поки маємо один верстат з 20 гніздами.

## Поле за полем: обіцяне проти живого

| Поле | Файл | Живий стан |
|---|---|---|
| `ActRunTime` | ToolProperties | **є**, `ГГ:ХХ:СС`, години тризначні (`271:36:28`) |
| `MaxRunTime` | ToolProperties | **є**, заповнене лише для 20 гнізд зі 129 секцій |
| `ErrStatusFlag` | ToolProperties | **є**: `0` норма, `256` тільки на перепрацьованому гнізді 14, `1040` на порожніх гніздах, `1` на гнізді 1 |
| `RefPosZ`, `MinLength`, `MaxLength` | ToolProperties | **є**, тисячні мм; `1073741823` = «немає значення» |
| `RefPosZTimeStamp` | ToolProperties | **є, специфікація не обіцяла**: unix epoch UTC, `-1` = ніколи. Це дата **останнього вимірювання довжини**, а вимірювання відбувається при кожному заборі фрези в шпиндель. **Не дата заміни** — див. «Другий знімок» |
| `ActCuttingTime` | ToolProperties | **є, специфікація не обіцяла**. Тикає паралельно з `ActRunTime`, але приблизно на 1 % повільніше (шпиндель крутиться довше, ніж ріже). Абсолютні значення розходяться, бо скидаються за різними подіями: `ActRunTime` — при заміні фрези, `ActCuttingTime` — рідше й невідомо коли. **Знос рахувати тільки за `ActRunTime`** |
| `Diameter` | ToolProperties | **завжди 0** — брати з тексту `ToolCfg` (`0.3mm`, `2.5mm`) |
| `CuttingVelocity` | ToolProperties | **завжди 0** |
| `AvgTimePerUnit` | ToolProperties | **завжди 00:00:00** |
| `MaxCuttingTime` | ToolProperties | **порожній рядок**, навіть не `00:00:00` |
| `MaxSpeed` | ToolProperties | є, скрізь `54000` (об/хв); `MinSpeed`/`DefaultSpeed` = 0 |
| `Length2` | ToolProperties | є, тисячні мм |
| `[ToolChange_ToolTable]` | ToolCfg | **є**, формат `ToolTTT.000=<магазин>,<гніздо>/<опис>` |
| `ActTool` | Toolstat | **є**: `15` у першому знімку, `14` у другому. `ActToolPlace=1073741823` («немає значення») в обох. **Чи це тип, чи гніздо — досі не розрізнити**: і тип 15, і тип 14 стоять у гніздах з тими самими номерами. Розрізнить лише знімок у момент, коли в шпинделі тип 16, 33, 63 або 64 (гнізда 10, 16, 9, 20) |
| `[ToolInClampPos_1..20]` | TchUni | **є**, з `MoveOrder=B,XY,Z` і `SpecificApproachSequence=1073741823` |
| системні позиції | TchUni | **є**, але переважно нульові |
| `Total` мотогодини | Spindle.LOG | **є**, зміст не доведений |

## Пастки, які треба врахувати в парсері

1. **`TchUni.INI` існує у двох місцях.** Копія з `Windows\` — це два рядки
   позиції діалогу (`[TeachDlg]`). Парсер мусить упізнавати файл **за вмістом**
   (`[TchUni_Options]`), а не за назвою, інакше «знайшов перший» дає сміття.
2. **Секцій `ToolProperties` 129 (Tool000..Tool128), змістовних 20.** Порожні —
   `Type=0` і `MaxRunTime=00:00:00`. Фільтрувати саме так; за `ActRunTime` не
   можна — свіжозмінена фреза теж має нуль.
3. **`ToolPlace` для гнізд 1-8 і 18 дорівнює 0**, хоча `ToolCfg` для гнізд 1 і 2
   тримає опис, а гніздо 2 має 98 % зносу й реальну дату вимірювання.
   Правильно читати номер гнізда **з назви секції**; `ToolPlace` — це радше
   ознака «фреза зараз фізично в магазині».
4. **Гнізда 5 і 11 обидва мають `Type=11`.** Гніздо 5 порожнє (`ErrStatusFlag=1040`,
   дата 2022 року) — привид старої конфігурації. Тобто тип **не унікальний** і
   ключем бути не може.
5. **Години бувають тризначні** (`271:36:28`, `1015:20:30`) — розбирати
   `ГГ:ХХ:СС` без припущення про дві цифри.
6. **`1073741823` = «немає значення»**, а в `RefPosZTimeStamp` те саме означає
   `-1`. Два різні сигнали порожнечі в одному файлі.
7. **Знос буває більший за 100 %** (166 % на гнізді 14) — не обрізати до 100,
   інакше зникає найважливіший випадок.
8. `ToolCfg.INI` не оновлювався з 31.08.26, решта файлів — 12.09.26 17:10.
   Тобто **таблиця типів змінюється рідко, лічильники — постійно**. Опитувати
   з різною частотою.

## Другий знімок: 12.09 17:10 → 13.09 20:54

Верстат між знімками працював. За 27 год 44 хв календарного часу лічильники
наросли на **4 год 20 хв 27 с** — тобто це **не календар**, головне рішення
липня підтверджено вимірюванням, а не міркуванням.

| Гніздо | Тип | Фреза | Δ`ActRunTime` | Δ`ActCuttingTime` | Стало | Знос |
|---:|---:|---|---:|---:|---:|---:|
| 13 | 13 | 2.5mm RADIUS Zr | +0:47:40 | +0:46:55 | 45:52:17 | 76 % |
| 14 | 14 | 1.0mm RADIUS Zr | +2:53:55 | +2:52:19 | 102:26:12 | **171 %** |
| 15 | 15 | 0.6mm CONICAL | +0:11:57 | +0:11:37 | 22:02:44 | 7 % |
| 17 | 17 | 1.5mm SHAFT | +0:26:55 | +0:26:44 | 272:03:23 | 91 % |

Решта 16 гнізд — без жодної зміни.

Що це довело:

1. **`ActRunTime` і `Spindle.LOG` — один лічильник.** Прирости по всіх чотирьох
   гніздах збіглися з приростами `Tool13000`/`Tool14000`/`Tool15000`/`Tool17000`
   секунда в секунду.
2. **`Total` — теж він.** Сума чотирьох приростів = `+4:20:27` = приріст
   `Total`. Точно, без залишку.
3. **`ActCuttingTime` — це власне різання, `ActRunTime` — обертання шпинделя.**
   Різниця приростів мала й завжди в один бік: різання відстає на 11-96 секунд
   за 4 години, тобто близько 1 %.
4. **`RefPosZTimeStamp` — НЕ дата заміни фрези.** Це виправлення до першого
   звіту. Гніздо 14 фрезу не міняли — `ActRunTime` не обнулявся, а тільки виріс
   з 99 до 102 год, — але штамп оновився з 12.09 на 13.09. Отже штамп
   оновлюється при кожному вимірюванні довжини, тобто щоразу як фреза йде в
   шпиндель. Корисне значення в нього інше: **«коли фреза востаннє була в
   роботі»**. Дату заміни доводиться ловити тільки падінням `ActRunTime`, як і
   вирішили в липні.
5. **Гніздо 14 ріже далі при 171 % ресурсу** — за добу додало майже три години,
   найбільше з усіх. `ErrStatusFlag=256` стоїть, і верстат на це не зважає.
   Це і є та цифра, заради якої все затівалось.

## Нові відкриті питання

- `Toolstat.ActTool` — тип чи гніздо. Другий знімок не розрізнив (`14`, а тип 14
  теж у гнізді 14). Потрібен знімок у момент роботи типів 16, 33, 63, 64.
- Що саме скидає `ActCuttingTime` (при заміні фрези він, на відміну від
  `ActRunTime`, не обнуляється).
- Точний зміст `ErrStatusFlag=1` (гніздо 1) і `=1040` (порожні гнізда).
  `256` впевнено читається як «ресурс вичерпано».
- Чи всі п'ять старих машин однакові — потрібен набір з другої.

## Наступні кроки

1. **Від Роми:** ім'я/адреса ПК цього верстата, повні шляхи тек `...\Bin` і
   `...\TchUni`, і чи всі п'ять машин тієї самої моделі.
2. Парсер + тест на обох знімках (`tests/data/old_machine/`): другий знімок
   дає перевірку не лише розбору, а й **приростів** — саме того, на чому
   тримається знос.
3. Віддача файла агентом (Go), опитування раз на годину; пастка
   `syscall.NewCallback` — `CLAUDE.md` §14.
