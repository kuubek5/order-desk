# План після аудиту 06.09.26 — робочий трекер

Джерело: `AUDIT.md` поруч + `review_backend.md`, `review_frontend.md`. Правила ті самі, що в
`design/audit_2026-09-05/PLAN.md`: один чат = один крок або група, статус ставити тут,
`⚠ ЗВІРИТИ З РОМОЮ` = спитати перед роботою.

Статуси: `⬜` не почато · `🔄` в роботі · `✅` готово · `⏸` відкладено · `❌` не робимо.

## Зроблено в чаті аудиту (гілка `audit/2026-09-06`, worktree `../CRM_Laba-audit`)

F1, F2, F3, F4, F5, F6, F7, F8, F9, F10, F11, F12, F14, F15, F16, F18, F19 — див. AUDIT.md §6.
Плюс: інструкція регенерації `tests/route_inventory.txt` у докстрінгу була застаріла
(давала 2 рядки замість 197) — виправлена.

## Організаційне (рішення Роми)

| # | Статус | Крок |
|---|---|---|
| O.1 | ✅ 07.09.26 | Гілку влито в master (`d30b90f`), worktree і 24 злиті гілки прибрано. |
| O.2 | ✅ (само собою) | Реліз v0.10.5 після v0.11.1: паралельний чат уже випустив **v0.11.2** (latest), колізія знята. Лишилось оновити прод. |
| O.3 | ⬜ | Два чати в одному дереві — знову. Нагадати собі: другий чат = `git worktree add`. |
| O.4 | ⬜ | `bump_version.py`: відмовлятися штампувати версію, нижчу за найновіший тег — саме так зʼявився v0.10.5 після v0.11.1. |

## Швидкодія / функціональність (лишилось)

| # | Статус | Крок | Джерело |
|---|---|---|---|
| P.1 | ⬜ | `/?period=earlier`: сервер досі 1,5 с на 200 рядків — «решта (незаміряне)» 0,8–1,4 с. Профілювати: `attach_export_folder_uris`/`attach_job_code_folder_uris` і сортування йдуть по всіх 616 ДО зрізу. Перенести дорогі помічники після зрізу. | AUDIT 4.1 |
| P.2 | ⬜ | `/clients/{id}/pane` вантажить усі роботи за весь час; обмежити SQL вікном або кандидатами імені. | review_backend MEDIUM, AUDIT 4.3 |
| P.3 | ⬜ | `/search`, `/journal/sync` — пагінація (389 і 192 КБ). | AUDIT 4.4 |
| P.4 | ⬜ ⚠ | `POST /handout/confirm-alias` — сирота без UI. Видалити або привʼязати (підтвердження нечіткого збігу імені клієнта на видачі). | AUDIT 4.8 |
| P.5 | ⬜ | «змінено після взяття» майже на кожному рядку тестової БД після бекфілу — перевірити на проді, чи не шумить діф після повного синку. | AUDIT 4.9 |
| P.6 | ⬜ | `_settings_about.html`: два різні тексти «Доступне оновлення» (плита + `_update_check_result.html`) — звести в один партіал. | огляд |

## Код-ревʼю MEDIUM (бекенд), у порядку болю

| # | Статус | Крок |
|---|---|---|
| C.1 | ⬜ | `mail_accept.py`: ловити `Exception`, а не лише `OSError/ValueError` після переносу файлів; `moved_pairs` у списку виклику. |
| C.2 | ⬜ | `reject_email`: не комітити «відхилено» поверх невдалого переносу файлів назад у спул. |
| C.3 | ⬜ | Тека спулу `mail_attachments/<uid>` без `uid_validity` — після зміни скриньки два листи ділять теку. |
| C.4 | ⬜ | Loopback на `save_imap_settings`, `test_imap_connection`, `POST /settings` (секрети) — як у сусідніх роутах. |
| C.5 | ⬜ | 10 ручних `admin + loopback` гейтів (backup/update/users/feedback) → `deps.require_admin(..., unauth="redirect")`. Ще ~20 `role != "адмін"` у queue/mail/machines. |
| C.6 | ⬜ | `furnace.poll_target` пише стан поза `_states_lock`; `grab()`/`read_panel` без широкого `except` — одна піч валить тік усіх. Те саме `screen_is_sisma` у machines. |
| C.7 | ⬜ | `_calib_signatures` ключ без теки — після зміни теки калібрування старі сигнатури глушать нові кадри. |
| C.8 | ⬜ | `prune_readings` — `delete()` замість ORM по одному; `freeze_due_days` — фільтр по дню в SQL. |
| C.9 | ⬜ | `settings_selfcheck._folder()` — використати `check_path_status(write_probe=True)`, інакше «доступна на запис» без проби запису. |
| C.10 | ⬜ | `palette.COMMANDS` — другий реєстр екранів; сторож або спільне джерело з рейкою. |
| C.11 | ⬜ | `queue.py` `/sheets/import-history`, `/sheets/reconcile-deletions`: докстрінг обіцяє loopback, коду немає. |
| C.12 | ⬜ | LOW-список у `review_backend.md` (17 пунктів) — одним чатом-прибиранням. |

## UX / вигляд (лишилось)

| # | Статус | Крок |
|---|---|---|
| U.1 | ⬜ | Один токен ширини для «вузьких» екранів (архів 1100 / статистика 1040 / журнал 920 / звернення 820). |
| U.2 | ⬜ ⚠ | Standalone-картки роботи й листа: прибрати «Статистика» з шапки; лишити «← До черги». |
| U.3 | ⬜ | 403 для оператора за прямою адресою — шаблон замість голого JSON. |
| U.4 | ⬜ | Бейдж «5/5» у рейці налаштувань — пояснити (title) або прибрати. |
| U.5 | ⬜ | Рейка налаштувань під банером оновлення на 900 px — заголовок групи обрізається. |
| U.6 | ⬜ | `settings.css`: 54 нові літеральні кольори без `var()` (review_frontend). |
| U.7 | ⬜ | Виробіток: клавіатура PIN зʼявляється із затримкою ~1 с — прибрати анімацію-затримку або показати одразу. |

## Журнал просування

| Дата | Крок | Що зроблено | Коміт |
|---|---|---|---|
| 06.09.26 | аудит | Повний прохід: 52 GET × 2 ролі, скріншоти, консоль, кнопки «Перевірити», інвентар контролів, 2 код-ревʼю, pytest+ruff. Звіт `AUDIT.md`. | — |
| 06.09.26 | F1–F19 | 17 виправлень з тестами у гілці `audit/2026-09-06` (див. AUDIT §6). Повний прогін: 2119 тестів зелені, ruff чистий. | `5415619` |

## B — збереження даних (пріоритет власника, 07.09.26)

Правило блоку: **з живої Google-таблиці нічого не має зникнути**, а після оновлення
має бути ВИДНО, що нічого не зникло з бази. Порядок випробувань завжди однаковий:
спершу локальний сервер 8002 на тестовій таблиці, потім робочий ПК.

| # | Статус | Крок |
|---|---|---|
| B.1 | ✅ 07.09.26 | Бекап охоплює всі 29 таблиць (28 у `_TABLE_MODELS` + `app_settings` окремо; було 13). Копія несе власний перелічник «таблиця → рядків»; після відновлення числа звіряються, розходження = відкат, а не напівжива база. Копія з новішого формату відхиляється замість тихої втрати таблиць. Сторож `tests/test_backup_covers_every_table.py` валиться на новій таблиці без рядка в списку. |
| B.2 | ✅ 07.09.26 | Звірка «після оновлення нічого не зникло»: знімок кількості рядків знімається ПЕРЕД встановленням оновлення, після першого старту нової версії звіряється, результат на «Стані системи» (зелений рядок або червоний перелік таблиць). Таблиці, які застосунок сам чистить, зі звірки виключені. |
| B.3 | ✅ 07.09.26 | Стирання рядка в таблиці лишає слід: вміст A:K читається ДО стирання й лягає в «Журнал синку» разом із номером рядка. «Зникло, і невідомо що там було» більше не буває. |
| B.4 | ✅ 07.09.26 (частково, див. нижче) | Прогін на КОПІЇ бойової dev-бази (2,1 МБ, 10 619 рядків у 25 непорожніх таблицях). Результат нижче. |

**Результат прогону B.4 (07.09.26, копія dev-бази в scratchpad, не жива):**

1. **Міграція.** `ensure_schema` довів копію 0047 → 0048 на живих даних; наявні
   427 записів журналу лишились, нові колонки порожні.
2. **Бекап → відновлення → числа.** Копія 5,19 МБ, `format_version` 2, у
   перелічнику 29 таблиць. Відновлення в порожню базу: **розбіжностей нема**
   (звірено по всіх 28 моделях). Чужий пароль відхилено `BackupPasswordError`,
   а не «відновилось сміттям».
3. **Звірка після оновлення.** Знімок «до» → підміна версії на `9.9.9-fake` →
   `check_after_update`: `ok: true`, рядок для екрана
   «3436 робіт · 4059 записів історії · 249 клієнтів · 107 клітинок Виробітку ·
   1 верстат · 1 піч».
4. **Звірка ЛОВИТЬ втрату.** Той самий цикл із видаленням 5 коментарів:
   `ok: false`, `lost: {"comments": [619, 614]}`. (Спершу пробували видаляти
   `sync_logs` — звірка мовчала правильно: журнал у `SELF_PRUNING`.)
5. **Слід від стертого рядка → кнопка.** `sync_logs.erased_row/erased_values`
   пишуться (`tests/test_sheet_writeback_erase_trace.py`) і доїжджають до HTML
   як `POST /journal/sync/{id}/restore-row`
   (`tests/test_sync_journal_render.py`); відновлений запис показує підпис без
   кнопки.

**Чого локально перевірити НЕ можна** (лишається на B.5, робочий ПК):
справжній запис у Google-таблицю (сервісний акаунт на тестовій таблиці — лише
«Читач», §12), справжній запуск інсталятора і копія `backups/pre-update/`,
знята бойовим роутом.
| B.5 | ⬜ | Те саме на робочому ПК ПІСЛЯ локального прогону, у порядку: бекап → оновлення → «Стан системи» → перший синк → перевірити, що таблиця ціла (кількість рядків у вкладці до і після). |
| B.6 | ✅ 07.09.26 | Автоматична повна копія бази перед КОЖНИМ оновленням: `app/pre_update_backup.py`, `VACUUM INTO` у `backups/pre-update/`, тримаються останні 5. Пароля нема свідомо — копія для відкату на цьому ж ПК, де DPAPI-ключ на місці. Копіювання синхронне (інсталятор не стартує раніше за копію); збій не зриває оновлення, але дописує попередження адміну. |
| B.7 | ✅ 07.09.26 | Стеля стирань рядків таблиці: 20 за годину (`KUUBMILL_SHEET_ERASE_LIMIT`), `app/sheet_erase_guard.py`. Перевищили — стирання пропускається, рядок лишається в таблиці (безпечний бік відмови). Стелю їдять лише здійснені стирання, не пропущені. `SheetEraseBlocked` — окремий тип, щоб у журнал ішла правильна причина. |
| B.8 | ✅ 07.09.26 | Кнопка «Відновити рядок» у «Журналі синку». Вміст стертого рядка тепер лягає ДАНИМИ (`sync_logs.erased_row/erased_values`, міграція 0048), відновлення — `POST /journal/sync/{log_id}/restore-row`. Рядок мусить бути порожній, інакше відмова: лабораторія переюзує звільнені рядки. Повторне відновлення неможливе. |

## Повне код-ревʼю 07.09.26 (CODE_REVIEW.md) — що лишилось

Зроблено в master (`dedecfc` + наступний коміт): 3 CRITICAL синку, 4 CRITICAL пошти, 2 CRITICAL
домену, 1 CRITICAL пристроїв, CSRF/Origin, токен Telegram, proxy_headers, вікно undo, статистика,
Виробіток-гонка, SISMA-кеш, лічильник верстатів, зображення зміни, STL-кеш, нічні тести,
`release.yml` з тестами перед білдом. Нижче — черга. Джерела: `review_full_*.md`.

### S2 — синк (HIGH)
| # | Статус | Крок |
|---|---|---|
| S2.1 | ✅ 07.09.26 | Воскресіння з архіву звіряється ще й через `_row_identity` (ключ `labpending`). Порівняння за нарядом/клієнтом лишилось — identity клієнтського рядка включає матеріал і к-сть. |
| S2.2 | ✅ 07.09.26 | Пара, яку не підтвердила identity, перевіряється на «спільне» (`_looks_like_another_work`): не збігається НІЧОГО з заповненого з обох боків — рядок імпортується як новий, чужа робота не переписується. Виправлення наряду під це не підпадає (тест). |
| S2.3 | ✅ 07.09.26 | `issue_group_warm` пакетний: на вкладку одне читання колонки-ідентифікатора, одне читання маркерів, один `batch_update`, одна правка заливки (`resolve_rows_bulk`, `write_order_fields_bulk`). Запобіжники не послаблені. |
| S2.4 | ✅ 07.09.26 | Знімок вкладок — одне `get_all_values`, під локом синку (лок у `finally`), пропуск вкладки при `quota_is_tight()`. Чанковий `read_all_values` видалений разом із тестами. |
| S2.5 | ✅ 07.09.26 | Undo/redo складають відкладений запис і виконують його через `submit_sheet_write` ПІСЛЯ коміту (warm-функції читають з БД). |
| S2.6 | ✅ 07.09.26 | Гейт паузи в самих функціях читання: ручний виклик кидає `SheetSyncPausedError` (у т.ч. «Звірити видалення»), фоновий і гарячий тік мовчки нічого не роблять. |
| S2.7 | ✅ 07.09.26 | Усі вісім: порожній прогін не рахується днем; довіра до листингу — від найновішої вкладки; ручне додавання під локом; поштова робота дістала identity клієнтського рядка; мʼяка ознака СЛМ лише для незнайомих рядків; притримане зникнення пише в «Журнал синку»; `logger.exception` перед санітизацією; перевірка вільного рядка через `call_with_retry`. |
| S2.8 | ✅ 07.09.26 | `app/main.py` і `app/sync_cli.py` видалені (останній ще й звіряв видалення проти чужого дня), мертва `SHEET_SYNC_HOT_INTERVAL_SECONDS` прибрана, посилання в `web.py`/`parser.py`/`DEPLOY.md` виправлені. |

### M — пошта (HIGH)
| # | Статус | Крок |
|---|---|---|
| M.1 | ✅ 07.09.26 | Покинутий (зомбі) фетч реєструється, і новий не стартує, поки він живий: лок віддається, але друга качалка в ту саму теку більше не запускається. Список самоочищається. |
| M.2 | ✅ 07.09.26 | Приймання перевіряє файли через `_file_is_missing`: моргання UNC більше не робить лист «прийнятим» із файлами, що лишились у спулі. |
| M.3 | ✅ 07.09.26 | Нові теки спулу — `<uid_validity>_<uid>`; старі не перейменовуються (на них посилаються збережені шляхи), `folder_candidates` визнає обидва імені, щоб прибиральник не порахував теку живого листа нічийною. |
| M.4 | ✅ 07.09.26 | Токени прев'ю кешуються на 60 с і скидаються разом із кешем обходу export (`clear_export_cache`). |
| M.5 | ✅ 07.09.26 | `analyze_spool_cached` (5 хв) на екрані налаштувань; кнопка «Прибрати» скидає кеш. |
| M.6 | ✅ 07.09.26 | Здогад клієнта — лише для пересланих листів (`looks_forwarded`). Набір зайнятих імен у циклі архівів рахується один раз і свідомо НЕ поповнюється розпакованим: два архіви з однаковим `crown.stl` мусять дати дві коронки. |
| M.7 | ✅ 07.09.26 | Відкат бере лише вкладення з `order_id`; `email.order_id` пишеться раз; лок на лист (двоє операторів); token-аліаси вимкнені для вільного тексту (`free_text=True`); `времен(?:…|а|ами)` без зʼїдання «временная»; межа слова `(?![^\W_])`; `_stream_to_file` прибирає обрізаний файл на BaseException; `can_edit` на трьох роутах; вкладка «Авто-прийняття» → «Авто-скачування»; один прохід по диску на рендер панелі; мертва колонка `staged_to_export` видалена (міграція 0049). |

### K — ядро / безпека
| # | Статус | Крок |
|---|---|---|
| K.1 | ✅ 07.09.26 (інше рішення) | Закрито кроком B.1, але НЕ через `Base.metadata.sorted_tables`: порядок вставки під FK тримає людина (цикл orders ↔ email_messages автосортувальник не розвʼязує), тому список явний — `_TABLE_MODELS` (`app/backup.py:84`), а коментар `app/backup.py:79` прямо відкидає `sorted_tables`. Тест-сторож `tests/test_backup_covers_every_table.py`. |
| K.2 | ⬜ ⚠ | Підпис інсталятора Ed25519 ключем із `license.py:34` і перевірка перед `launch_silent_install` (ланцюг довіри зараз — TLS через проксі лабораторії). Потребує ключа у CI. |
| K.3 | ⬜ ⚠ | `db.py:14` — `PRAGMA foreign_keys=ON` + `ondelete` там, де каскад свідомий; перевірити `restore_backup` і `delete_machine/furnace` під ним. Ризик: наявні висячі FK у бойовій базі. |
| K.4 | ⬜ | Loopback послідовно: `POST /settings` (секрети), `/settings/imap`, `/settings/test-imap`, `settings/users.py` ×4 — через `require_settings_edit(loopback=True)`. |
| K.5 | ⬜ | `machine_calibration_path` — адмінська або під `data_dir()` (зараз оператор може вказати довільну шару, куди пишуться JPEG). |
| K.6 | ⬜ | `ActionLog` на мутації налаштувань (пароль IMAP, JSON, ліцензія, видалення верстата, відновлення бекапу). |
| K.7 | ⬜ | Пул зʼєднань: розмір явно (`NullPool` для локального SQLite?) + заміряти; `deps.py` — 8 Jinja-глобалів по сесії на рендер. |
| K.8 | ⬜ | `backup.restore`: перевіряти `format_version`, повторний пароль перед відновленням. Ротація `SESSION_SECRET_KEY` незалежно від ключа БД; інвалідація сесій при зміні пароля. |
| K.9 | ⬜ | LOW-список у `review_full_core_security.md` (IDOR `/feedback/images`, rate-limit `/feedback`, `palette.COMMANDS` з `nav_payload`, `blocked_response` для HX, `vyrobitok_pin` хешем, PII в логах IMAP, docstring `license_gate`). |

### D — пристрої
| # | Статус | Крок |
|---|---|---|
| D.1 | ⬜ ⚠ | Go-агент: firewall-правило лише з IP CRM (мінімум) або TLS з pinned-сертифікатом; токен ≥ 20 символів + backoff; `/info` без токена; ACL на `agent.json`/`crm-setup.txt`; без `?token=`. |
| D.2 | ⬜ | `furnace.py:397-436` — стан під `_states_lock` (як `machines.py:795-825`); широкий `except` у `grab()`/`read_panel`; `screen_is_sisma` під `except`. |
| D.3 | ⬜ | `furnace_ocr.py` — «голосування трьох» задокументувати як два або вимагати ≥2; патерн `command` зі структурою. |
| D.4 | ⬜ | `furnace_vnc.py:66-84` — тест/доказ закриття сокета при cancel; кап framebuffer. |
| D.5 | ⬜ | `poll_target` (200 рядків) розбити; `_calib_signatures` ключ із текою; `resolve_frame` мертва `_HOST_RE`; `frame_path` через `_sanitize_key`; мертві `MachineConfigError`, `SismaReading.printing`, `config_error`. |
| D.6 | ⬜ | SISMA-пайплайн на спільних хелперах `furnace_ocr`; `scripts/machine_collect_frames.py` з капом/дедлайном. |

### T — тести / CI
| # | Статус | Крок |
|---|---|---|
| T.1 | ⬜ | `release.yml`: імʼя інсталятора з тегу (6 літералів `KuubMill-Setup-X.exe`) + сторож у `test_version_sync`. |
| T.2 | ⬜ | Lock-файл залежностей (`pip-compile`) — зараз майже все `>=`. |
| T.3 | ⬜ | Сторож бізнес-дня сканує і `tests/` (падіння між 00:00 і 07:30 ловилось лише вночі). |
| T.4 | ⬜ | 61 файл тестів на `conftest.db_session`; `pyproject.toml` (назва/версія); mypy — рахувати, не ігнорувати. |

### U2 — фронтенд
| # | Статус | Крок |
|---|---|---|
| U2.1 | ⬜ | `--mat-zr/pmma/ti/slm/wax` у `tokens.css` замість літералів у `furnaces.css` і `v2a_queue.css`. |
| U2.2 | ⬜ | Шкала `--z-*` (22 значення в 15 файлах); один токен ширини контейнера (див. U.1). |
| U2.3 | ⬜ | Одна палітра Ctrl+K замість `palette.js` + `settings_console.js`. |
| U2.4 | ⬜ | SVG-спрайт для ~15 повторюваних іконок (`_topbar_nav` 22, `_mail_detail_panel` 18); `hx-indicator` там, де нема іншого фідбеку; `lookgear.js` один document-listener; `@media print` для паспорта/архіву. |
