# Backend review — e52f85f..HEAD (76 commits)

Scope: `app/routers/**`, `app/services/**`, `app/*.py`, `migrations/versions/**`.
Method: `git diff e52f85f..HEAD` to find the surface, then read the CURRENT code at HEAD.
Guard tests `test_route_inventory.py`, `test_settings_nav.py`, `test_frontend_assets.py` — 33 passed.

## CRITICAL

(none — no data-loss or auth-bypass defect confirmed at HEAD)

## HIGH

app/routers/mail.py:962 — `async def accept_email` runs `accept_letter` inline: `_resolve_target_tab` → `open_spreadsheet` + `append_mail_placeholder_row` (gspread through the lab TLS proxy, ~40 s cold) and UNC attachment moves execute ON the event loop. Violates §14 "роут, який пише синхронно, має бути звичайним `def`"; this is the hottest triage action, so the whole app freezes several times an hour — exactly the C-2 defect the audit fixed elsewhere — fix: drop `async` (FastAPI then uses the threadpool), or route the sheet part through `await_on_writeback`.
app/routers/mail.py:1560 — `async def restore_email` → `_unaccept_email` calls `open_spreadsheet` (mail.py:1537) and `clear_order_row` (mail.py:1543) plus `restore_attachments_to_spool` (mail.py:1521) directly on the event loop — same invariant, same freeze — fix: plain `def`.
app/routers/mail.py:1063 — `async def reject_email` moves attachments back from export on the event loop (mail.py:1079) — fix: plain `def`.
app/routers/settings/overview.py:375,439 — `async def post_settings` calls `sync_google_sheets(db)` synchronously for `action=save_and_sync`: a full multi-tab Sheets read on the event loop, minutes long — fix: make the route plain `def`, or move the sync to a background/pool call as `/sheets/import-history` already does.
app/services/mail_accept.py:175 — `_write_placeholder_row` appends a REAL row to the shared sheet BEFORE `db.commit()` (mail_accept.py:185). The commit-failure branch (188-204) compensates only the file moves via `undo_moves`; the sheet row survives the rollback and the next sync re-imports it as a separate наряд-less `sheet_client` order — the same work appears twice — fix: append after a successful commit (a `*_warm` fn on the writeback pool), or blank the row in the rollback branch.
app/mail_spool.py:82 — `letters = {uid: (status, received_at)}` keys the spool-prune map by `uid` alone, but uniqueness is now the PAIR `(uid, uid_validity)` (models.py:419, migration 0045). After a UIDVALIDITY change two rows share a uid, the dict keeps only the last one, and a live «нове» letter's spool folder can be pruned because a stale «відхилено» row won — fix: aggregate per uid (any non-rejected / newest row wins).
app/services/settings_nav.py:171 — NAV declares `mail-filters` with `edit_roles=None` (operator may edit) but all eight routes hard-fail `user.role != "адмін"` (mail.py:1209,1265,1313,1337,1376,1418,1444,1470), and `settings.html` includes the panel for everyone who `can_see`. An operator gets the full editing UI where every button returns 403; `tests/test_settings_nav.py` misses it because it only scans the settings package — fix: set `roles/edit_roles=ADMIN_ONLY` in NAV, or move the eight routes onto `can_edit(user, "mail-filters")`.
app/routers/settings/devices.py:396 — `async def upload_machine_portrait` awaits nothing and does synchronous work on the event loop: `require_settings_edit`/`db.get` plus `save_portrait` (up to 12 MB read + Pillow verify/decode) — fix: plain `def`; it is the only `async` route among all device routes.

## MEDIUM

app/routers/vyrobitok.py:190 — `/vyrobitok/day-sync` calls `unfreeze_day` + `sync_google_sheets` with NO `sync_control.is_paused()` gate; `POST /settings` (overview.py:439) is ungated too. §14 says a pause stops reading AND writing, and `_guard_paused` (sheet_writeback.py:70) covers only writes — fix: gate both entry points like `queue.py:372`.
app/services/mail_accept.py:171 — `except (OSError, ValueError)` only; any other exception out of `_move_attachments` escapes with files already moved, no `db.rollback()`, no `undo_moves` (the `moved_pairs` list dies with the frame) → 500 plus a permanent disk/DB split — fix: catch `Exception`, and let `_move_attachments` append pairs into a caller-owned list.
app/routers/mail.py:1088 — `reject_email` logs and swallows a failed move back to the spool, then still sets `status="відхилено"` and commits (mail.py:1091-1092): the DB says rejected while the files stay in export — fix: abort the reject (or mark the attachments) instead of committing over a failed move.
app/routers/mail.py:1541 — `clear_order_row` blanks A:K in the sheet before `db.commit()` (mail.py:1592); a failed commit restores the order but the placeholder row stays wiped, with no compensation — fix: blank after the commit succeeds.
app/mail_reader.py:368 — the spool directory is `attachments_dir / email_message.uid` with no `uid_validity` (same at mail.py:545, 1080, 1522); after a UIDVALIDITY change two different letters share one folder and their files mix — fix: fold `uid_validity` into the folder name with a back-compat lookup.
app/routers/settings/connections.py:116,151 — `save_imap_settings` (writes the mailbox password) and `test_imap_connection` use an inline `can_edit(user,"imap")` with NO loopback, while the sibling secret routes (`furnaces/password`, `machines/password`) go through `require_settings_edit` where `loopback=True` — the exact asymmetry `deps.require_admin:117` claims to have cured — fix: `require_settings_edit(request, db, "imap")`.
app/routers/settings/overview.py:374 — `POST /settings` writes every secret (IMAP password, service-account JSON, license key) with no loopback check, unlike every per-section route — fix: apply the same loopback envelope, or document the exception in `require_settings_edit`.
app/routers/settings/backup.py:43,83 + update.py:67,90,108 + users.py:24,76,102,126 + feedback.py:21 — the "admin + loopback" gate is hand-rolled ten more times although `deps.require_admin` exists as the single copy (audit step 2.7 is half done). Not cosmetic: these ten return `login_redirect` (303) while `require_admin` raises 401, so a mechanical swap breaks HTMX — fix: add `require_admin(..., unauth="redirect")` and migrate all ten.
app/routers/queue.py:407,443 — docstrings on `/sheets/import-history` and `/sheets/reconcile-deletions` say "Admin + loopback, як і решта дій синку", but neither route calls `is_loopback_request` (no `loopback` reference exists in queue.py) — fix: add the check or correct the docstrings.
app/routers/auth.py:79,102 — `GET/POST /license` have NO auth at all (they are exempt from `license_gate`, web.py:857), yet NAV marks the `license` section ADMIN_ONLY (settings_nav.py:266). Once the app IS licensed, a logged-out visitor at the PC sees the licence passport (expiry, machine ID) and can POST a replacement key — fix: keep the anonymous path only while `not status.valid`, require admin otherwise.
app/routers/clients.py:48 — `select(Order).where(Order.client_name.isnot(None))` loads EVERY order ever (archive keeps all, ~92/day) into memory on each `/clients/{id}` and `/clients/{id}/pane` request — fix: filter to the client's candidate names in SQL, or cap by a date window.
app/services/furnace.py:393-427 — `poll_target` writes `attempted_at`, `error`, `reading`, `frame_at`, `captured_at` OUTSIDE `_states_lock` (only the `setdefault` at 390-392 is locked) although the comment at :188 promises the lock; disk I/O and `db.commit()` sit between the writes, so `snapshot` (:566) can read a torn combination — fix: build the new state, then assign under the lock (as machines.py:795-825 does).
app/services/furnace.py:367,411 — `grab()` catches only `FurnaceVncError` and `read_panel(image)` is unguarded, so one unexpected error kills the tick for ALL furnaces (machines.py:906 catches broadly) — fix: wrap both in `except Exception`.
app/services/machines.py:753 — `screen_is_sisma(frame)` has no try/except while its neighbours `read_progress_percent` (:759), `screen_is_completed` (:785) and `read_sisma` do; an exception escapes `poll_target` and aborts `poll_all` (:918) for the remaining machines — fix: same guard.
app/services/machines.py:308-326 — `_calib_signatures` is keyed by `key` only, not by folder; after the calibration folder changes (devices.py:282) stale signatures suppress new frames and `_most_redundant` tries to `unlink` names from the old folder — fix: key on `(folder, key)`; `reset_state_for_tests` (:1600) does not clear it either.
app/routers/settings/devices.py:295-305 — the calibration path is accepted with no validation and no write probe, and is operator-editable, while later failures are swallowed at `logger.debug` (machines.py:488, 521) → the banner shows 0 frames forever with no signal. Also collides with the rule "write-проба шляху лишається адмінською" — fix: probe on save and surface the error.
app/services/machines.py:1290,1308 + app/services/queue_view.py:500 — `machine_side_context` calls `snapshot(db)`, `strip_summary(db)` calls it again, and the queue context calls `sisma_context(db)` for a third — three `configured_targets` + up to six `Order`/`ReworkRecord` queries per queue render and per 10 s strip poll — fix: compute `snapshot` once and pass the cards down.
app/services/furnace.py:458-468 — `prune_readings` loads all expired `FurnaceReading` rows as ORM objects and deletes them one by one; a missed day means tens of thousands of objects in memory — fix: `delete(FurnaceReading).where(...)`.
app/services/vyrobitok.py:252,296 — `freeze_due_days` loads every non-archived Order, then `_snapshot_orders` → `_live_units` re-loads them for EACH day being frozen (`_live_units` ignores its `first`/`last` args in the query) — the catch-up path after a long outage is O(days x all live orders) — fix: filter by day in SQL, or compute once for the whole range.
app/routers/settings/connections.py:385 — `_folder()` in `settings_selfcheck` is a weaker second copy of `overview.py:88 check_path_status`: only `exists()/is_dir()`, no write probe, yet the step is labelled "Папка export доступна на запис" (:421) and the docstring (:340) promises the same probes — a green self-check does not mean writable — fix: call `check_path_status(path, write_probe=True)`.
app/routers/settings/handout.py:16 — `POST /settings/handout-qc` is an orphan: key `handout` is in neither `NAV` nor `build_slabs`, no template posts to it, and the redirect `/settings?saved=1#handout` targets a non-existent anchor. The QC checklist can only be toggled by curl — fix: add the NavItem + partial + slab, or drop the route.
app/routers/palette.py:53-84 — `COMMANDS` is a second hardcoded map of the app's screens alongside `_topbar_nav.html`, with no guard test; a new screen silently misses the Ctrl+K palette — fix: derive it from one registry, or add a snapshot test like `test_settings_nav.py`.
app/routers/sync_journal.py:73 (+ queue.py:348,369,417,447; mail.py:1224,1282,1323,1350,1389,1457,1483; machines.py:130,158; settings/feedback.py:24; settings/backup.py:46,86) — ~20 more inline `user.role != "адмін"` checks outside `deps.require_admin`; the "one gate" invariant holds only inside the settings package — fix: converge on the shared gate.

## LOW

app/mail_export.py:256 — `except Exception: pass` while rolling back a partial `restore_attachments_to_spool`; a file stranded in export during un-accept leaves no trace (contrast `undo_moves` at :222, which reports) — fix: collect and log the errors.
app/link_attachments.py:208 — only `LinkDownloadError` triggers `dest.unlink`; a mid-stream `ConnectionError`/`OSError` leaves a truncated file in the spool that later counts as a real attachment — fix: `except Exception` → unlink → re-raise.
app/mail_reader.py:581 — `cutoff = date.today() - timedelta(days=IMAP_LOOKBACK_DAYS)`; on a night shift at 00:05 the window jumps a calendar day early — fix: `business_today()`.
app/routers/mail.py:570 — `db.commit()` after the link file is already on disk, unguarded; a commit failure leaves an orphan spool file with no `Attachment` row — fix: try/except + `path.unlink`.
app/triage_status.py:73,92 — per-row N+1: the triage template calls `files_on_disk` and `triage_readiness` → `_has_rows_but_no_files`, so every attachment is `Path.exists()`-ed twice per row on each 15 s poll (`MAIL_ATTACHMENTS_PATH` is env-overridable to a share) — fix: compute the on-disk set once per row.
app/web.py:878 — `license_gate`'s docstring claims it "Runs outermost", but `log_slow_requests` is registered after it and therefore wraps it — fix: correct the comment.
app/services/machines.py:549-558 — the `_HOST_RE` check in `resolve_frame` is dead: with `known=True` a regex miss falls through and the path is built anyway — fix: return None, or drop the check.
app/services/machines.py:253-256 — `frames_root()` does `mkdir` on every `GET /machines/{key}/frame.png` (via `frame_path`→`resolve_frame`) — a GET with a filesystem side effect on a hot path.
app/services/machines.py:469-472 — the cap counts ALL `*.png` but only `d-*` are evicted; a folder full of `pct-*` frames makes `_most_redundant` return None and manual collection stops silently forever.
app/services/machines.py:110 — `MachineConfigError` is declared and exported but never raised or caught; `validate_address` (:47) is imported only for `__all__` — dead code left after the refactor.
app/machine_sisma.py:100-102 — `SismaReading.printing` is computed but read only by tests; production reads `percent/layer/layers_total/started_at/ends_at/lasing`.
app/routers/settings/backup.py:121 — `_iso_to_tab_name` re-derives the tab name from the filename although `SheetSnapshot.tab` already stores it and the ZIP uses that (sheet_backup.py:410); a non-standard tab name gets two different download names.
app/routers/settings/materials.py:60 — `can_edit(user,"materials")` gates PAGE VIEW where `can_see` is the registry's semantics; harmless today (`edit_roles=None`), but a read-only role would get 403 on the screen.
app/routers/settings/__init__.py:62 — the `.devices` re-export omits `save_machine_calibration_path` and `open_machine_calibration_folder` although `__all__` is declared the package's public contract; a test monkeypatching them via the package would fail with AttributeError.
app/sheet_sync_service.py:120-127 — `_header_mismatch` is only cleared when the same tab is re-read successfully; a tab that later fails at `get_all_values` keeps a stale banner entry forever.
app/services/attempt_limit.py:35 — `_buckets` is pruned only when the specific key is queried after `reset_after_seconds`; keys are `login+IP`, so an unbounded set of attempted logins grows the dict for the process lifetime.
app/routers/machines.py:110 — `POST /machines/refresh` lets any logged-in user trigger `poll_all(db)` (network capture of every machine) with no rate limit.

## Verified OK

- No router imports `app.web` — grep across `app/routers/**` including the new `settings/` package is clean.
- Sheet writeback pool: `submit_sheet_write` is the single entry point and `_guard_paused` (sheet_writeback.py:70) stops every write during a pause; all four `await_on_writeback` call sites (orders.py:191,195,253,659; handout.py:783) commit BEFORE calling, and every `*_warm` fn opens its own `SessionLocal`.
- Row position is always verified: `write_sheet_fields` skips and logs `status="skipped"` when `write_order_fields` returns falsy (sheet_writeback.py:126-135); `issue_group_warm` resolves the row before clearing a fill and reports rather than guessing (sheet_writeback.py:536-542); `_unaccept_email` blanks only through `clear_order_row` identity matching (mail.py:1541).
- Sync writers that must stay synchronous ARE plain `def`: `undo_action`/`undo_last_action` (orders.py:687,717), `create_manual_order` (orders.py:~380, uses `.result(timeout=120)` on the pool), `post_vyrobitok_day_sync` (vyrobitok.py:160).
- Alembic chain 0033→0047 is strictly linear, single head, every `down_revision` present; 0045 handles the pre-existing unique index by PRAGMA lookup rather than a name constant. `app/schema.py` runs the real alembic upgrade on every startup path.
- Path traversal closed everywhere I checked: `/settings/sheets/download/{filename}` → `_safe_snapshot_path` (sheet_backup.py:368) requires an ISO date + `.csv`, then `resolve()` + `relative_to()` + `is_file()`; `/stl-preview/{token}/{filename}` re-derives the folder from an opaque token and revalidates the name (stl.py:43-66); `/feedback/images/{id}` and `/machines/portrait/{id}.jpg` build paths from integers; `/machines/{key}/frame.png` and `/furnaces/{key}/frame.png` resolve the key through process state, not the path.
- Literal-vs-`{param}` ordering holds: `settings/furnaces/password` (devices.py:111) above `{furnace_id}` (:128); `settings/machines/password` (:266), `calibration-path` (:282), `calibration-folder` (:308) above `{machine_id}` (:332); `/machines/calibration.zip` and `/machines/sisma` above `/machines/{key}/frame.png`; `/mail/sync` above `/mail/{email_id}`; `/mail/filters/dismiss-suggest` above `/mail/filters/{rule_id}/...`.
- Secrets never reach Jinja: `overview.py:193-195` strips `SECRET_SETTING_KEYS` and passes only boolean `values_set`; `get_setting` degrades to None on `InvalidToken` (settings_store.py:244-260).
- Field-level settings authorisation is real, not just a hidden card: `post_settings` re-checks each field against its own section via `can_edit` (overview.py:388-394), and an unsectioned key stays admin-only.
- UIDVALIDITY dedup: `mail_reader.py:606` is the only `EmailMessage.uid` lookup in app code and it partitions by `uid_validity`, adopting blank legacy rows and refusing dead-namespace matches.
- Redirect hardening in `link_attachments.py:152-193`: hops are walked by hand with `allow_redirects=False`, `_host_allowed` runs BEFORE each request, relative `Location` resolved via `urljoin`, capped at 5.
- The undownloaded-link accept gate lives in the SERVICE (`mail_accept.py:111-118`), so a direct POST to `/mail/{id}/accept` hits it; only the explicit operator checkbox overrides it.
- Move-then-commit with compensation on the mail happy paths: `mail_accept.py:186-204` and `mail.py:1594-1610` both `rollback` + `undo_moves` + `clear_export_cache` and report undo errors rather than swallowing them.
- Background workers own their sessions per iteration (`web.py:327,490-518`); only the documented post-watchdog zombie keeps one, deliberately.
- Business date: no new `date.today()` in queue/handout/vyrobitok/orders/devices; `order_detail` (orders.py:900) and the vyrobitok freeze use `business_today()`. The two remaining hits (`mail_reader.py:581`, `stats.py:45`) are pre-existing.
- New models follow the local-time rule: `SavedQueueView.created_at` and `VyrobitokDay.*_frozen_at` have no `server_default=func.now()`.
- Saved queue views are private: `user_id` is in the unique constraint and in every query (`queue_view.py:648-736`); `normalize_view_query` whitelists keys and clamps values.
- All `/settings/*` and `/diag/*` POST/DELETE routes have a gate; `tests/test_admin_gate.py` holds that as a snapshot with a justified allowlist.
- No leftover `app/routers/settings.py` module shadowing the package; no dead imports or unreferenced functions in the settings package or `routers/mail.py`.
- No bare `except: pass` in the new services; the wide catches log with `logger.exception` and roll back.
- Guard tests pass at HEAD: `test_route_inventory.py`, `test_settings_nav.py`, `test_frontend_assets.py` — 33 passed.
