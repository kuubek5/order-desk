# Хендоф у новий чат — 07.09.26 (вечір)

## 0. Перше, що зробити

```bash
git worktree add ../CRM_Laba-next master
```

У головному дереві періодично працює другий чат. Два чати на одному дереві = гонка
`route_inventory.txt` і фантомні падіння тестів.

## 1. ГОЛОВНЕ ЗАВДАННЯ ЦЬОГО ЧАТУ

**Прогнати все зроблене на тестовому сервері й лише потім релізити.** Рома просив саме це.
Покроковий ранбук — `MEMO.md`, розділ **§13** (десять пунктів, у порядку залежностей).

Порядок роботи:

1. підняти dev-сервер (див. §4 нижче);
2. пройти §13 `MEMO.md` пункт за пунктом, записуючи результат;
3. усе зійшлося → реліз 0.11.4 скілом `/kuubmill-release`;
4. щось поводиться інакше → **не релізити**, розібратись і виправити.

## 2. Стан коду

- master `e91ee23`, **29 комітів поверх тегу v0.11.3** — у прод це ще не поїхало.
- 2271 тест зелений, ruff чистий, `.mypy-baseline` = 212.
- Дві нові міграції: **0049** (прибрано мертву `attachments.staged_to_export`) і
  **0050** (`users.session_epoch` — покоління сесій).
- **Побічний ефект оновлення, попередити Рому:** після встановлення 0.11.4 всі, хто був
  залогінений, увійдуть заново — секрет підпису сесій тепер окремий від ключа бази (K.8).
  Це разово.

## 3. Що зроблено (блоки закриті)

| Блок | Суть одним рядком |
|---|---|
| **B** | Копія охоплює всі 29 таблиць; автокопія перед КОЖНИМ оновленням; звірка «нічого не зникло»; кнопка «Відновити рядок» |
| **S2** | Наряд-less роботи вертаються з архіву; чужа робота не переписується при зсуві рядків; видача пакетна (3 запити замість сотень); пауза зупиняє й читання |
| **M** | Зомбі-фетч не пускає другий; моргання шари не робить лист «прийнятим» без файлів; теки спулу `<uid_validity>_<uid>`; кеші прев'ю й спулу |
| **K** | Loopback на секрети й людей; тека кадрів адмінська; журнал змін налаштувань; `foreign_keys=ON` (з відмовою на брудній базі); секрет сесій окремо; зміна пароля обриває сесії |
| **D** | Збій пристрою не валить воркер; статус печі вимагає ДВОХ згодних сигналів; стеля розміру кадру; `poll_target` розбито |
| **T** | Імʼя інсталятора з тегу; mypy-храповик; стелі версій `gspread`/`cryptography`/`asyncvnc`; сторож дат у тестах |
| **U2** | `--mat-*` і шкала `--z-*` у токенах; один слухач на шестерні; друк паспорта й архіву |

Деталі кожного — у `PLAN.md` (статуси зі змістовним описом) і в повідомленнях комітів.

## 4. Як підняти dev-сервер

**Порт 8002 зайнятий процесом-привидом.** PID 19248 слухає й відповідає, але його немає в
`Get-Process`, а `taskkill` каже «процес не знайдено». Звільнить лише перезапуск ПК.
**Перевірки на ньому НЕ РОБИТИ** — там старий код, він одного разу вже дав хибне «все гаразд».

Робочий спосіб — 8003 із worktree:

```powershell
$env:KUUBMILL_DATA_DIR = "P:\AI-Projects\CRM_Laba"
Start-Process -FilePath "P:\AI-Projects\CRM_Laba\.venv\Scripts\python.exe" `
  -ArgumentList @("-m","uvicorn","app.web:app","--port","8003","--host","127.0.0.1") `
  -WorkingDirectory "P:\AI-Projects\CRM_Laba-next" -WindowStyle Hidden
```

`KUUBMILL_DATA_DIR` обовʼязковий: без нього worktree заведе порожню базу (пастка з аудиту 06.09).
Якщо 8002 звільнився після перезавантаження — краще 8002, як просив Рома.

Логін: dev-адмін `claude` і оператор `qa-claude` (обидва в тестовій базі). Паролі не збережені —
перевстановити скриптом, див. памʼять `project_dev_login_pane`.

## 5. Що лишилось у PLAN (29 рядків)

- **Можна без Роми (18):** P.1–P.3, P.6 (швидкодія: «Раніше» 1,5 с, `/clients/{id}/pane`,
  пагінація пошуку й журналу), C.1/C.2 (пошта), C.5/C.11 (гейти прав), C.8–C.10, C.12
  (LOW-список), U.6 (кольори в `settings.css`).
- **Потрібне око Роми (7):** U.1–U.5, U.7, U2.3 і решта U2.4.
- **Потрібен Рома фізично (4):** **B.5 прод**, D.1 Go-агент (перевстановлення на ПК верстатів),
  D.6 SISMA (потрібні свіжі кадри), P.5 перевірка на проді.

## 6. Пастки, які кусали саме в цій роботі

- **Жива перевірка ловить те, чого не ловлять тести.** Двічі рятувало: старий код на 8002 і
  коментар зі словом `require_settings_edit` усередині роута, який збив сторожа гейтів
  (`tests/test_admin_gate.py` читає ТЕКСТ тіла функції, не AST).
- **curl із Git-Bash шле кирилицю не в UTF-8** — тестові рядки лягають мохібейком, і `LIKE`
  їх не знаходить. Прибирати за `id`/`created_at`.
- **Ревʼю помиляється.** Тричі: `config_error` живий, `_HOST_RE` вживається, а буквальне
  `времен\w*` зʼїдало б «временная коронка» (це ПММА). Перевіряти кожну «мертву» знахідку.
- `tests/route_inventory.txt` — новий роут = новий рядок у знімку.
