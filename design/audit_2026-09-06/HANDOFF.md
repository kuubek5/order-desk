# Хендоф у новий чат — 07.09.26

## 0. Перше, що зробити

```bash
git worktree add ../CRM_Laba-B2 master
```

У дереві `P:\AI-Projects\CRM_Laba` періодично працює другий чат. Два чати на
одному дереві = гонка `route_inventory.txt` і фантомні падіння тестів.

## 1. Де ми зараз

Розділ **B — збереження даних** (`design/audit_2026-09-06/PLAN.md`, рядок 71)
закритий по коду повністю: B.1–B.3 і B.6–B.8 ✅. Лишились **B.4 і B.5** —
випробування, а не код:

- **B.4** — ранбук на локальному 8002 (тестова таблиця): створити бекап →
  відновити в порожню базу → звірити числа; імітувати оновлення (підмінити
  версію) → побачити зелений рядок звірки; видалити тестову роботу → знайти
  вміст рядка в журналі й натиснути «Відновити рядок». Записати результат у
  PLAN.md.
- **B.5** — те саме на робочому ПК ПІСЛЯ локального прогону, у порядку:
  бекап → оновлення → «Стан системи» → перший синк → звірити, що кількість
  рядків у вкладці до і після збіглася.

Робочий ПК ще на попередній версії — B.5 починається з оновлення.

## 2. Що зроблено в блоці B (для розуміння коду)

| Крок | Суть | Головні файли |
|---|---|---|
| B.1 | Бекап на всі 29 таблиць + перелічник рядків, звірка при відновленні, відмова від новішого формату | `app/backup.py`, `tests/test_backup_covers_every_table.py` |
| B.2 | Звірка «після оновлення нічого не зникло» на «Стані системи» | `app/services/health_snapshot.py` |
| B.3 | Слід від стертого рядка: A:K читається ДО стирання | `app/sheet_writer.py:clear_order_row` |
| B.6 | Повна копія бази перед КОЖНИМ оновленням, `VACUUM INTO` у `backups/pre-update/`, останні 5 | `app/pre_update_backup.py`, `app/routers/settings/update.py` |
| B.7 | Стеля 20 стирань рядків таблиці за годину; перевищили — рядок лишається | `app/sheet_erase_guard.py` |
| B.8 | Кнопка «Відновити рядок» у «Журналі синку», вміст лежить даними | `sync_logs.erased_*` (міграція 0048), `app/routers/sync_journal.py` |

Наскрізне правило блоку: **безпечний бік відмови — не зробити, а не зробити
навмання**. Не підтвердили позицію — не стираємо. Перевищили стелю — не
стираємо. Рядок зайнятий — не відновлюємо. Не прочитали рядок — вважаємо
зайнятим, а не вільним.

## 3. Контекст

**`design/audit_2026-09-06/CODE_REVIEW.md`** — повне код-ревʼю 07.09.26.
Черги після B: `S2` синк (HIGH), `M` пошта (HIGH), `K` ядро/безпека,
`D` пристрої, `T` тести/CI, `U2` фронтенд. Деталі — у `review_full_*.md`.

`AUDIT.md` — аудит 06.09.26, уже влитий.

Наступний за пріоритетом після B — **S2** (синк): S2.1/S2.2 про наряд-less
рядки, які зараз не воскресають з архіву ніколи.

## 4. Реліз

- Тег `v0.11.3` стоїть на `2e2dafb` (не на `ee54ca6`): перший Release-прогін
  упав, тег переставили на HEAD. Реліз у `kuubek5/order-desk-releases` вже
  містить B.1–B.3.
- B.6–B.8 у релізі ЩЕ немає — вони після тегу.
- Реліз на прод — скіл `/kuubmill-release`, не руками.

## 5. Нагадування, які тут кусають

- Порти: **8000 — прод, 8002 — dev**. Рестарт dev: `.\dev_restart.ps1`.
- Повний прогін тестів — один раз наприкінці блоку; під час роботи цільові
  файли + два сторожі (`test_route_inventory.py`, `test_frontend_assets.py`).
- Новий роут = рядок у `tests/route_inventory.txt`.
- Запис у таблицю ніколи не на event loop; позиція рядка звіряється завжди
  (`_resolve_row` → `None` = пропустити запис).
