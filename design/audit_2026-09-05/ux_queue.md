# UX: Черга / Картка / Нова робота / Пошук / Журнал / Кабінет (агент, 05.09.26)

## Прогалини (ранжовано)
1. [В] Статус-колонка в черзі прихована CSS (v2a_queue.css:292), select живий у DOM. Поставити «проблема»/«переробка» — лише через картку. Порушує «один клік».
2. [В] /journal нема в головному меню; шлях лише попап «Останні дії» → «Увесь журнал» або підменю налаштувань (_topbar_nav.html:143-146).
3. [В] Рядки /journal не клікабельні (journal.html:37) — з журналу не відкрити роботу.
4. [С] /orders/new дублює inline-форму черги, без «+ ще рядок», без посилання в навігації; існує лише як fallback помилки валідації (orders.py:413-418).
5. [С] Картка роботи не має CAM-коментаря, «Оператор», шпильки, бейджів rework/rush/«технік змінив» — усе це є в рядку черги.
6. [С] У картці поле Sum3D для активної переробки підписане як звичайне «Sum3D ID», хоча пише в rework.sum3d_id; черга каже «ID перер.».
7. [С] «Прострочено» і «Мої зараз» вимкнені (_queue_sidebar.html:10-18), логіка жива, CLAUDE.md §9 описує як штатні.
8. [С] /search показує архівні роботи повністю редагованим рядком (_order_row.html без read_only) — можна змінити Sum3D закритої роботи.
9. [Н/С] /search без полла; при двох операторах застаріває мовчки.
10. [Н] dblclick job_code → відкрити папку: мовчазний .catch (queue.js:273-294); аналог у app.js:104-108 дає тост.
11. [Н] _queue_peek_row.html — порожня заглушка.
12. [Н] /account optimistic UI: при провалі /account/appearance тост є, відкат стану нема (account.html:443-453).

## Факти
- Черга: ~35 дій; полл 15с #queue-rows з гейтом на фокус, збереження скролу; 4 варіанти порожнього стану; slide-over паспорта через iframe (queue.js:745-781).
- Хоткеї: Enter/Shift+Enter у CAM-коментарі; Enter приймає ghost Sum3D; Esc закриває слайдовер/попап. Глобальних хоткеїв (J/K, Ctrl+K) нема.
- Картка: нема полла; після дії JS reload через 900мс; read_only для архіву; «← До черги» скидає фільтри; Esc не працює при прямому відкритті.
- Undo: лише своя дія, вікно 5 хв, concurrent-change guard (undo.py:99-104), не під паузою синку.
- Кабінет: 3 вкладки (Профіль/Вигляд/Робочі екрани), пресети оформлення, live-прев'ю.
