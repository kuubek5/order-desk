# Журнал змін

Всі помітні зміни KuubMill. Формат — [Keep a Changelog](https://keepachangelog.com/uk/);
версії — [семантичні](https://semver.org/lang/uk/).

Новий запис пишемо в найвищий розділ без дати (він зветься «Наступна версія» на
екрані); на релізі `bump_version.py` проштамповує його номером і датою. Мова
записів — проста, «що змінилось для оператора», а не технічні деталі.

## [Незалежно від версії]

## [0.13.21] — 2026-09-10
### Виправлено
- **Верстати нового покоління показують, яка робота фрезерується.** На
  CORiTEC 150i PRO і 250i PRO+ назва програми не потрапляє в заголовок
  вікна, тому зв'язка з рядком черги на них не працювала. Тепер програма
  читає назву з екрана верстата (список JOBS, рядок із ▶) і підсвічує
  роботу в черзі так само, як на інших верстатах. Підсвітка з'являється,
  лише коли прочитане точно збігається з роботою в черзі; поки на верстаті
  відкрито інший екран (не JOBS), її не буде.

## [0.13.20] — 2026-09-10
### Додано
- **Віджети черги вимикаються в шестерні.** У шестерні над чергою два нові
  рядки: «Віджети в шапці» (Верстати, Sisma) і «Бокова панель» (Пошта, Пічки,
  Верстати, Таблиця, Видача). Вимкнений віджет зникає одразу, увімкнений —
  повертається без перезавантаження сторінки. Сховаєте всю бокову панель —
  черга займе всю ширину. Вибір зберігається у вашому обліковому записі, тож
  їде з вами на інший комп'ютер. За замовчуванням усе видно, як і було.

### Виправлено
- **Кнопки в шестерні черги більше не вилазять за край** — довгий ряд
  перемикачів переноситься на новий рядок, без горизонтальної прокрутки.

## [0.13.19] — 2026-09-10
### Додано
- **Заготовки: список «До замовлення» розкладено по днях.** Кожен день —
  заголовок із датою, днем тижня й кількістю дисків та власною галочкою:
  один клік знімає або ставить увесь день. Біля рядка тепер лише час.
- **Заготовки: фільтр за днями й матеріалом.** Чіпи над списком
  (`10.09`, `09.09`… і `ZR`, `PMMA-PEEK`…) ховають і показують диски — і в
  списку, і в таблиці під ним. Сховане в буфер не йде: копіюється рівно те,
  що видно позначеним. Та сама позиція з різних днів іде одним рядком —
  `mono a2 18(2)`.

### Виправлено
- **Згорнуте меню налаштувань — іконки знову в одну лінію.** Пункти з
  вкладками (Мій акаунт, Google Таблиця, Пошта) стояли лівіше за сусідів,
  уся колонка іконок — лівіше за логотип, а «Вийти» й версія внизу —
  правіше. Тепер усе на одній осі.

## [0.13.18] — 2026-09-10
### Додано
- **Плашка «на погодженні» в черзі.** Якщо технік пише в коментарі для CAM
  «погодження …», «на погодженні» чи «не погоджено», біля наряду з'являється
  фіолетова плашка — робота ще погоджується з лікарем. Вона ж є в паспорті
  роботи. Коли технік допише «погоджено», плашка зникне сама з наступним
  оновленням з таблиці. Вже погоджені роботи («погоджено», «погоджен») плашки
  не отримують.
- **Заготовки: вибір, що копіювати комірниці.** Кожен рядок списку «До
  замовлення» має галочку й дату, коли диск створено; кнопка «Скопіювати для
  комірниці» копіює лише позначене. Кнопки «Усі» і «Жодного» — поруч.

### Змінено
- **Порядок днів у черзі — за часом:** Вчора · Сьогодні · Завтра · Раніше.
  Відкривається черга, як і раніше, на «Сьогодні».
- **Заготовки: новий вигляд замовлення** — одна позиція на рядок, як пишуть
  люди: `mono a3 18`, `emo a2 16`, `zr s1 14`; однакових кілька — у дужках
  `zr a2 25(2)`. Свіжі диски — згори, і в списку, і в таблиці під ним.
- **Заготовки: історія замовлень переїхала під список** і згорнута — щоденний
  список більше не ховається під нею. Одразу після «Замовлено» вона розгорнута
  сама, з кнопкою «Скасувати» під рукою. Тепер видно 30 останніх замовлень.

### Виправлено
- **Заготовки з виробником у назві йшли комірниці сирими іменами файлів.**
  Назви на кшталт `zr18_18-Monolith-A2-x37`, `zr20_20-Monolith-a3-5-x24` (колір
  A3.5) чи `zr14_14-Emotions-A3-x843PRO` програма не розуміла. Тепер вони
  читаються як `mono a2 18`, `mono a3.5 20`, `emo a3 14` — і ті, що вже чекають
  замовлення, перечитаються самі з наступним скануванням теки.
- **Кнопки «Скопіювати» в налаштуваннях копіювали мовчки.** Тепер кнопка
  зеленіє з ✓ — видно, що скопійовано.

## [0.13.17] — 2026-09-10
### Виправлено
- **Журнал зв'язку більше не звинувачує брандмауер наосліп.** Коли ПК
  верстата в мережі, а порт агента мовчить, причин дві, і ззовні їх не
  розрізнити: агент на ПК не працює, або порт закриває брандмауер. Досі журнал
  називав лише брандмауер — і помилився саме тоді, коли це було важливо: на
  150i і 250i агент падав, а брандмауер був у порядку. Тепер журнал називає
  обидві причини, а першою дією радить подивитись, коли стартував агент.
- **Верстати більше не зникають на кілька хвилин кожні ~3 години.** Причина
  була в агенті на ПК верстата: він падав після ~2000 запитів назви програми й
  мовчав, доки його не підніме сторож. Виправлено в новій версії агента разом
  із двома поліпшеннями: сторож перевіряє агента щохвилини замість раз на п'ять
  хвилин, а лог агента тепер справді пишеться (у `C:\ProgramData\KMill Agent`).
  **Агент оновлюється окремо** — інсталятор треба один раз запустити на
  кожному ПК верстата; оновлення CRM його не замінює.

## [0.13.16] — 2026-09-10
### Додано
- **Журнал зв'язку з верстатами знаходиться через Ctrl+K.** Наберіть
  «зв'язок» або «обриви» — і палітра відкриє журнал обривів (для
  адміністратора). Меню від цього не росте: журнал — розбір після факту, а не
  щоденний екран, і як і раніше відкривається кнопкою «Журнал зв'язку» в
  шапці екрана «Верстати».

## [0.13.15] — 2026-09-10
### Змінено
- **Черга йде в тому ж порядку, що й Google Таблиця.** Раніше рядки
  сортувались за дедлайном «Здати до» (09:00 → 14:00 → 16:00) і за тим, коли
  робота потрапила в базу, — і черга читалась інакше, ніж таблиця, з якою її
  звіряють щодня: 8 вересня всі 93 роботи стояли не на своїх місцях. Тепер
  порядок — день, далі рядки згори вниз, рівно як у вкладці. Сортування
  кліком по заголовку колонки й пришпилені роботи працюють, як і раніше.

### Додано
- **Видача пам'ятає, кого ви шукаєте.** Відкрили теку роботи в прев'ю — її
  рядок і клієнт підсвічуються й лишаються підсвіченими після закриття
  прев'ю, поки не відкриєте інші файли або не поставите галочку «знайдено».
  Повернувшись від лотка, одразу видно, чию коронку шукали. У шапці самого
  прев'ю тепер написано клієнта, матеріал і кількість — на повному екрані
  сторінку за ним не видно.

### Виправлено
- **Журнал зв'язку з верстатами більше не суперечить сам собі.** Про обрив
  він писав водночас «ПК у мережі озивається» і «чи живий ПК — невідомо», а
  поради, що перевірити, губились. Тепер пояснення цілісне: якщо ПК відповів,
  а мовчить лише порт агента, журнал прямо каже, що винен брандмауер або
  профіль мережі на ПК верстата.
- **Тривалість обриву зв'язку показується повністю.** Відлік ішов від
  моменту, коли застосунок визнав обрив, а не від останньої відповіді
  верстата, — з кожного обриву губилось 15–25 секунд.

## [0.13.14] — 2026-09-10
### Виправлено
- **Верстати показувались по-різному в різних місцях.** Смуга над чергою,
  бічна панель і картки на екрані «Верстати» рахували стан верстата трьома
  різними способами — той самий верстат у ту саму секунду міг бути «запуск» в
  одному місці й «стоїть» в іншому. Ба більше, у бічній панелі **вигляд
  плитки суперечив її ж підпису**: верстат із завантаженою програмою
  притемнювався як зупинений, хоч сам про себе писав «запуск». Тепер стан
  рахується в одному місці й однаковий скрізь.
- **«Програма не йде» більше не кажеться про верстат, екран якого застосунок
  читати не навчений.** На пласких CORiTEC (150-й) назва програми стоїть на
  екрані, а не в заголовку вікна, і смуга відсотка інша — застосунок не бачив
  нічого й писав «програма не йде» про верстат, який у цю мить фрезерував.
  Тепер він каже правду: «екран цього верстата не читається» і підказує
  увімкнути «Калібр.» у налаштуваннях. Верстати, від яких відсоток колись
  приходив, як і раніше чесно показують «стоїть».
- **«Де ця робота?» — розслідування по наряду.** Вводите номер наряду (або
  Sum3D, клієнта, техніка) — застосунок проходить увесь шлях і каже, на якому
  кроці робота загубилась: чи є рядок у таблиці, чи стала вона роботою, чи в
  черзі вона зараз, чи в архіві й чому. Відповідає на найчастіше питання цеху
  — «робота є в таблиці, а в черзі її нема», — на яке звичайний пошук не
  відповідає принципово: він шукає серед того, що вже є в базі. Дані про
  таблицю беруться з локальних копій вкладок, тому працює швидко, без
  інтернету й не заважає синхронізації; про те, коли знято копію, сказано
  прямо. Вхід — з екрана «Що не так».
- **Екран «Що не так» — проблеми людською мовою.** Застосунок і раніше помічав
  поломки, але писав про них мовою розробника й на різних екранах. Тепер вони
  зібрані в одному місці, і кожна відповідає на три питання: що сталось, що
  через це **не працює**, і що робити руками. Наприклад, замість
  «sheet_to_db/skipped» — «синхронізація не бачить сьогоднішньої вкладки →
  роботи за сьогодні в чергу не потраплять → перевір назву вкладки в таблиці,
  вона мусить бути рівно 10.09.26». Ловить і те, про що раніше не писалось
  узагалі: синхронізацію, яка не падає, а просто мовчить кілька годин.
  Однакові поломки, що повторюються щохвилини, згортаються в одну картку.
  У меню з'являється червоний рядок «Що не так» — **лише коли справді є
  проблеми** і лише в адміністратора.
### Додано
- **«Звіт для розробника» — одна кнопка замість семи скріншотів.** Налаштування
  → Стан системи → «Зібрати звіт»: застосунок складає текстовий файл, у якому є
  все, що потрібно, щоб розібратись у проблемі — самоперевірка (з живими
  пробами Google, пошти й тек), які налаштування задані, скільки рядків у
  кожній таблиці, журнал синку, журнал обривів зв'язку з верстатами, останні
  помилки та хвіст лога. Файл можна просто переслати. **Паролі, ключі й токени
  в нього не потрапляють** — лише «задано / не задано»; Sheet ID і логін пошти
  показані кінцями, щоб їх можна було впізнати, але не використати.
  Однакові записи журналу синку, що йдуть підряд, згортаються в один рядок
  «× N» — інакше хворий синк заповнював би собою весь звіт.
## [0.13.13] — 2026-09-10
### Додано
- **Адміністратор може зачинити будь-який екран для операторів.** Налаштування
  → Блокування розділів: обираєте екран і стан («роботи тривають», «у роботі»,
  «тестується», «проєктується») — оператори бачать екран-блокатор, адмін
  працює як завжди. Можна закрити не всім, а окремим ролям. Раніше так умів
  лише один екран зі всіх. У бічному меню адміністратора з'явився рядок
  «Зачинено: …» — щоб закритий колись розділ не знаходився за скаргою
  оператора.

### Виправлено
- Плита «Блокування розділів» у налаштуваннях завжди писала «усі відкриті»,
  навіть коли розділ був зачинений.
## [0.13.12] — 2026-09-10
### Додано
- **Журнал зв'язку з верстатами.** На екрані «Верстати» з'явилась кнопка
  «Журнал зв'язку». Там видно, коли саме рвався зв'язок з кожним верстатом
  (смуга по годинах або днях), хто рветься найчастіше, і — головне — розбір
  кожного обриву людською мовою: що це доводить, чого це **не** доводить і що
  робити руками. Раніше короткий обрив не лишав узагалі жодного сліду, тож на
  питання «чому воно рветься регулярно» відповісти було нічим.
- **Галочка «Діагн.» на кожен верстат окремо** (Налаштування → Обладнання →
  Верстати). Журнал ведеться для всіх верстатів завжди. Галочка вмикає
  детальний збір саме на тому верстаті, що турбує: на кожному обриві
  застосунок стукає в кілька портів ПК і зберігає, який порт що відповів і за
  скільки. Ставте її на проблемний верстат — і наступний обрив запишеться
  сам, без того щоб хтось стояв поруч.
- **Дата спливу ліцензії** видно на екрані ліцензії, а в черзі з'являється
  попередження заздалегідь.

### Виправлено
- **Перевірка «чи живий сам ПК» під час обриву не працювала жодного разу.**
  Застосунок мав у момент обриву перевіряти, чи ПК верстата взагалі
  відгукується — щоб відрізнити вимкнений комп'ютер від закритого порту. Через
  помилку в умові ця перевірка не запускалась ніколи, і плитка показувала лише
  загальне «немає зв'язку». Тепер працює.
- **Перезапуск застосунку посеред обриву більше не додає зайвих обривів.**
  Один безперервний обрив рахувався як кілька — саме та цифра, на яку
  дивляться, була завищена.
- Підпис «Кількість» у черзі стояв не над своїми цифрами.
- Кілька Sum3D ID в одній комірці архіву налазили на колонку «Технік».
- Вибір вигляду сповіщень у кабінеті не зберігався.

## [0.13.11] — 2026-09-09
### Швидкодія
- **Галочка «знайдено» на видачі спрацьовує миттєво.** Раніше кожен клік
  перебудовував увесь екран і чекав на відповідь — на бойових замірах від 0.7
  до 2.1 секунди. Тепер галочка малюється одразу, а найдорожче в перебудові
  (зіставлення імен клієнтів із теками в `export`) більше не рахується
  щоразу заново. Якщо запит не пройшов, галочка повертається як була.
- **Верстати перестали з'їдати процесор.** Розбір одного знімка екрана
  коштував до чверті секунди, а знімків сім і робляться вони кожні п'ять
  секунд — за цим у черзі стояли черга робіт, видача й синхронізація. Тепер
  удвічі-втричі дешевше, а числа на плитках ті самі: перевірено на 490
  збережених кадрах усіх шести верстатів, жодне не змінилось.
- **Нова робота з таблиці видно швидше.** Сьогоднішня вкладка тепер читається
  окремим, частішим тактом, а вчорашня й переглянуті дні — рідшим. Режим
  «Турбо» через це нарешті означає справжні 5 секунд: раніше він упирався в
  обмеження Google і мовчки пропускав читання. Зворотний бік: зняте синє на
  вчорашній вкладці доїжджає до CRM за 30 секунд замість 15.

### Додано
- **Верстат під час перевірки програми більше не виглядає зупиненим.** На
  екрані VALIDATE JOBS плитка показувала «—», як у вимкненої машини. Тепер
  пише «перевірка». Число з того екрана свідомо не беремо: смуга внизу рахує
  перевірку, а не фрезерування, і показати її як прогрес означало б сказати
  «робота на 56 %», коли робота ще не почалась.


## [0.13.10] — 2026-09-09
### Виправлено
- **Помилково введений наряд зникає з черги одразу, а не через 3-5 хвилин.**
  Якщо адміністратор помітив свою помилку і видалив рядок у таблиці, робота
  ще кілька хвилин висіла в черзі — і саме в цьому вікні оператор устигав
  узяти її в роботу. Тепер видалений рядок прибирається з першого ж читання
  таблиці (15 секунд на швидкості «Звичайно», 5 на «Турбо»). Захист від
  зворотного випадку — коли роботу щойно додали руками і рядок ще не встиг
  прочитатись — лишився на місці.
- **Віджет верстатів більше не показує мовчки застарілий стан.** Якщо його
  оновлення перестало проходити, він далі виглядав живим: над верстатом, який
  фрезерував 50 %, могло висіти «готово» від попередньої, давно знятої
  роботи. Тепер після 35 секунд без оновлення числа гаснуть і з'являється
  «дані застаріли», а при поверненні на вкладку віджет оновлюється одразу, не
  чекаючи такту.
- **Вигляд сповіщень «Аврора» нарешті показує власне появлення картки.**
  Вибраний стиль малював картку зі звичайним висуванням згори — власна
  анімація «Аврори» не спрацьовувала жодного разу з моменту її появи.


## [0.13.9] — 2026-09-09
### Виправлено
- **Виробіток: кнопка ⟳ у рядку дня тепер оновлює й виправлені руками
  клітинки.** Раніше, якщо число в клітинці виправили вручну, повторна
  синхронізація того дня наче нічого не робила — свіже число ховалось під
  ручним, і побачити його можна було, лише стерши клітинку геть. Тепер ⟳
  означає «перерахувати день начисто»: він скидає ручні правки цього дня в
  колонках, які CRM рахує сама, і пише, скільки їх скинув. Диски, підкови й
  опаки не чіпає — там ручне число і є значенням. Якщо синхронізація не
  вдалась, правки лишаються на місці.

## [0.13.8] — 2026-09-09
### Виправлено
- **Робота повертається з архіву, навіть якщо рядок над нею видалили або в
  неї лишилось старе ім'я.** Два випадки, які попереднє виправлення не
  покривало: рядки над роботою видалили, і на її старій позиції тепер стоїть
  сусідня жива робота — синхронізація створювала дубль, а стара лишалась в
  Архіві; і робота, яка МАЛА ім'я техніка чи клієнта, коли її архівували, а
  зараз рядок без імені — вона не впізнавалась. Обидва закриті, без дублів.

## [0.13.7] — 2026-09-09
### Виправлено
- **Робота без імені повертається з архіву, ким би її колись не завели.**
  Попереднє виправлення рятувало лише роботи, заведені як клієнтські. Ті, що
  колись були лабораторними чи поштовими, лишались в Архіві й далі: рядок у
  таблиці цілий, синхронізація каже «без змін», а у Виробіток робота не йде.
  Тепер повертаються всі, і одиниці лягають у ту колонку, якій рядок
  відповідає за правилами таблиці.

## [0.13.6] — 2026-09-09
### Додано
- **Роботи без імені клієнта тепер видно на Видачі.** Раніше вони випадали з
  цього екрана зовсім: були в черзі й в обліку, а там, де коронку фізично
  шукають у лотку, їх не було. Тепер вони стоять окремою групою «Без імені»,
  на своєму місці за порядком таблиці. Ім'я програма не вигадує — впишіть
  клієнта в таблицю, і робота перейде до нього.

## [0.13.5] — 2026-09-08
### Змінено
- **Архів показує день цілком.** Раніше він показував лише те, що випало з
  черги: за 3 вересня це 10 робіт із 74, і виглядало так, ніби решта історії
  зникла. Тепер Архів — уся історія робіт по днях, а рядки, яких більше немає
  в Google Таблиці, позначені окремо: приглушений рядок, мітка «зникла» і
  лічильник у шапці дня. Черга не змінилась.
- **Клієнтський рядок без імені підписаний «без імені»**, а не прочерком —
  в історії його тепер видно поруч із іменованими.

### Виправлено
- **Робота без імені клієнта повертається з архіву.** Якщо така робота колись
  потрапила в Архів, повернути її було нічим: програма шукала «ту саму роботу»
  за іменем клієнта, а імені немає. Рядок у таблиці цілий, синхронізація каже
  «без змін», а робота лежить в Архіві й у Виробіток не рахується — саме через
  це в таблиці за 3 вересня було 102 одиниці ПММА, а на екрані 96. Тепер такі
  роботи впізнаються за матеріалом і кількістю й повертаються самі. Щоб
  повернути вже застряглу роботу за минулий день, натисніть ⟳ біля цього дня
  на екрані «Виробіток».

## [0.13.4] — 2026-09-08
### Виправлено
- **Робочий день більше не зникає з черги через пробіл у назві вкладки.**
  08.09.26 вкладку в таблиці назвали « 08.09.26» — з пробілом на початку.
  Програма такої назви не впізнавала й день не читала взагалі: у черзі стояло
  «Сьогодні 0» при повній вкладці, і ніде не було видно, чому. Тепер зайві та
  невидимі пробіли в назві не заважають, а якщо вкладки за сьогодні серед
  датованих немає — про це є запис у Журналі синку разом зі списком вкладок,
  які програма побачила.
- **Роботи без імені клієнта більше не губляться в обліку.** Рядок, у якому
  є матеріал і кількість, але порожня колонка клієнта, не потрапляв у програму
  зовсім: у таблиці 102 одиниці, в черзі 96, і різниця ніде не показувалась.
  Тепер така робота стає в чергу з порожнім іменем — допишіть клієнта в
  таблиці, і воно підтягнеться до тієї самої роботи. Рядок із Sum3D ID, який
  усе ж не став роботою, лишає в Журналі синку номер рядка таблиці.
- **Один збій читання більше не відправляє цілий день в Архів.** Якщо таблиця
  віддала неповний список вкладок, програма чекає три читання поспіль і лише
  тоді щось архівує, а вкладку за вчора/сьогодні/завтра тримає до вашого
  підтвердження в будь-якому разі.
- **Час у Журналі синку й у підписі «Синхронізовано» тепер київський.**
  Показувався час за Гринвічем: о 20:17 підпис казав «17:15», і виглядало,
  ніби синхронізація три години мовчить.

### Змінено
- **Коли верстат перестає відповідати, програма каже, куди дивитись.** У
  момент обриву вона перевіряє, чи озивається сам ПК верстата. Озвався —
  на плитці стоїть, що мовчить саме порт агента, тобто справа в брандмауері
  або профілі мережі на тому ПК, а не в кабелі. Підказка чесна й тоді, коли
  певної відповіді немає.
- **Технічний лог перестав заповнюватись однаковими рядками.** Попередження,
  що повторювалось кожні кілька секунд, тепер пишеться раз на годину й
  показує, скільки разів подія повторилась. Через це в лозі знову видно
  справжні події.

## [0.13.3] — 2026-09-08

### Додано
- **Реєстрація оператора на сторінці входу.** Комбінація Alt+Enter показує
  кнопку «Реєстрація» — на звичайному завантаженні її немає. Форма питає лише
  логін і пароль; акаунт створюється робочим одразу, з правами оператора.
  Хто і коли завівся, видно в журналі дій.


## [0.13.2] — 2026-09-08
### Додано
- **Замовлення дисків комірниці складається саме.** Зʼявився розділ
  «Заготовки» в Налаштуваннях: вкажіть теку, куди CAM кладе нові диски, і
  програма покаже, які диски взяли з архіву після останнього замовлення, та
  дасть готовий текст для копіювання. Обходити шухляди й переписувати руками
  більше не треба. Відлік іде від кнопки «Замовлено», тому нічна зміна й
  вихідні враховані самі. Перед вмиканням є кнопка «Перевірити читання»: вона
  показує, що програма бачить у теці, і нічого при цьому не записує.
- **Синхронізація тепер каже, чи збігається з таблицею.** Після кожного
  проходу видно рядок «звірено 40 рядків, розбіжностей 0», а якщо розбіжність
  є — у якій саме колонці. Це та сама перевірка, яку робили очима, тримаючи
  таблицю поруч. Коли рядки саме рухались, програма чесно каже «звірка
  відкладена» замість того, щоб дати сумнівну цифру.
- **Програма почала запамʼятовувати, що і коли фрезерувалось.** Дані з екрана
  верстата раніше показувались і зникали; тепер вони зберігаються. Одразу це
  нічого не міняє, але через кілька тижнів зʼявиться відповідь на питання
  «скільки насправді фрезерується така робота», якої досі не існувало ніде.

### Виправлено
- **Прохання техніка «на швидку» більше не губиться.** Програма шукала лише
  один відмінок, тож коли технік писав «швидка» або «швидко плс», позначки в
  черзі не було взагалі. З 35 таких прохань помічалось 22.
- **Повідомлення «зв'язок відновлено» більше не зникає раніше часу.**


## [0.12.0] — 2026-09-08
### Змінено
- **Оновлення саме повертається назад, якщо нова версія не запустилась.**
  Раніше в такому разі лабораторія стояла до приїзду людини з ноутбуком.
  Тепер програма ставить попередню версію, повертає базу зі збереженої копії
  і перевіряє, що все піднялось. Зламану базу не викидає, а лишає поруч —
  на випадок розбору.
- **Копія бази може лягати на другий носій.** У Налаштуваннях зʼявилось поле
  «Друга копія на іншому носії»: вкажіть інший диск або мережеву папку, і
  копія автоматично ляже ще й туди. Досі всі копії лежали поруч із самою
  базою, тобто вмирали разом із диском. Порожнє поле нічого не міняє.
- **Стан пошти й таблиці тепер показує, коли вони востаннє ПРАЦЮВАЛИ.**
  Під час збою «остання спроба» оновлюється щохвилини і виглядає як життя;
  тепер поруч видно час останнього успіху й причину помилки.
- **Видача перестала підвисати.** Галочка «знайдено» більше не змушує всю
  програму чекати, поки відповість мережева тека. Помітно, коли теку гальмує,
  і критично, коли за програмою працюють двоє.
- **Порожній список тек більше не сприймається за правду.** Якщо мережева
  тека на мить відпала, клієнти на видачі більше не зникають на три хвилини —
  показується попередній список.
- **Дуже довгі назви клієнтів більше не ховають роботу.** Раніше тека з
  задовгим імʼям просто не створювалась, і коронка була на диску, але
  невидима на видачі.

### Виправлено
- **Оновлення могли тихо перестати приходити.** Екран показував би «у вас
  найновіша версія» назавжди, без жодної помилки.
- **Температура печі могла показати правдоподібне, але хибне число,** якщо
  цифра не вміщалась у кадр. Тепер у такому разі поле порожнє — як і має бути.
- **Числа на печах могли зникнути до перезапуску** після одного випадкового
  збою диска.
- **Лист більше не може вважатись прийнятим без частини коронок,** якщо
  мережева тека відпала посеред перенесення файлів.
- **Сторінка входу обмінника більше не зберігається як файл роботи.**
- **Ручне додавання роботи більше не може затерти рядок,** який у ту саму
  мить вписав технік.
- **Робота більше не роздвоюється,** якщо додати її руками рівно в мить
  чергової синхронізації.
- **Вимкнена піч більше не сповільнює опитування живих.**
- **Згорнута вкладка більше не звертається до сервера всю ніч.**

## [0.11.8] — 2026-09-08

### Змінено
- **Видача: кнопки «Видати» більше немає.** Достатньо галочки «знайдено» —
  вона знімає синю заливку рядка в таблиці, а це і є той сигнал, за яким
  живе лабораторія. Картка клієнта згортається, щойно все знайдено.
- **Видача: новий перемикач у шапці — картки по клієнтах або рядки в
  порядку таблиці.** Коли видачу ведуть за самою таблицею, згори вниз,
  картки заважали: у таблиці роботи одного клієнта стоять врозкид. Порядок
  робіт в обох виглядах однаковий, відрізняється лише групування. Вибір
  запамʼятовується на вашому акаунті.
- **Видача: вигляд «Плитки» прибрано.** Ним не користувались, а поруч із
  сусіднім перемикачем він плутався: клік по одному скидав інший.
- **Паспорт роботи: одна стрічка «Шлях роботи» замість двох.** Видно все за
  часом — коли робота зʼявилась і звідки, як мінялись статуси, коли її
  прорахували, хто саме і під яким Sum3D ID, коли вона пішла в архів. Поле
  «Здати до» прибрано: дедлайни в роботі не враховуються.

### Додано
- **Опак у ручному додаванні роботи.** Нове поле «Опак, к-сть» перед Sum3D
  ID: вводите число, портал сам дописує слово і кладе «2 opaq» у колонку
  «Коментар для CAM-оператора» — так само, як пишуть рукою. Кількість
  зберігається ще й числом, окремо від тексту, і рахується також із
  рукописних коментарів.
- **STL-прев'ю стало вікном.** Відкривається мініатюрою, тягнеться за
  шапку (і розгорнуте теж), памʼятає своє місце, розмір і нову кнопку
  світлого тла — сіра модель на білому, коли дрібний рельєф не читається на
  темному.

### Виправлено
- **Кнопка покажчика дня на видачі вмикалась, але не вимикалась.**
- **Ручне додавання клієнтської роботи більше не питає техніка й шлях
  роботи** — у клієнтському рядку їх нема звідки взяти; вони лишились у
  режимі «Лабораторія».
- **Дні в «Виробітку» вирівняні** — стовпець чисел більше не йде драбинкою.

## [0.11.7] — 2026-09-07
### Виправлено
- **Паролі пічок і верстатів переживають переїзд на інший компʼютер.** Вони
  зашифровані ключем машини, і в копії їхали як є — формально переносились, а
  на новому ПК не працювали: пічки й верстати довелося б заводити заново.
  Тепер вони їдуть усередині копії (сам файл під вашим паролем) і на новій
  машині шифруються вже її ключем. Старі файли копій читаються як раніше.

### Додано
- **«Зібрати файли для переїзду».** Копія бази несе рядки, але не самі файли,
  тож для переїзду потрібен ще архів із фото до записок зміни, скріншотами
  звернень і портретами верстатів. Кнопка стоїть поруч зі створенням копії,
  скільки файлів поїде — видно одразу, а всередині архіву лежить порядок дій
  на новій машині: що залити, що розпакувати, чому потрібен новий ключ
  ліцензії й що робити з листами, чиї вкладення лишились у скриньці.


## [0.11.6] — 2026-09-07
### Виправлено
- **«Створити копію» більше не падає на машині зі зміненим ключем шифрування.**
  Ключ прив'язаний до компʼютера, і після переїзду теки чи перевстановлення
  Windows частина налаштувань перестає читатись — саме тоді, коли копію й
  роблять, щоб перевезти дані. Раніше перше ж таке налаштування давало помилку
  і забрати роботи було нічим. Тепер копія збирається повністю, а нечитабельні
  налаштування перелічені в самому файлі — на новому ПК видно, що доведеться
  ввести руками.


## [0.11.5] — 2026-09-07
### Швидше
- **Картка клієнта відкривається миттєво.** Раніше на кожен клік застосунок
  перечитував усі роботи з іменами за весь час — а їх уже за дві тисячі, і
  щодня прибуває. Тепер читаються тільки роботи цього клієнта.
- **Вкладка «Раніше» менше смикає мережеву теку.** Значки папок рахувались для
  всіх шестисот робіт періоду, хоча на екран іде двісті. Тепер — тільки для
  показаних; на робочому ПК, де кожне звернення до Synology коштує секунди, це
  найпомітніше.
- **Пошук і Журнал синку віддаються сторінками.** Пошук показує 50 знайдених
  робіт (замість сотні одним шматком), журнал — 150 записів; унизу кнопка
  «Показати ще». Сторінка пошуку схудла вдвічі.

### Виправлено
- **Прийняття листа не лишає файли «між папками».** Якщо після переносу
  вкладень щось зривалось, файли лежали в export, а лист вважався неприйнятим —
  тріаж казав «файли зникли», хоча вони були на місці. Тепер вони завжди
  повертаються в лист, а якщо повернути не вдалось — про це буде сказано.
- **Не можна «відхилити» лист, який уже взяли в роботу.** Раніше статус
  мінявся, а робота лишалась у черзі, файли в export і рядок у таблиці: база
  казала «відхилено» про роботу, яку цех фрезерує. Тепер застосунок відмовляє й
  підказує спершу «Повернути в тріаж».
- **Нічна зміна більше не втрачає найстаріші листи.** Вікно пошуку пошти
  рахувалось за календарною добою, і після півночі стрибало на день раніше.
- **«Стан системи» більше не обіцяє зайвого.** Рядок «Папка export доступна на
  запис» тепер справді пробує туди записати, а не лише перевіряє, що тека є.

### Безпека
- **Дві важкі дії синку доступні лише з цього компʼютера** — повний імпорт
  історії та підтвердження масового видалення. Досі вистачало сесії адміна з
  будь-якого ПК у мережі лабораторії.
- **Кнопка «Оновити зараз» на екрані верстатів має паузу.** Затиснута кнопка
  зверталась до кожного цехового ПК без упину.


## [0.11.4] — 2026-09-07
### Виправлено
- **Відновлення з резервної копії знову працює.** У цій же збірці воно
  ламалося: застосунок почав суворо перевіряти звʼязки між таблицями, а
  лист і робота посилаються одне на одного, тож при заливанні копії база
  відмовлялася приймати листи й відновлення обривалося помилкою. Тепер
  звʼязки перевіряються в кінці, коли всі таблиці вже на місці, а непридатна
  копія пояснює причину замість порожньої помилки.
### Додано
- **Резервна копія тепер справді повна.** Раніше в неї потрапляло 13 таблиць
  із 27: переїзд на новий компʼютер привозив роботи й операторів, але лишав
  позаду пічки, верстати з паролями, матеріали, фільтри пошти, журнал дій і
  **весь Виробіток**. Тепер копія несе все й перевіряє сама себе: після
  відновлення числа звіряються з тим, що лежало у файлі, а розходження
  скасовує відновлення замість того, щоб лишити напівживу базу.
- **Перевірка «після оновлення нічого не зникло».** Перед встановленням
  оновлення застосунок рахує, скільки чого в базі, а після першого запуску
  нової версії звіряє. Результат видно в «Налаштування → Стан системи»:
  зелений рядок із числами або червоний перелік того, чого поменшало.
- **Стирання рядка в таблиці лишає слід.** Перед тим як очистити рядок,
  застосунок читає його вміст і записує в «Журнал синку» разом із номером
  рядка. Якщо щось стерлося не те, видно, що саме там було.

## [0.11.3] — 2026-09-07
### Виправлено
- **Принтер більше не зникає з віджета, коли на його екрані відкрите вікно.**
  Після друку SISMA сама показує звіт, і його рамка накривала краєчок підпису,
  за яким програма впізнає цей екран — через це принтер повертався в стрічку
  фрезерних верстатів саме тоді, коли щойно закінчив роботу.
### Додано
- **«Друк завершено» для SISMA.** Ознака — те саме вікно звіту, яке машина
  відкриває після останнього шару. За лічильником шарів фініш не видно: до
  кінця він не доходить, принтер одразу скидається в підготовку.
### Виправлено (код-ревʼю 07.09.26)
- **Синк не зітре чужий рядок.** Звірка позиції рядка вважала ПОРОЖНЮ
  клітинку підтвердженням — після видалення рядка вище стирання могло лягти
  на сусіда. Тепер порожня клітинка = «не наш рядок»: пошук по колонці або
  пропуск. Поштові роботи отримали якір (імʼя клієнта), рядок без якоря не
  стирається ніколи.
- **Один збій читання заливок більше не скасовує «видано» всім клієнтам
  вкладки** — без прочитаних кольорів таблиця нічого не «стверджує».
- **Ручне «Скачати вкладення» і «Скачати наново» звіряють нумерацію скриньки**:
  після перестворення теки провайдером файли чужого листа не причепляться.
- **Видача бачить сьогоднішні роботи в групових діях** («Усі знайдено»,
  «Видати N з M») — раніше вони мовчки пропускались, хоч і стояли на екрані.
- **«Повернути в тріаж» не видаляє відфрезеровану чи видану роботу** — далі за
  «прораховано» відкат неможливий, історія лишається.
- **Клієнт із кирилицею знаходить свою латинську теку** й серед сотень тек
  («Мулик Петро» ↔ «Mulyk Petro»); раніше при >40 теках тека не знаходилась,
  а прийняття листа заводило нову.
- **Вікно «Скасувати» справді 5 хвилин** (рахувалось у UTC — тривало 3 години).
- **Статистика рахує «Сьогодні» по робочій добі**, як черга.
- **«⟳» дня у Виробітку не програє фоновому заморожувачу** — ресинк не губиться.
- **SISMA не «зникає» після одного збою читання еталонів**; принтер не
  рахується серед «фрезерує / без звʼязку».
- Безпека: мутації приймаються лише з власної адреси застосунку (інша
  локальна сторінка не постить кукою оператора); токен Telegram не потрапляє
  в текст помилки; сторінки не вбудовуються в чужі фрейми.
- Скріншоти зміни: кап на розмір до декодування, орієнтація з телефона; STL-панель
  не росте в памʼяті за зміну.
- Реліз збирається лише після зелених тестів.
### Додано
- **Черга віддає рядки сторінками.** «Раніше» тримає до 30 днів робіт — 600+
  рядків важили 5,5 МБ і перезавантажувались кожні 15 с. Тепер одразу їдуть
  перші 200, під таблицею — «Показати ще»; порядок той самий, лічильники
  «N у вигляді» та одиниць — по всьому списку. На великому списку полл
  рідшає до хвилини.
- **Розділ «Ранкова видача» в Налаштуваннях.** Перемикач QC-чеклиста
  (три звірки перед «знайдено») нарешті має кнопку — раніше роут існував,
  а ввімкнути його з екрана було нічим.
- **«Перевірити оновлення» розрізняє «немає новішої» і «немає звʼязку».**
  Раніше недоступний GitHub теж звітував «у вас найновіша версія».
### Змінено
- **Налаштування відкриваються вдвічі легше.** Журнал змін показує перші
  8 релізів, решта — за кліком «Показати всі»; раніше всі 114 релізів
  (225 КБ) їхали в кожне відкриття.
- **Фільтри пошти редагує й оператор** — як і решту «Джерел робіт» (рішення
  власника 06.09). Раніше кнопки показувались, а кожна відповідала «лише для
  адміністратора».
- **Ліцензія лише для тих, хто увійшов.** Паспорт ліцензії (термін, machine ID)
  і поле нового ключа більше не відкриті без входу; активація на новому ПК
  працює як і раніше.
- **«Вхідні» → «Звернення».** Поруч із «Нові з пошти» два «вхідні» читались
  як дві пошти.
- **«Оновити зараз» на екрані печей** чекає недоступну піч 6 с, а не 20.
- Місячні копії бази тепер звуться `kuubmill-РРРР-ММ.db`; старі `orderdesk-`
  читаються далі.
### Виправлено
- **Прийняття листа більше не заморожує застосунок.** Переміщення файлів і
  рядок-нотатка в таблицю йшли на головному потоці — кілька разів на годину
  всі екрани стояли. Те саме для «Відхилити», «Відкотити прийняття»,
  завантаження портрета верстата і «Зберегти й синхронізувати».
- **Рядок-нотатка в таблиці пишеться ПІСЛЯ збереження в базі.** Невдале
  збереження більше не лишає в таблиці рядок-сироту, який наступний синк
  імпортував би як другу роботу.
- **Чистка спулу пошти не зачепить живий лист**, якщо після зміни скриньки
  два листи отримали однаковий UID.
- **Пауза синку зупиняє й «⟳» дня у Виробітку, і «Зберегти й
  синхронізувати»** — раніше ці дві кнопки читали таблицю попри паузу.
- Згорнута рейка знову має підписи для читалки екрана; QC-діалог видачі не
  випускає фокус клавіатури; червоний стан у налаштуваннях — з токена теми.

## [0.11.2] — 2026-09-06
### Виправлено
- **Оновлення знову знаходяться.** Дві версії готувались паралельно, і збірка,
  яка вийшла останньою, отримала МЕНШИЙ номер (0.10.5 після 0.11.1). Програма
  чесно відповідала «новішого немає», хоч насправді свіжіший застосунок був.
  Ця версія містить усе з обох ліній — і згорнуті вкладки меню, і віджет
  3D-друку над чергою — під номером, більшим за обидва.
## [0.10.5] — 2026-09-06
### Додано
- **Віджет 3D-друку над чергою.** Один рядок із головним: **о котрій SISMA
  закінчить друк**. Клік розгортає табло — шар N з M, початок, кінець,
  скільки лишилось. Принтер прибрано зі стрічки фрезерних верстатів: у них
  відсоток програми, у нього шари й час, і поруч вони читались як однакові
  речі. Обрив звʼязку принтер із віджета не ховає.
- **«Готово» тепер бачать усі верстати.** Раніше галочка завершення залежала
  від екрана, який є лише в одного верстата; решта доходили до 100% і
  показували число. Тепер сотня, що простояла понад дві хвилини, теж означає
  «зняти роботу».
### Змінено
- **Збір калібрувальних кадрів шукає РІЗНІ екрани, а не свіжі.** Кадр
  відкладається, лише якщо він не схожий на вже зібрані, а коли тека повна —
  витісняється найзайвіший. Доба простою більше не витісняє двохвилинну
  аварію: саме рідкісні екрани й потрібні, щоб навчити програму розрізняти
  «помилка» і «закінчив». Лічильник кадрів росте повільніше — і це правильно.
### Додано
- **День у Виробітку тепер закривається сам.** Раніше число зберігалось лише
  тоді, коли хтось відкривав розділ: місяць, у який не заглядали, після чистки
  старих вкладок міг стати нулем. Тепер CRM закриває день за розкладом —
  лабораторію й пошту о **07:30** (кінець робочої доби, нічна зміна вже
  дописала), СЛМ о **18:00** того ж дня (його заповнюють і правлять ще пів дня
  після кінця доби). Після закриття ні чистка таблиці, ні повний імпорт історії
  число не зачіпають. Якщо ПК був вимкнений — застосунок закриє пропущені дні
  при першому запуску.
- **Синхронізація одного дня прямо з табеля.** Наведи на рядок дня — зʼявиться
  ⟳. Він перечитує вкладку саме цього дня (кілька секунд, не десятки хвилин, як
  повний імпорт історії) і рахує день начисто, знімаючи закриття. Це для
  випадку «таблиця правильна, а число в CRM — ні». Дія двостороння: якщо рядок
  у таблиці видалили, робота піде в архів, як при звичайній ручній
  синхронізації того дня.
### Змінено
- **Лабораторна робота йде у виробіток лише коли в неї є Sum3D ID.** Технік
  заводить рядок наперед: якщо сьогодні роботу не встигли, завтра її заводять
  знову — і та сама коронка рахувалась двічі. Тепер рядок лабораторії
  (цирконій, ПММА, титан, віск) враховується того дня, коли оператор реально
  взяв роботу в прорахунок. Клієнтських робіт із пошти це не стосується — вони
  приходять листом один раз.
### Прибрано
- **Кнопку «Добрати СЛМ» у Виробітку прибрано** на прохання власника: для
  добору минулих днів він користується повною синхронізацією таблиці
  («Імпортувати всю історію» в Налаштуваннях → Google Таблиця), окрема вузька
  дія не потрібна. Самі числа СЛМ як писались синхронізацією, так і пишуться.


## [0.11.1] — 2026-09-06
### Змінено
- **Вкладки розділу в меню налаштувань тепер згорнуті.** «Копії таблиці»,
  «Скачування вкладень» і «Сповіщення» більше не висять у лівому меню
  постійно: вони показуються, коли відкриваєш їхній розділ, або якщо
  натиснути стрілочку поруч із ним.
### Виправлено
- **Підпункти меню не натискались.** Стрілочка розкриття малювалась порожнім
  прямокутником і не реагувала, а «Сповіщення» з самого кабінету не
  перемикали вкладку.
- **Підпис шару SISMA був тьмяним.** «Шар 250 з 1049» у картці верстата
  фарбувався неіснуючим кольором і зливався з рештою рядка.

## [0.11.0] — 2026-09-06
### Додано
- **Кнопка «Добрати СЛМ» у Виробітку.** Числа СЛМ пише синхронізація тим самим
  проходом, що читає вкладку дня, а вона щодня дивиться лише сьогодні й сусідні
  дні. Тому дні, чиї вкладки вже вийшли з цього вікна, лишались із порожніми
  колонками СЛМ. Кнопка перечитує вкладки **показаного місяця** і дописує лише
  СЛМ — черга, архів і видача не зачіпаються, а виправлені вручну клітинки
  лишаються як є. Легша заміна «Імпортувати всю історію», яка перечитує весь
  документ.
### Змінено
- **Три розділи налаштувань стали вкладками своїх господарів.** «Копії таблиці»
  тепер усередині «Google Таблиця», «Скачування вкладень» — усередині
  «Пошта (IMAP)», «Сповіщення» — у кабінеті четвертою вкладкою. Нічого не
  зникло: пункти лишились у меню й у Ctrl+K, а посилання відкриває господаря
  одразу на потрібній вкладці.
- **Реєстр меню налаштувань.** Розділи, права на них і ключі описані в одному
  місці — менше шансів, що пункт зʼявиться комусь, кому його не можна.
### Виправлено
- **Верстка розділів налаштувань.** «Зворотний зв'язок» відкривався взагалі без
  оформлення; у «Резервній копії» половина ряду стояла порожньою, а список
  автоматичних копій висів поза картками; у «Google Таблиця» кнопки збереження
  були віднесені до правого краю; «Про застосунок» друкував усі 111 релізів на
  сім екранів — тепер журнал має власний скрол.

## [0.10.4] — 2026-09-06
### Додано
- **SLM-принтер SISMA у моніторингу.** CRM читає його екран і показує в
  картці: **шар 250 з 1049**, **о котрій закінчить друк** і що машина робить
  зараз — пише шар чи розрівнює порошок між шарами. Числа беруться з екрана
  як є, час завершення — прогноз самої машини, а не наша оцінка з відсотка.
  Друк не вважається зупиненим під час нанесення порошку, хоч лазер у цю мить
  і не світить.
- **Макет «Стенд» у Налаштуваннях.** Кожен розділ відкривається плитою стану.
  Колір плити береться лише з підтвердженого сигналу — живого синку, кадру з
  пристрою, активного адміністратора; заповнене поле зеленим не стає.
### Виправлено
- **Кнопки «Перевірити» в Налаштуваннях знову працюють.** Через зниклий
  шаблон вони девʼять днів віддавали помилку без жодного видимого
  повідомлення.
## [0.10.3] — 2026-09-06
### Змінено
- **Збір калібрувальних кадрів переїхав у Налаштування → Верстати.** Це
  обслуговування, а не робочий екран: раніше смуга займала верх екрана
  «Верстати», який оператор дивиться цілий день. Тепер вона стоїть поруч із
  галкою «Калібр.» і текою кадрів, і бачить її лише адміністратор.
- **Картку «Видача» з налаштувань прибрано** на прохання власника — чеклист
  трьох звірок перед «знайдено» наразі не використовується. Сама видача
  працює як раніше, одним кліком.
### Виправлено
- **«Зібрано кадрів» більше не виглядає замерзлим.** Коли тека набирає межу,
  нові кадри витісняють найстаріші — і число за побудовою перестає рости.
  Тепер поруч видно час останнього кадру (він тикає щоразу, як приходить
  новий) і пряме пояснення, що межа набрана. Блок оновлює себе сам.
## [0.10.2] — 2026-09-06
### Додано
- **Тека калібрувальних кадрів — у Налаштуваннях → Верстати.** Видно, куди
  саме складаються кадри й скільки їх уже зібрано; кнопка «Відкрити теку»
  відкриває її у Провіднику, а поле поруч дозволяє відвести під кадри іншу
  теку (порожнє поле повертає типову). Раніше теку треба було шукати руками
  в системних каталогах.
## [0.10.1] — 2026-09-06
### Виправлено
- **Збір калібрувальних кадрів верстата більше не глухне.** Якщо верстат довго
  СТОЯВ з увімкненою галкою «збирати кадри», ліміт забивався знімками
  нерухомого екрана — і коли робота нарешті стартувала, кадрів не з'являлось
  зовсім. Тепер найстаріші кадри витісняються новими: у теці завжди останні
  ~30 хвилин життя екрана. Знімки по відсотках (з фрезерних верстатів)
  не витісняються — вони незамінні.

## [0.10.0] — 2026-09-06
### Додано
- **Кнопка «Усі знайдено»** на картці клієнта у видачі. Зібрав лоток на 30
  робіт — раніше ставив 30 галочок поспіль, тепер один клік. «Видати N з M»
  лишилась окремою кнопкою навмисно: «я тримаю коронку» і «я віддав її
  логісту» — різні рішення, а роботи одного клієнта виходять із пічок партіями.
- **Чеклист перед «знайдено»** — три звірки (форма з STL, матеріал і колір,
  кількість). **Вимкнений**; вмикається в Налаштуваннях → «Видача». Поки він
  увімкнений, «Усі знайдено» ховається — вона обійшла б звірку.
- **Екран «Журнал синку»** і кружок стану в меню на кожному екрані. Видно, що
  таблиця чи пошта мовчать, не заходячи в Чергу. Сірий кружок означає «стану ще
  немає» — зелений за замовчуванням брехав би.
- **Історія печей і верстатів за добу** прямо в картці: у печей крива
  температури, у верстатів стрічка «фрезерував / стояв». Пропуски в даних
  заштриховані з підписом, а не замальовані лінією — чесна дірка краща за
  намальовану температуру.
- **Ctrl+K — пошук і переходи звідусіль.** Одне поле: номер наряду, Sum3D ID або
  назва екрана. Пункт «Пошук» у меню відкриває те саме мишею. У пошті додались
  J/K — рух по листах з клавіатури.
- **Збережені вигляди черги.** Набір фільтрів зберігається під назвою й
  повертається одним кліком. Вигляди приватні: кожен бачить лише свої.

### Виправлено
- **Лист більше не губить вкладення.** Якщо база спотикалась посеред прийняття,
  файли вже лежали в `export`, а роботи не було — і ніхто про це не знав. Тепер
  прийняття відкочується саме: файли повертаються, оператор бачить помилку.
- **Портал не завмирає, чекаючи таблицю.** «Видати 8 з 8» могло заморозити
  застосунок на дві хвилини. Тепер уся видача клієнта — одне фонове завдання.
  Те саме зі статусами, Sum3D і «Скасувати».
- **Не пишемо в чужий рядок таблиці.** Якщо технік видалив рядок вище, наш
  номер рядка починав показувати на сусідню живу роботу. Тепер позиція
  звіряється перед КОЖНИМ записом, і якщо не збіглась — запис пропускається.
- **Синк не ховає робочі дні.** Неповний список вкладок від Google більше не
  читається як «день зник» і не відправляє його в архів.
- **Робочий день замість календарного.** О другій ночі файли лягали в теку
  одного дня, а рядок у таблиці — в інший. Тепер скрізь одна доба з 07:30.
- **Вхід і коди.** Пауза після 5 невдалих спроб, пароль від 10 символів,
  технічна сторінка `/docs` закрита.

## [0.9.0] — 2026-09-06
### Змінено
- **Кнопки більше не мовчать.** Будь-яка дія, що чекає відповідь, тьмяніє й не
  приймає повторного кліку, а кнопки ще й показують кільце. Раніше «працюю»
  показували 8 кнопок із дев'яноста — оператор тиснув удруге, бо не бачив, що
  вже щось відбувається.
- **STL відкривається на весь екран одразу** і памʼятає ваш вибір. Це головний
  інструмент звірки на видачі, а він щоразу відкривався маленьким. У тріажі
  зʼявилась така сама кнопка, але там вигляд лишився вбудованим — модель там
  дивляться поруч із формою.
- **Видача не збиває налаштоване.** Згорнуті картки клієнтів лишаються
  згорнутими після кожної галочки, зʼявився пошук клієнта в списку (порядок
  списку не змінюється), а «Прив'язати папку» повертає в той самий день.
- **Статус роботи — прямо з рядка черги** (меню «⋯» біля наряду): «проблема» чи
  «переробка» більше не вимагають відкривати картку роботи.
- **Архівні роботи в пошуку — лише перегляд**, із міткою «архів». Раніше їх
  можна було правити з пошуку так само, як живі.
- **Журнал дій зʼявився в меню**, а стан синхронізації з таблицею тепер видно на
  кожному екрані, не лише в Черзі.
- **Архів памʼятає, де ви були**: перезавантаження сторінки, «назад» у браузері
  й надіслане колезі посилання відкривають той самий місяць і день.
- **«Встановити оновлення» перепитує** і каже, скільки операторів зараз працює —
  застосунок перезапускається, і на спільному ПК це обриває роботу колеги.

### Виправлено
- **Помилка при прийнятті листа більше не стирає введене.** Раніше будь-яка
  затримка чи відмова перекидала на початок: усе, що заповнили в трьох кроках,
  зникало, а лист доводилось шукати заново. Тепер помилка показується просто у
  візарді, з усіма вашими значеннями на місці.
- **Фільтри пошти й контакти клієнта більше не перезавантажують сторінку.**
  «Навчити фільтр» лишає вас у листі, з якого ви це зробили.
- **Бейдж «готово» в тріажі більше не бреше**: лист із нескачаними файлами за
  посиланням прийняти не можна, і список тепер це показує.
- **Портал більше не завмирає, поки чекає Google-таблицю.** Збереження Sum3D,
  оператора, статусу, «Скасувати» і «Видати N з M» ішли в таблицю прямо в
  головному потоці: поки Google відповідав (а холодне з'єднання через проксі
  лабораторії — до 40 секунд), програма не відповідала НІКОМУ — ні другому
  оператору, ні автооновленню черги. Тепер запис іде окремим потоком, екран
  лишається живим, а результат («записано» / «не вдалося») видно як і раніше.
  Видача клієнта тепер один пакет замість запису по одній роботі.
- **Запис у таблицю більше не може влучити в чужий рядок.** Якщо технік
  видалив рядок у таблиці, все нижче зсувається на одиницю — і збережена
  позиція починає показувати на сусідню живу роботу. Тепер перед кожним
  записом (заливка «видано», стирання видаленої роботи, коментар) програма
  звіряє, що в рядку досі та сама робота; не збіглось — не пише й каже про це.
  Раніше стирання могло знищити чужу роботу без сліду.
- **Неповний список вкладок більше не архівує робочі дні.** Якщо Google
  повернув обрізаний чи застарілий перелік вкладок, програма вважала, що дні
  видалили, і прибирала їх роботи в Архів — звідки вони самі не поверталися.
  Тепер спрацьовує той самий запобіжник, що й для рядків, а кнопка «Звірити
  видалення» знімає його ЛИШЕ для тієї вкладки, яку підтвердив адміністратор
  (раніше — для всіх вкладок прогону одразу).
- **Прийняття листа більше не може «загубити» вкладення.** Раніше файли спершу
  переїжджали в теку export, і лише потім запис ішов у базу. Якщо запис падав
  (база зайнята, диск повний), база «забувала» прийняття, а файли лишались у
  export — тріаж показував «файли зникли», хоча вони були на місці. Тепер при
  такій помилці файли повертаються в лист, а оператор бачить пояснення. Те саме
  для відкату прийняття.
- **Лист із вкладенням `con.stl` (чи `NUL`, `COM1`…) більше не залипає.** Windows
  забороняє такі імена, тому лист вічно лишався «в очікуванні» й перекачувався
  на кожному синку. Тепер такий файл зберігається під безпечним іменем; те саме
  правило діє для назв тек клієнтів і матеріалів.
- **Обірваний перенос файлу не лишає «половинку» в export.** Раніше при обриві
  мережі в теці міг залишитись обрізаний файл, який на видачі виглядав як
  справжня робота.
- **Архів у листі більше не може забити диск.** Розмір перевіряється за реально
  прочитаними байтами, а не за тим, що архів сам про себе пише.

### Безпека
- Вхід через Google захищено за стандартом (`state` + PKCE): чужий запит на
  тимчасовий локальний порт більше не зриває вхід і не може підсунути свій код.
- Технічний опис усіх адрес застосунку (`/docs`, `/openapi.json`) закрито.
- Код «Виробітку» більше не підібрати перебором: 5 невдалих спроб — пауза;
  те саме для входу в застосунок.
- Єдина вимога до пароля скрізь — щонайменше 10 символів (раніше адмін міг
  створити оператора з паролем «1»); роль обмежено списком «адмін / оператор».
- Перевірка шляху з правом запису (створення тимчасового файлу в будь-якій
  вказаній теці) доступна лише адміністратору.

## [0.8.24] — 2026-09-05
### Змінено
- **Архів переробили: один екран замість трьох сторінок.** Місяці тепер завжди
  видно ліворуч; вибір місяця чи дня більше не перемикає сторінку — все
  оновлюється на місці. Роботи дня показуються таблицею, схожою на Google
  Таблицю, у тому самому порядку, що й у ній. Додано пошук по всьому архіву —
  за клієнтом, номером наряду, ID Sum3D чи матеріалом.
### Додано
- **Копії Google-таблиці.** У Налаштуваннях зʼявився розділ, що зберігає сирі
  знімки вкладок таблиці — на випадок, якщо щось трапиться з оригіналом.

## [0.8.23] — 2026-09-05
### Додано
- **Розділ можна тимчасово закрити, поки він у розробці чи на тестуванні.**
  У Налаштуваннях зʼявився пункт «Блокування розділів» (лише для адміністратора).
  Закритий розділ операторам показує гарний екран-заставку «в роботі», а
  адміністратор бачить сам розділ із банером і кнопкою «Відкрити для всіх».
  Можна обрати, кому саме закрити — усім чи окремим ролям. Першим так закрито
  розділ «Статистика», поки звіряємо його цифри.
### Додано
- **Стрічку навантаження можна налаштувати в шестерні вигляду.** У шестерні на
  черзі з'явився рядок «Навантаження системи» з трьома перемикачами — CRM, ПК,
  ОЗП. Можна лишити тільки потрібне, будь-яку комбінацію, або вимкнути стрічку
  зовсім. Вибір зберігається у вашому обліковому записі.

## [0.8.21] — 2026-09-05
### Додано
- **Навантаження системи в шапці черги.** Тонка стрічка під кнопками показує,
  наскільки завантажений комп'ютер і скільки з цього займає сам застосунок:
  CRM, потім ПК, потім оперативна пам'ять. Коли комп'ютер близький до межі,
  стрічка оживає й червоніє — видно одразу, не вдивляючись.

### Змінено
- **Праву панель зведено в єдину родину віджетів** — пічки й верстати
  виглядають однаково узгоджено.

### Виправлено
- **Порожня черга більше не «їздить» фоном вліво-вправо** — зайвий рух прибрано.
- **HTMX-оновлення без активної сесії більше не підставляє голу сторінку входу**
  всередину екрана — коректно веде на вхід.
- **Розгорнутий блок на картці верстата більше не згортається сам.**

## [0.8.20] — 2026-09-04
### Змінено
- **Верстат показує ВСІ роботи з одним Sum3D ID.** Коли один проєкт фрезерує
  кілька робіт з однієї заготовки, раніше не показувалась жодна — програма не
  бралася вгадувати. Тепер видно всі: на екрані «Верстати» кожна окремим
  значком, у бічній панелі — перша й «+N».
- **У смузі верстатів «готово» замінено на галочку.** Слово налазило на назву
  верстата, бо комірка розрахована під двоцифрове число. Пояснення лишилось у
  підказці й у бічній панелі.

## [0.8.19] — 2026-09-04
### Виправлено
- **Список минулих Sum3D ID нарешті відкривається з першого кліку.** Лоток у
  шапці підмінював сам себе раз на секунду, тож клік мусив влучити між
  оновленнями — звідси «відкривається з десятого разу».
- **Прибрано напис «Перетягніть межу заголовка стовпця» з шапки.** Він залипав
  після виходу з режиму: кнопка в шестерні втрачала обробник, коли панель
  перемальовувалась, і режим не вимикався. Саме перетягування меж лишилось на
  місці, у шестерні «вигляд».

## [0.8.18] — 2026-09-04
### Додано
- **Картка верстата показує історію обривів звʼязку** — скільки разів рвалось,
  найдовший обрив, загалом, і чи триває зараз. На питання «як часто рветься»
  тепер відповідають цифри, а не відчуття. Показується лише там, де обриви
  справді були.
- **Зрозуміло, чому робота не знайшлась за Sum3D ID.** Раніше на всі випадки
  був один напис «немає в черзі». Тепер видно, що саме: такого ID немає в
  жодній роботі, він стоїть одразу в кількох (тоді програма свідомо не вгадує),
  або робота вже в архіві.
- **Видно, чому верстат не називає роботу.** Назва береться із заголовка вікна;
  якщо там її немає — картка каже про це прямо, замість мовчазної порожнечі.

### Виправлено
- **Верстат, який фрезерує переробку, тепер знаходить свою роботу.** Для
  переробки Sum3D ID зберігається в окремому полі, і пошук його не бачив.
- **Агент на верстаті більше не помирає непомітно.** Виявилось, що зникнення
  верстата з CRM — це не обрив мережі, а смерть програми-агента: у треї
  лишався значок-привид. Тепер агент захищений від такого падіння, причина
  збою записується у власний журнал, а якщо він усе ж зупиниться — його
  автоматично підніме сторож протягом пʼяти хвилин, не чекаючи входу в систему.

## [0.8.17] — 2026-09-04
### Виправлено
- **Верстат більше не «блимає» червоним.** Плитка писала «немає зв'язку» після
  одного невдалого опитування — досить було загубленого пакета в мережі цеху,
  щоб верстат на кілька секунд виглядав мертвим. Тепер потрібні три невдачі
  поспіль: справжній обрив видно так само швидко, а миготіння немає.
- **Прибрано хибні «100%».** Синя пляма на екрані верстата (наприклад фрагмент
  шпалер Windows) могла читатись як завершена робота. Це найдорожча помилка —
  після неї йдуть знімати недофрезероване. Тепер таке число не показується.
- **Мовчазний верстат більше не гальмує решту.** Вимкнений ПК затримував обхід
  усіх інших, бо частина запитів ішла по черзі.
- **Агент на верстаті став живучішим:** переживає зіпсований файл налаштувань
  (раніше після цього не запускався взагалі), не падає цілком через помилку в
  одному запиті, обмежує одночасні знімки, щоб не навантажувати ПК під час
  фрезерування, і більше не роздуває свій лог-файл без міри.

### Додано
- **Обриви зв'язку тепер пишуться в журнал** — з часом і тривалістю кожного.
  Це дає відповідь на питання «як часто рветься» цифрами, а не на відчуття.

## [0.8.16] — 2026-09-04
### Додано
- **Верстати нового покоління пишуть «готово», коли дофрезерували.** Раніше в
  них стояла риска — так само, як на зупиненому верстаті, — бо після
  завершення на екрані стоїть підсумок без смуги прогресу. Тепер видно, що
  роботу можна знімати.

### Виправлено
- **Прибрана в таблиці переробка тепер зникає і в CRM.** Технік прибирав брак
  з рядка, а в черзі далі висіла стара причина; на одному наряді через це
  показувалось дві переробки замість однієї. Прилипла переробка ще й відправляла
  Sum3D ID у переробкову колонку таблиці замість звичайної.

## [0.8.15] — 2026-09-04
### Додано
- **Режим редагування віджетів.** У шестерні вигляду на черзі з'явилась кнопка
  «Перетягувати віджети»: в цьому режимі смугу верстатів угорі й секції правої
  панелі можна перетягнути мишею й поставити в потрібному порядку. Порядок
  зберігається у вашому обліковому записі й не збивається під час оновлення
  екрана. Те саме працює з клавіатури: Ctrl зі стрілками.

## [0.8.14] — 2026-09-04
### Змінено
- **Замість незрозумілого «%?» віджет пише «запуск».** Так позначається верстат,
  який щойно почав програму: він точно фрезерує, але заливка смуги ще надто
  вузька, щоб прочитати з неї відсоток. Попередній значок читався як збій.

## [0.8.13] — 2026-09-04
### Додано
- **Вибір портрета верстата.** У Налаштування → Верстати під мініатюрою
  з'явився селектор: 350i, 350i loader, 250i або 250i dry. Обране одразу
  видно на картці екрана «Верстати». «Авто» лишає здогад за назвою, як було.

## [0.8.12] — 2026-09-04
### Додано
- **Фото на кожен верстат.** У Налаштування → Верстати з'явилась колонка
  «Фото»: натисни мініатюру, вибери знімок з телефона — і картка на екрані
  «Верстати» показує саме цей верстат. Поворот з телефона враховується,
  розмір стискається сам. Хрестик повертає портрет моделі.

### Змінено
- **Портрети моделей тепер справжні.** Чотири моделі цеху — 350i, 350i loader,
  250i і 250i dry — намальовані з реальних фото цеху: чорний фронт із синім
  вікном, а не вигадана біла шафа. Модель береться з назви верстата.
- **Оновлення показує справжній хід.** Замість підписів по таймеру — скільки
  мегабайт скачано з скількох і жива смужка. Якщо скачування обірвалось,
  оверлей каже чому і дає кнопку «Закрити», а не висить.

### Виправлено
- **Відсоток з вузької смуги без підпису більше не показується** — така
  смуга не була прогресом, і число вводило в оману.
- **Синій текст на екрані верстата більше не читається як число** — смуга
  прогресу мусить бути суцільною.

## [0.8.11] — 2026-09-04
### Змінено
- **Екран «Верстати» — картки з портретами у три стовпці.** Кожен верстат
  тепер видно як модель (350i чи 250i), а не як безликий рядок: у шапці картки
  портрет, поверх нього назва, адреса й великий відсоток виконання. Кадр
  екрана нікуди не подівся — він розгортається під карткою, і оновлення не
  заважає, поки кадр відкритий. Хто звик бачити сам екран у плитці — у кабінеті
  є «Живий кадр».
- **Налаштування → Верстати й Пічки — таблиця стала охайною.** Поля більше не
  лягають стовпчиком, рядок «Додати» — в одну лінію з кнопкою. Раніше стилі
  цієї таблиці на екран налаштувань просто не долітали, тому додати верстат
  було незручно.

### Виправлено
- **Верстат без зв'язку пояснює по-людськи.** Замість шести рядків технічного
  тексту в плитці — «агент не відповідає — ПК вимкнено або порт закрито
  брандмауером».

## [0.8.10] — 2026-09-04
### Додано
- **Картки верстатів у налаштуваннях.** Верстат тепер має власний портрет, а не
  безликий рядок — його видно одразу, без вчитування в адресу. Вигляд картки
  вибирається в кабінеті: «Портрет верстата» (за замовчуванням) або «Живий
  кадр», якщо зручніше бачити сам екран.

### Виправлено
- **Верстат CORiTEC 150i PRO тепер показує відсоток.** Він трохи іншого вигляду,
  ніж решта нового покоління, і віджет для нього мовчав. Тепер читається так
  само, як усі інші.
- **Кілька верстатів нового покоління більше не пропускають окремі оновлення.**
  Раз на кілька хвилин відсоток міг зникнути на один тік і зʼявитись знову —
  тепер ряд суцільний.

## [0.8.9] — 2026-09-04
### Виправлено
- **Щойно запущена програма тепер видно одразу.** На дуже низькому відсотку
  (1-3%) залита частина смуги така вузька, що програма її не помічала і не
  показувала нічого. Тепер відсоток читається з самого початку роботи.

## [0.8.8] — 2026-09-04
### Виправлено
- **Вертикальний верстат (портретний екран) тепер показує відсоток.** Раніше
  для нього нічого не читалось. Разом із попередніми правками відсоток
  читається на ВСІХ чотирьох типах екранів у цеху.

## [0.8.7] — 2026-09-04
### Додано
- **Верстати нового покоління (CORiTEC, плаский екран) показують відсоток.**
  Раніше для них віджет нічого не міг прочитати. Тепер відсоток береться зі
  смуги «PROGRESS» — окремо від старих RemiCORE, робота яких не змінилась.

## [0.8.6] — 2026-09-04
### Виправлено
- **Верстат більше не завищує низький відсоток.** Показувало 57%, коли реально
  було 18%: на малому прогресі RemiCORE пише число синім по порожній частині
  смуги, і програма рахувала ці літери як залиту частину. Тепер число читається
  правильно на всьому діапазоні.

## [0.8.5] — 2026-09-04
### Додано
- **Ручний збір калібрувальних кадрів для будь-якого верстата.** У Налаштуваннях
  → Верстати з'явився перемикач «Калібр.». Увімкни його для верстата, де
  відсоток ще не читається (нове покоління, інша розкладка екрана), дай
  пофрезерувати — кадри зберуться самі, тоді скачай їх кнопкою «Скачати кадри» і
  надішли розробнику. Вимкни, коли зібрав.
### Виправлено
- Читання підпису смуги RemiCORE тепер терпить різну висоту знака «%» — раніше
  через це деякі кадри не читались.

## [0.8.4] — 2026-09-04
### Виправлено
- **Віджет верстатів більше не пише «програма не йде», коли вона йде.** Якщо на
  верстаті завантажена програма (видно з заголовка вікна), а відсоток на
  поточному екрані RemiCORE не показано, віджет тепер каже «йде · відсоток не
  видно», а не помилкове «не йде». Саме число з таких екранів — окреме
  завдання.

## [0.8.3] — 2026-09-04
### Виправлено
- **Верстати старого покоління читають відсоток з підпису смуги.** Тепер
  система бере число, яке верстат сам пише на смузі («72%»), і довіряє йому
  більше за оцінку по довжині заливки — та занижувала на ~2%. Навчено на
  реальних кадрах двох роздільностей екранів. (Верстати нового покоління — з
  іншим інтерфейсом — потребують окремої підтримки, буде далі.)

## [0.8.2] — 2026-09-04
### Швидше
- **Іконки папок і лоток Sum3D більше не змушують чекати мережу.** Програма
  тепер тримає перелік папок «теплим» у фоні, тож коли ви відкриваєте чергу,
  все вже готове — а не сканується заново по 4-5 секунд. Кілька вкладок,
  відкритих поспіль, більше не запускають кілька однакових сканів водночас.

## [0.8.1] — 2026-09-04
### Швидше
- **Черга робіт відкривається одразу.** Раніше вона чекала на мережеву папку
  (Synology) по кілька секунд, перш ніж показатись — тепер сторінка малюється
  миттєво з рядків, а іконки папок і лоток Sum3D підтягуються за мить після.
  Дані ті самі, зникло лише очікування.
### Додано
- **Верстати самі збирають кадри для калібрування.** Поки програма ще не вміє
  читати відсоток з підпису смуги, вона під час фрезерування сама відкладає
  кадри з різними відсотками. На екрані «Верстати» адмін бачить, скільки
  зібрано й скільки лишилось, і може скачати їх одним архівом, щоб надіслати
  розробнику. Коли читання навчено — збір зупиняється сам.

## [0.8.0] — 2026-09-03
### Додано
- **Екран «Швидкодія» (`/diag/perf`, лише адмін).** Показує, скільки триває
  кожна взаємодія — окремо час застосунку і окремо те, що чекає оператор уже
  після відповіді (перемальовка сторінки). З розкладкою, де саме йде час, і
  кнопкою «Скопіювати як текст». Потрібен, щоб знайти, звідки береться відчуття
  «повільно», а не гадати.
- **Спостерігач за верстатами читає підпис зі смуги прогресу.** Раніше відсоток
  оцінювався лише за довжиною заливки; тепер система читає саме число, яке
  верстат пише на смузі («43%»), і довіряє йому більше. Доки цифри не навчено
  на реальних кадрах, працює як раніше.
### Виправлено
- **Оновлення застосунку більше не може лишити базу «наполовину оновленою».**
  Тепер на старті структура бази завжди доводиться до поточної версії (з копією
  перед тим), незалежно від способу запуску. Раніше певні оновлення показували
  помилку на всіх екранах, доки базу не полагодять вручну.
- **Нічна зміна.** Робота, яку черга показує в роботі, більше не відкривається
  «тільки для читання» вночі: паспорт роботи, архів і додавання нової роботи
  тепер рахують добу від початку робочого дня, як і черга.
- **Запис у Google-таблицю надійніший при збоях мережі.** Перевірка, у який
  саме рядок писати, тепер переживає обрив зв'язку так само, як і сам запис —
  менше шансів записати не в той рядок.
- **3D-прев'ю в пошті більше не «чорніє» після багатьох листів.** Кожен
  переглянутий лист лишав по собі відкрите вікно перегляду; тепер вони
  прибираються, і прев'ю працює цілу зміну.
- Підсвітка плиток печей і верстатів більше не гасне після першого оновлення
  даних на них.

## [0.7.19] — 2026-09-03
### Виправлено
- **Рядок про запити до Google більше не вводить в оману.** Він рахує читання
  й записи разом, а порівнював себе з лімітом самих лише читань. Тепер так і
  написано; для нашого профілю (читань на два порядки більше) сума лишається
  правильним орієнтиром.
- Дрібне після ревʼю: екран видачі більше не читає одне й те саме
  налаштування двічі за рендер, а перелік тек техніків повертає однаковий
  результат незалежно від того, чи шара відповіла з першого разу.

## [0.7.18] — 2026-09-03
### Змінено
- **Черга й видача перестали смикати мережеву шару на кожен рядок.** Це і була
  причина «вкладка відкривається 5 секунд». Щоб намалювати один рядок, програма
  зверталась до шари **18 разів** — перевіряла, чи є тека техніка, чи на місці
  файли, чи не підмінили корінь. Кожна така перевірка на мережевому диску це
  окрема ходка туди-назад, а рядків на екрані сотні; те саме повторювалось у
  фоновому оновленні **кожні 15 секунд**, тож програма забивала шару сама собі.

  Тепер теки читаються одним списком і тримаються в памʼяті 30 секунд, а корінь
  перевіряється раз на екран, а не раз на рядок. Виміряно на 200 рядках:
  **600 звернень до диска → 1**, на поштових роботах **18 на рядок → 4**. На
  повільній шарі це різниця між ~6 секундами й миттю.

  Захист не ослаблено: коли оператор справді відкриває теку чи прев'ю, сервер
  перевіряє шлях повністю, як і раніше.

## [0.7.17] — 2026-09-03
### Змінено
- **Черга більше не сканує теку проєктів Sum3D двічі за одне відкриття.** На
  мережевій теці другий обхід коштував реального часу.
- **Кеш обходу сховища живе довше за проміжок між прогрівами** (було 90 с при
  прогріві раз на 2 хв — кожен запис протухав ще до наступного прогріву).
- **Фоновий прогрів рахує день так само, як екран видачі.** Уночі, до 07:30,
  вони розходились на день, і третина роботи прогріву гріла день, якого на
  екрані немає.

### Додано
- **Лог тепер каже, КУДИ йде час.** Рядок видачі показує, скільки звернень до
  сховища взяв на себе кеш («кеш 12 влуч./0 протух./35 промах.»), а черга при
  довгому рендері пише окремо час запиту до бази й час обходу теки Sum3D. Без
  цього причину «вкладка відкривається 5 секунд» доводилось вгадувати.

## [0.7.16] — 2026-09-03
### Виправлено
- **Верстат знову показує відсоток.** Коли поверх екрана RemiCORE відкрите
  вікно G-коду, поточний блок рядків у ньому виділений суцільним синім — і цей
  прямокутник «перекрикував» справжню смугу прогресу: розпізнавання перебирало
  десятки його однакових шматків і до смуги просто не доходило. Плитка при
  цьому писала «програма не йде», хоча кадри надходили справно. Тепер кожна
  синя область розглядається один раз, а пошук звужено до самого низу екрана,
  де смуга й живе.

### Додано
- **Видно, коли верстат СТОЇТЬ.** Відсоток, що не змінюється понад 5 хвилин, —
  це зупинена або призупинена програма, а не помилка CRM. Чіп у черзі лишає те
  саме число (воно правдиве) і гасне до нейтрального зі значком паузи, тож
  «81% уже пів години» більше не виглядає як залипання.
- **Лічильник запитів до Google.** Раз на хвилину в лог іде рядок «Sheets API:
  N запитів за останні 60 с». Потрібен, щоб розібратися з помилкою «перевищено
  квоту читань», не гадаючи.

## [0.7.15] — 2026-09-03
### Виправлено
- **Збереження налаштувань більше не стирає сусідні налаштування.** Екран
  налаштувань складається з кількох окремих карток («Google Таблиця», «Пошта»,
  «Шляхи»), і збереження ОДНІЄЇ з них мовчки витирало поля решти: зберіг шлях
  до проєктів Sum3D — і в ту саму мить зник Google Sheet ID (застосунок писав
  «таблиця не може синхронізуватися») та шлях до теки export (видача показувала
  «0 тек у сховищі»). Один клік — три втрачені налаштування без жодного
  попередження. Тепер кожна картка зберігає тільки свої поля. Очистити поле
  руками, як і раніше, можна — просто стерти його вміст і зберегти.

## [0.7.14] — 2026-09-03
### Змінено
- **Черга й видача відкриваються помітно швидше.** Кожне відкриття піднімало в
  памʼять УСЮ історію робіт (зокрема архів за всі місяці), щоб одразу її ж і
  відкинути. Тепер архів відсіюється одразу в базі: на реалістичному обсязі
  (5,5 тис. активних + 16,5 тис. архівних) запит черги став вчетверо легшим —
  476 → 123 мс. Далі різниця тільки зростатиме, бо архів більшає щодня.

## [0.7.13] — 2026-09-03
### Змінено
- **Готово до десяти верстатів.** Частіше опитування не має коштувати ресурсу:
  кадр тепер лягає на диск раз на 15 секунд (аналізується — кожні 5), тож
  замість ~35 ГБ запису на добу виходить ~12; усі верстати опитуються
  одночасно, а не вісім за раз; мовчазний ПК більше не тримає чергу 20 секунд
  (для агента — не більше 8).

## [0.7.12] — 2026-09-03
### Змінено
- **Відсоток на верстаті оновлюється майже наживо.** Було відставання до
  45 секунд (≈5% на 15-хвилинній програмі): кадр знімався раз на 15 с, а
  віджет оновлювався раз на 30 с. Тепер 5 с і 10 с — відставання до ~15 с.
  Навантаження при цьому не зросло: розбір кадру пришвидшено вдвічі.

## [0.7.11] — 2026-09-03
### Виправлено
- **Відсоток читається на всьому діапазоні, а не лише від 10%.** Щойно
  запущена програма показувала «програма не йде»: смуга вузька, і заливка на
  3-9% просто відкидалась. Тепер читається від ~3%.
- **Малий відсоток більше не завищується.** 23% могли показатись як 59%:
  підпис на смузі («23 %») сходив за її рамку й обрізав вимір. Рамку від
  підпису тепер відрізняє те, що вона є й за межами заливки.
- **Список «▾ N» у лотку Sum3D нарешті розкривається.** Панель відкривалась,
  але була невидима — обрізалась контейнером.
### Змінено
- **Підказка Sum3D показується лише в ОДНОМУ рядку** — тому, що пришпилений
  останнім. У всіх пришпилених одразу вона була шумом.

## [0.7.10] — 2026-09-03
### Додано
- **Довідка про агента верстата прямо в налаштуваннях.** У «Налаштування →
  Верстати» з'явився розділ, що розкривається: посилання на завантаження
  інсталятора, три кроки встановлення, і чесна відповідь, куди дінуться кадри
  (на верстаті — нікуди, у CRM — один файл на верстат).
- **Агент верстата тепер має іконку в треї.** Біля годинника на ПК верстата:
  «Налаштування агента», «Призупинити спостереження» (кадр перестає
  віддаватись, CRM показує «немає зв'язку») і «Вийти». Людина за верстатом
  керує тим, що з її екрана віддається, без виклику адміна.

## [0.7.9] — 2026-09-03
### Додано
- **Новий Sum3D ID сам підставляється в пришпилену роботу.** Якщо робота
  позначена «мої зараз» і поле Sum3D порожнє, у ньому світиться щойно
  створений проєкт — Enter або клік записує. Почав друкувати своє — підказка
  зникає й не заважає.
- **Кнопка «▾ N» у лотку показує решту захоплених ID** — кожен зі своїм часом
  і власною кнопкою копіювання. Раніше «+N» був просто написом.
### Змінено
- **Нові проєкти Sum3D помічаються за 15 секунд, а не за хвилину** — стільки
  ж, скільки оновлюється сама черга.

## [0.7.8] — 2026-09-03
### Виправлено
- **Відсоток на верстаті тепер правильний (100% — це 100%).** CRM міряла не ту
  область екрана: чіплялася за сині цифри таблиці інструментів і за синій
  напис «100%» ліворуч від смуги, тож замість смуги виходила «смуга» через пів
  екрана — звідси 85% і 99% на завершеній програмі. Тепер смуга шукається
  надійно, за формою, і не залежить від того, де вона на екрані — це важливо
  для наступних верстатів з іншим макетом.

## [0.7.7] — 2026-09-03
### Виправлено
- **Завершена програма показує 100%, а не 99%.** Останній відсоток губився на
  згладженому краї смуги. Різниця в 1-2 пікселі — нижча за роздільність самої
  смуги, тож тепер вважається повною заливкою; справжні неповні (85, 90, 95%)
  лишаються собою.

## [0.7.6] — 2026-09-03
### Виправлено
- **Повна смуга тепер читається як 100%, а не 85%.** Коли програма
  дофрезерувалась, відсоток занижувався: CRM «перестрибувала» рамку смуги і
  міряла ще й біле тло панелі поруч. Тепер межа смуги визначається чесно —
  рамка відрізняється від підпису тим, що йде на всю висоту смуги.

## [0.7.5] — 2026-09-03
### Виправлено
- **Відсоток верстата більше не залипає після завершення.** Коли програма
  дофрезерувалась і смуга зникла з екрана, робота показувала старий відсоток
  (напр. «85%») й позначку «фрезерується» назавжди. Тепер свіжий кадр без
  смуги (чи закрите вікно програми) знімає і відсоток, і позначку — робота
  перестає світитись як активна.

## [0.7.4] — 2026-09-03
### Додано
- **Робота, яка зараз фрезерується, помітна прямо в черзі.** Біля наряду
  з'являється чіп із назвою верстата й відсотком — напр. «350i Loader · 9%».
  Зв'язка автоматична: CRM бачить ім'я програми на верстаті й знаходить рядок
  за Sum3D ID. Чіп зникає, коли кадр застарів або зв'язок із верстатом
  втрачено — щоб не показувати «фрезерується» для того, що вже зняли.

## [0.7.3] — 2026-09-03
### Виправлено
- **Черга більше не перескакує на новий день опівночі.** Нічна зміна після
  00:00 ще опрацьовує вчорашній день, а застосунок перемикався календарною
  північчю — і роботи зміни раптом ставали «вчорашніми» й «простроченими».
  Тепер робочий день починається о **07:30** (між відходом нічного о 05:00 і
  приходом ранкового о 08:00): до цього часу черга, видача, синхронізація
  вкладок і позначка «прострочено» лишаються на дні зміни. Час межі
  налаштовується — «Налаштування → Шляхи папок → Коли починається робочий
  день». Лист із пошти, прийнятий уночі, теж лишається на дні зміни, а не
  падає у «Завтра».

## [0.7.2] — 2026-09-03
### Виправлено
- **Відсоток на верстаті тепер читається.** На реальному екрані підпис («9 %»)
  намальований ПОВЕРХ смуги, а фон панелі сірий, а не білий — через це CRM або
  не бачила смугу зовсім, або рахувала неправильно. Тепер підпис не заважає:
  9% читаються як 9%.

## [0.7.1] — 2026-09-02
### Додано
- **Видно, ЯКА саме робота фрезерується на верстаті.** CRM читає ім'я
  `.iso`-програми із заголовка вікна RemiCORE, дістає з нього Sum3D ID і
  знаходить роботу в черзі. На плитці верстата й у віджеті тепер видно наряд і
  клієнта поруч із відсотком, а наряд клікається — веде в картку роботи. Якщо
  роботи з таким ID у черзі немає — так і написано, замість здогадок.
  **Потрібен оновлений агент на ПК верстата** (він віддає заголовок вікна).

## [0.7.0] — 2026-09-02
### Додано
- **Видно, скільки відсотків пройшла програма на верстаті.** CRM читає смугу
  виконання прямо з екрана RemiCORE: відсоток показується на плитці верстата
  (екран «Верстати») і в новому віджеті **«Верстати»** в бічній панелі черги —
  разом зі смугою, щоб було видно краєм ока. У шапці віджета видно, скільки
  верстатів зараз фрезерує. Якщо програма не йде або кадр застарів — пишемо
  «—», а не старе число: краще нічого, ніж хибний відсоток.

## [0.6.13] — 2026-09-02
### Виправлено
- **Верстат з HTTP-агентом тепер справді підключається.** Фонове опитування
  ходило до верстата по VNC навіть тоді, коли вказано токен агента — і картка
  показувала «not a VNC server». Тепер і фонове опитування, і разовий знімок
  однаково обирають спосіб: є токен — HTTP-агент, немає — VNC.

## [0.6.12] — 2026-09-02
### Змінено
- **Технічне:** детальніший лог підключення верстата (чи бачить CRM токен
  агента й який транспорт обирає — HTTP чи VNC) для діагностики.

## [0.6.11] — 2026-09-02
### Виправлено
- **Sum3D-лоток тепер бачить реальні проєкти.** У цеху проєкт Sum3D — це файл
  `.cam` (не тека), а Windows-копії дописують «— копия». CRM тепер ловить саме
  `.cam` (і теки — про запас), а копії того самого проєкту згортає в один
  запис, щоб лічильник «+N» не роздувався дублями.

## [0.6.10] — 2026-09-02
### Додано
- **Верстат можна підключити через HTTP-агент — і бачити смугу відсотка.** У
  «Налаштування → Верстати» біля пароля VNC з'явилось поле «Токен агента».
  Якщо на ПК верстата стоїть наш агент (kmill-agent), впиши його токен і порт
  8765 — CRM тягтиме кадр по HTTP. На відміну від VNC, так видно синю смугу
  виконання RemiCORE. Пароль VNC лишається для машин без агента (порт 5900).
  Заповнюй щось одне: токен АБО пароль.

## [0.6.9] — 2026-09-02
### Додано
- **Sum3D ID у шапці черги — менше ручного набору.** Вкажи в «Налаштування →
  Шляхи» теку проєктів Sum3D (Cam-work), і коли створюєш новий проєкт, його
  номер (хвіст часу, напр. `17-55-28`) сам з'являється у шапці черги з кнопкою
  «копія». Копіюєш — і вставляєш у потрібний рядок, не переписуючи цифри
  вручну. Показує найновіший, решту — лічильником «+N». CRM у теку нічого не
  пише, лише читає.

## [0.6.8] — 2026-09-02
### Додано
- **Форма зворотного зв'язку — прямо із застосунку.** У кутку кожного екрана
  з'явився маячок: клік — і можна швидко написати про баг, лишити ідею чи
  поставити питання, вставити скріншот (Ctrl+V, перетягнути або вибрати файл).
  Треба докладніше — кнопка «Розгорнути» відкриває повну панель із важливістю
  («заважає» / «блокує»), вибором екрана й кількома скріншотами. Екран, версія
  й ім'я оператора додаються самі.
- **Екран «Вхідні» для адміністратора.** Усі звернення операторів зібрані в
  одному місці з бейджем нових у бічній панелі; кожне можна позначити
  прочитаним, закрити або відкрити знову.
- **Сповіщення в Telegram (необов'язково).** У «Налаштування → Зворотний
  зв'язок» можна під'єднати бота: тоді кожне звернення миттєво прилітає в
  Telegram зі скріншотом. Звернення завжди зберігається в застосунку — навіть
  якщо мережа тимчасово недоступна, сповіщення дошлеться пізніше саме.

## [0.6.7] — 2026-09-01
### Виправлено
- **Застосунок більше не падає в помилку 500, якщо змінився ключ шифрування.**
  Після переїзду теки даних або нової інсталяції збережені секрети (ліцензія,
  пошта, доступ до таблиці) могли стати нечитабельними — і тоді ВЕСЬ застосунок
  показував «500 Щось пішло не так» на кожному екрані, включно з екраном
  активації. Тепер нечитабельне значення вважається незаданим: застосунок
  працює далі, а секрет можна ввести заново.
### Додано
- **Екран активації сам пояснює причину й дає ввести ключ заново.** Якщо термін
  дії ліцензії сплив, ключ видано іншому комп'ютеру, або збережену ліцензію
  більше не вдається прочитати (змінився ключ шифрування) — на вході видно
  зрозуміле повідомлення й поле для нового ліцензійного ключа.

## [0.6.6] — 2026-09-01
### Виправлено
- **СЛМ більше не потрапляє у фрезерну чергу, а звичайні роботи не зникають.**
  Раніше «не наша робота» визначалась за сірим кольором рядка — і реальні
  роботи, помилково зафарбовані сірим, тихо зникали з черги. Тепер колір не
  враховується взагалі: СЛМ визначається по суті — це рядок клієнта без
  матеріалу або без кількості (у фрезерної роботи завжди є і те, і те), або з
  явним словом «слм». Перевірено на робочій таблиці: усі фрезерні роботи дня
  на місці, жоден СЛМ не потрапив у чергу.

## [0.6.5] — 2026-09-01
### Виправлено
- **Роботи більше не зникають через сірий колір рядка.** CRM виключала з черги
  всі СІРІ рядки (вважаючи їх СЛМ). Але сірим у таблиці фарбують непослідовно —
  цілий блок реальних клієнтських робіт (напр. клямарчук, Kovtun) був сірим і
  тихо зникав із черги. Тепер СЛМ визначається за **текстом «слм»** у матеріалі
  чи виді роботи (надійна ознака), а не за кольором. Справжній СЛМ і далі поза
  чергою; реальні роботи, помилково зафарбовані сірим, повертаються. Сірий
  колір більше не означає «видано» — сіра клієнтська робота показується як
  «нове».

## [0.6.4] — 2026-09-01
### Виправлено
- **Синхронізація більше не зависає.** У 0.6.3 було додано читання таблиці
  дрібними частинами — воно давало забагато запитів до Google, впиралось у
  ліміт і синхронізація тривала нескінченно. Повернуто звичайне читання (одне
  на вкладку). Причина «зниклих» рядків виявилась іншою — не в читанні (див.
  далі окремим виправленням про сірі рядки).

## [0.6.3] — 2026-09-01
### Виправлено
- **Великі дні тепер підтягуються повністю.** На вкладці з великою кількістю
  робіт CRM іноді бачила лише перші ~100 рядків, а «хвіст» (останні клієнти)
  не з'являвся, хоча в таблиці вони були — а повторна синхронізація щоразу
  бачила те саме. Причина — мережевий проксі лабораторії віддавав стару/обрізану
  копію відповіді. Тепер CRM читає вкладку невеликими частинами й просить проксі
  не кешувати — весь день доходить цілком. Якщо десь усе одно бракує рядків,
  розмір частини можна зменшити в налаштуваннях без оновлення.

## [0.6.2] — 2026-09-01
### Виправлено
- **Банер масового видалення тепер поводиться правильно.** Зʼявляється й
  зникає **наживо** (не лише після перезавантаження сторінки); кнопка
  «Звірити видалення» прибирає навіть рядки зі старих вкладок, що вийшли з
  робочого вікна (раніше банер для них міг зависнути назавжди); а якщо велике
  зникнення було збоєм читання — банер зникає сам наступним синком.
- **«Імпортувати всю історію» не лякає помилкою, якщо синк саме зайнятий** —
  чекає й повторює спробу, а не показує «Не вдалося» одразу після «почато».

## [0.6.1] — 2026-09-01
### Виправлено
- **Роботи не плутаються, коли в таблиці зсуваються рядки.** Якщо видалити або
  вставити рядки над роботою (чи між роботами), синк упізнає кожну за змістом
  (наряд, або клієнт+матеріал, або технік+матеріал+вид) і просто пересуває її
  на нову позицію — дані й історія не переплутуються. Раніше роботи без наряду
  (які технік вписав до присвоєння наряду) при зсуві могли перезаписатись
  сусідньою; а два однакові рядки при вставці між ними — помінятись місцями.

### Додано
- **Попередження про масове видалення прямо на черзі.** Коли з таблиці зникає
  одразу багато рядків, синк не прибирає їх мовчки (велике зникнення буває й
  через збій читання) — тепер над чергою зʼявляється банер із кнопкою
  «Звірити видалення»: підтвердьте, якщо справді видаляли, і вони підуть в
  Архів. Помилкове повертається наступним синком. Банер зникає сам, коли
  причина минула.

## [0.6.0] — 2026-08-31
### Додано
- **Видно, коли йде синхронізація з таблицею.** У шапці черги напис «таблиця»
  показує стан наживо: поки CRM звіряється з Google Таблицею — пульсує
  «синхронізує…», на паузі — «на паузі», інакше час останнього оновлення.
  Більше не треба гадати, працює воно чи зависло.
- **«Імпортувати всю історію» більше не блокує застосунок.** Раніше кнопка
  вішала сторінку на кілька хвилин без жодного знаку життя. Тепер імпорт іде
  у фоні: одразу кажемо «почато», можна працювати далі, а по завершенню
  зʼявляється підсумок.
- **Дія «Звірити видалення»** (Налаштування → Google → Разові дії). Коли ви
  свідомо видалили в таблиці одразу багато рядків, звичайний синк тримає їх у
  черзі (захист від збою читання). Ця кнопка прибирає їх у Архів за вашим
  підтвердженням. Помилка не страшна — робота, що досі в таблиці, повернеться
  сама за ~10 хвилин.
- **Права панель на черзі згортається.** На широких екранах, коли всі колонки
  не вміщались і «Оператор» обрізався, тепер можна згорнути праву панель —
  таблиця забирає її місце. Якщо не влазить — панель згортається сама.

### Змінено
- Картки «Стан системи» в налаштуваннях ожили: числа набігають, тепле світло
  ковзає по картках. Кнопки в налаштуваннях вирівняно по одній лінії.

## [0.5.1] — 2026-08-31
### Виправлено
- **Можна безпечно вимикати CRM на кілька днів.** Поки застосунок вимкнено,
  лабораторія працює в Google Таблиці — з'являються нові дні, змінюються
  роботи. Раніше після ввімкнення підтягувались лише вчора-сьогодні-завтра, і
  дні простою доводилось добирати вручну кнопкою «Імпортувати всю історію».
  Тепер CRM сама пам'ятає, коли синхронізувалась востаннє, і при запуску
  добирає всі пропущені дні. Роботи при цьому не видаляються — тільки
  оновлюються й доповнюються.

## [0.5.0] — 2026-08-31
### Виправлено
- **На видачі з'явився сьогоднішній день.** Раніше екран показував тільки
  вчора і старіше — за логікою «цирконій увечері в піч, видаємо вранці». Але
  ПММА, титан і воски готові одразу після фрезерування, і їх видають того ж
  дня — а видача їх ховала. Тепер сьогоднішній день є серед днів; екран
  за замовчуванням, як і раніше, відкривається на вчорашньому.

## [0.4.9] — 2026-08-31
### Виправлено
- **Видача: сусідні дні відкриваються швидко.** Перемикання на «вчора» щоразу
  ходило в повний обхід мережевої папки прямо в момент кліку — тепер усі
  видимі дні прогріваються заздалегідь у фоні. Режим «усі дні» лишається
  повільнішим свідомо: це обхід усіх клієнтів за місяць.
- **Видача: список днів більше не ховається за фото шапки** — випадне меню
  з усіма днями різалося рамкою шапки після появи фонового кадру.
- **Кадр верстата: довший прогрів знімка.** На верстатних ПК стоїть старий
  Windows, де швидке читання екрана недоступне — тепер знімок чекає вісім
  секунд, поки сервер обійде весь екран, і краї кадру не лишаються білими.

## [0.4.8] — 2026-08-31
### Виправлено
- **Кадр верстата більше не приходить «недофарбованим».** Одразу після
  підключення частина екрана (колонка задач, температура, нижній рядок)
  могла бути білою — сервер на ПК верстата не встигав зібрати картинку.
  Тепер знімок дає йому пару секунд і бере повний кадр.

## [0.4.7] — 2026-08-31
### Додано
- **Верстати в CRM — видно екран кожного, не відкриваючи RustDesk.** Новий
  екран «Верстати»: живий кадр екрана RemiCORE кожного верстата раз на
  15 секунд, з розгортанням у повний розмір. Верстат, чий ПК не відповідає,
  не зникає — плитка прямо пише причину. Підключення — у Налаштуваннях,
  розділ «Верстати»: на ПК верстата ставиться безкоштовний UltraVNC у режимі
  «тільки перегляд», CRM лише дивиться і фізично не вміє нічого натиснути.
  Наступний крок — розпізнавання відсотка виконання й приблизного часу
  закінчення прямо з кадру, як уже зроблено з табло печі.

## [0.4.6] — 2026-08-31
### Додано
- **Сторінка входу ожила.** Замість мальованого фону — живе відео з нашого
  власного цеху: руки оператора тягнуться до щойно відфрезеруваного диска у
  верстаті. Назва застосунку стоїть по центру, тихо, не заважаючи полям.
- **Порожня черга теж із нашим верстатом.** Коли робіт немає, на всю картку
  черги йде кадр камери з диском — верстат працює, роботи будуть. Текст
  підказки стоїть по центру поверх притемненого кадру.
- Обидва відео зупиняються для тих, хто ввімкнув у системі «менше руху».

## [0.4.5] — 2026-08-31
### Додано
- **Матеріал тепер видно, а не тільки читаєш.** У бібліотеці матеріалів кожна
  родина отримала кадр самої речі: цирконієвий диск із відфрезерованими
  коронками (справжнє фото з нашого верстата), диск PMMA у відтінку A3,
  титанові абатменти, спечений каркас у порошку, восковий блок. Той самий кадр
  іде маленькою міткою в паспорті роботи, поруч із матеріалом і кольором.

### Виправлено
- **Знак застосунку більше не «губився» дорогою до ярлика.** Після
  перейменування на KuubMill генератор знака малював іконку під старим ім'ям,
  а збірка брала стару — тобто будь-яка правка логотипа не доїхала б ні до
  ярлика, ні до трею, ні до інсталятора. Сам знак не змінився.

## [0.4.4] — 2026-08-31
### Виправлено
- **Шапка ранкової видачі: замість розмитої плями — видно самі коронки.**
  Знімок був завеликий для тонкої смуги, і в неї влізав шматок однієї
  розфокусованої коронки. Тепер у смузі стоїть панорамний кадр ряду коронок
  на лотку, а числа дня й перемикачі отримали власну підкладку — вони
  читаються навіть там, де під ними світле фото.

## [0.4.3] — 2026-08-30
### Додано
- **Пічки в бічній панелі стали живими плитками з жаром.** Замість блідого
  прямокутника — кадр розжарених коронок на всю плитку. Піч, яка працює,
  світиться і пускає іскри; вільна — притишена й холодна; та, що не
  відповідає, фарбується тривожним і прямо каже причину. Стан печі видно
  тепер краєм ока, не вчитуючись.
- **Шапка ранкової видачі отримала свій кадр** — спечені коронки на лотку,
  а поверх них ті самі контроли й числа дня (скільки клієнтів і скільки
  робіт уже зібрано).

## [0.4.2] — 2026-08-30
### Виправлено
- **17 клієнтських робіт зникли з черги, стоячи в таблиці.** Один збійний
  сеанс читання таблиці повернувся неповним, і синк вирішив, що «зниклих»
  рядків більше немає — відправив їх в архів. А повернути їх не міг ніхто:
  правило повернення спрацьовувало лише коли в рядку інша робота. Тепер,
  по-перше, масове «зникнення» (понад чверть вкладки за раз) не архівує
  нікого — це ознака поганого читання, а не чищення; по-друге, робота, що
  стоїть у таблиці, сама повертається з архіву. Ваші 17 робіт повернуться
  автоматично після оновлення.
- **«моно А3» показувалось як «моноА3»** — пробіл з'їдало оформлення, дані
  були цілі. Тепер видиме значення збігається з таблицею символ-у-символ.
- **Синхронізація з таблицею падала об хвилинний ліміт Google.** Повтори йшли
  надто швидко і всі влучали в ту саму хвилину; тепер пауза виходить за її
  межу, і синк доживає до успіху.
- **Пришпилені минулі роботи тепер добре видно** — контур удвічі товщий і
  яскравіший, на червонуватих прострочених рядках він більше не тоне.
- **Sum3D на переробці**: порожнє поле — це поле для ID нового фрезерування
  (колонка W), а не втрата; підказка тепер прямо називає перший ID і де він.
- Вимірювач «ваги» таблиці в налаштуваннях знову працює.

## [0.4.1] — 2026-08-30
### Виправлено
- **Час відкриття печі рахувався не з того лічильника — помилка на сім годин.**
  На табло три різні числа часу: залишок поточної команди рецепта, залишок
  усієї програми і час доби. Ми брали перше. На реальній закладці застосунок
  написав би «відкриється о 23:00», а піч відкривається близько 06:30 ранку —
  оператор прийшов би вночі до зачиненої печі. Тепер час рахується із залишку
  всієї програми і лише коли піч справді працює.
- **Смужка прогресу на плитці печі показувала неправду** — вона брала час доби
  за «скільки вже відпекли» і на щойно запущеній програмі малювала 90%.
  Прибрана: надійного «скільки пройдено» піч не показує.
- **Температура могла показати чуже число.** На живій печі табло показувало
  138 °C, а застосунок — 133: звірка цифр допускала невеликий незбіг, і
  невідома вісімка «виграла» в трійки. Тепер цифра або збігається точно, або
  поле лишається порожнім.
- **Зникав залишок часу, коли на табло траплялось «44»** — дві четвірки
  злипались в одну пляму, і число не читалось. А саме з нього рахується, коли
  відкривати піч.
- Температура тепер читається повністю: раніше великому шрифту бракувало
  цифр 1, 2, 3, 6 і 8, і будь-яке число з ними показувалось як «—».

## [0.4.0] — 2026-08-30
### Додано
- **Застосунок називається KuubMill.** Нова назва скрізь: у вікні, в теці даних,
  в інсталяторі, у звітах. Свій знак — монограма KM.
- **Дві теми оформлення на вибір і шість стилів іконок**, окремо для кожного
  оператора: увійшов під своїм логіном — бачиш свій вигляд. Вибирається в
  «Мій обліковий запис», поруч показано живий приклад сторінки.
- **Шестерня «вигляд списку»** над чергою і над поштою: висота рядків, ширина
  списку, готові набори (компактний / звичайний / просторий). Кнопки «−» і «+»
  можна затиснути. Налаштування зберігається на вашому обліковому записі.
- **Пічки в правій панелі черги** — плитки з температурою, смужкою до
  відкриття і часом, коли піч відкриється.
- **Ранкова видача: розкладка «Список + покажчик»** — вузький покажчик дня
  збоку, де видно всіх клієнтів, скільки в кого зібрано і де ви зараз.
  Перемикається іконкою в шапці, вибір запам'ятовується.
- **Ранкова видача: у шапці видно і клієнтів, і роботи** за обраний день.
- **Сторінки помилок** замість голого «Internal Server Error»: що сталося,
  куди натиснути, і де лежить файл журналу, який просять надіслати.
- **Пришпилені роботи спливають нагору** черги, у порядку, в якому ви їх
  пришпилювали, і їдуть туди анімовано.

### Виправлено
- **Лист, чиї файли зникли з диска, більше не глухий кут.** Раніше в списку
  стояло «4 файл. · ГОТОВО», хоча приймати не було чого. Тепер видно
  «0 з 4 файл. · НЕМАЄ ФАЙЛІВ», а в самому листі є кнопка «Скачати наново».
- **Піч, яка перестала відповідати, більше не зникає тихо** — лишається на
  екрані й пише причину («не відповіла за 20 с»).
- **Лічильник видачі більше не їде назад**: раніше після виданого клієнта
  «1 / 34» ставало «0 / 33».
- **Попередження «технік змінив уже взяту роботу» більше не ховається** під
  підсвіткою пришпиленого рядка.
- **Невдала дія більше не мовчить** — раніше на черзі й у пошті кнопка могла
  просто нічого не зробити, без жодного повідомлення.
- Імена клієнтів у черзі більше не обрізаються до «Vis…»; стовпець чесно
  зветься «Наряд / Клієнт», бо в ньому буває і те, і те.
- Тріаж пошти на широкому екрані: список більше не тулиться в третину екрана.

## [0.3.32] — 2026-08-30
### Додано
- **Екран «Пічки» — видно, що робить пічка спікання, не встаючи від комп'ютера.**
  Для кожної печі: працює вона зараз чи стоїть (RUN / WAIT), скільки лишилось
  до кінця програми (і о котрій це буде), і яка зараз температура. Оновлюється
  саме собою кожні кілька секунд.
  Під кожним числом — смужка справжнього екрана печі, з якої це число прочитано:
  цифру завжди можна звірити оком. Якщо щось прочиталось непевно, замість числа
  стоїть «—» і попередження — вигадувати число система не буде.
  Екран тільки для перегляду: керувати піччю з порталу не можна й не планується.
  Адреси пічок і пароль екрана — у Налаштуваннях, розділ «Пічки спікання».
- **Пічки налаштовуються по одній.** У Налаштуваннях тепер рядок на пічку:
  назва, IP-адреса, порт, галочка «стежити» і, за потреби, власний пароль
  (порожній = спільний). Пічку можна тимчасово вимкнути на час ремонту — вона
  лишиться в переліку зі своїми налаштуваннями, але не опитуватиметься й не
  показуватиметься на черзі. Стара текстова настройка переноситься сама.
- **Смуга пічок угорі черги.** На головному екрані, там де оператор і так
  стоїть, по кожній пічці видно: скільки градусів, скільки ще йти програмі
  і о котрій вона відкриється (час київський). Показуються лише ті пічки, з
  яких справді приходять дані: пічка, що мовчить, не займає місце в смузі —
  її стан видно на екрані «Пічки». Смугу можна згорнути — тоді
  лишається один рядок «Пічки · 2 у роботі · найближча 17:54», і застосунок
  запам'ятовує цей вибір. Клік — на повний екран печей. Поки печі не
  налаштовані, смуги немає взагалі.

## [0.3.31] — 2026-08-28
### Додано
- **STL-прев'ю: повне ручне керування моделлю.** Права кнопка миші зупиняє
  авто-обертання; утримуєш і ведеш — крутиш модель рукою в будь-який бік
  (вільний трекбол, без «замкнених» осей); колесо миші наближає й віддаляє.
  Швидкість авто-обертання зі слайдера тепер запам'ятовується — виставив раз
  і лишається наступного разу, поки сам не зміниш.

### Виправлено
- **Додавання коментаря до роботи більше не зависає.** Раніше коментар чекав
  відповіді Google-таблиці (до ~40с на повільному з'єднанні); тепер зберігається
  миттєво, а в таблицю дописується у фоні.

### Технічне
- Прибрано рідкісну гонку між фоновим синком і переглядом черги.
- Внутрішнє впорядкування коду (фонові процеси, Windows-автоматизація) —
  підготовка до подальшого розбиття; поведінка не змінилась.
## [0.3.30] — 2026-08-28
### Виправлено
- **Видача більше не каже «Прив'язати папку» там, де папка прив'язана.**
  Посилання на папку клієнта бралося зі знайдених партій, тож «немає свіжих
  партій» виглядало як «прив'язки немає» — і оператор ішов шукати те, чого не
  бракує. Тепер папка клієнта відкривається завжди, коли вона прив'язана.
- **Робота знаходить свою папку, навіть якщо файли надіслали задовго до
  фрезерування.** Раніше програма дивилась лише на свіжі партії, і давні
  файли ніби зникали. Тепер, якщо у свіжих нічого немає, вона зазирає в
  найновіші папки клієнта незалежно від дати.
## [0.3.29] — 2026-08-28
### Виправлено
- **Папка нарешті відкривається розгорнутою.** Попередня спроба знаходила
  вікно й розгортала його — а Провідник згортав назад, бо застосовує своє
  збережене положення вже після того, як вікно з'явилось. Тепер програма не
  просить один раз, а стежить кілька секунд і не дає вікну згорнутися.
## [0.3.28] — 2026-08-28
### Виправлено
- **Галочка «знайдено» ставиться миттєво.** Раніше кожен клік чекав, поки
  програма достукається до Google Таблиці й перефарбує рядок. На видачі
  галочки клацають підряд, тож це чекання діставалось десятки разів за ранок.
  Тепер таблиця оновлюється у фоні, а галочка стає одразу.
- **Екран більше не смикається на початок.** Кожна відмітка перезавантажувала
  сторінку цілком, і оператора викидало вгору — хоча він іде списком згори
  вниз. Тепер оновлюється лише список карток, а екран лишається там, де його
  прокрутили.
- **Провідник таки відкривається розгорнутим.** Виправлення з 0.3.27 не
  спрацювало: вікно шукалося за назвою в заголовку, а Провідник дописує до неї
  службовий текст, різний у різних версіях Windows. Тепер вікно знаходиться
  надійно, незалежно від заголовка й мови системи.
- **Sum3D ID більше не перекреслений у знайденій роботі.** Перекреслення
  читалось як «цього ID більше немає», хоча робота ще не видана і ID ще
  звіряють. Стан і так видно по галочці.
## [0.3.27] — 2026-08-28
### Виправлено
- **Під роботою тепер її папка, а не тиждень чужих.** У постійного клієнта,
  який замовляє той самий матеріал мало не щодня, під однією роботою висіло
  по чотири папки з різних днів — підказка, що не звужувала пошук, а
  розширювала. Тепер береться одна партія: найближча з тих, що не пізніші за
  день самої роботи. Кілька папок лишається тільки коли вони справді з
  одного дня — це вже вибір оком, як і має бути. Якщо папка все-таки з
  іншого дня, поруч з часом стоїть і дата.
- **Провідник відкривається розгорнутим і спереду.** Раніше вікно могло
  з'явитись згорнутим або за браузером — програма просила відкрити папку, а
  вікно створювала оболонка Windows, до якої це прохання не доходило. Тепер
  вікно розгортається й виноситься наперед окремо.
## [0.3.26] — 2026-08-28
### Виправлено
- **Робота знаходить свою папку, навіть коли назви написані по-різному.**
  У таблиці технік пише коротко — `emo a3`, а папку на диску називають
  повністю — `Emotions A3 опаковий всередині`. Раніше програма вимагала, щоб
  збіглося слово в слово, тому папка лежала поруч, а робота її не бачила.
  Тепер коротка назва впізнає повну, а зайві уточнення в назві папки не
  заважають. Відтінок як був головним, так і лишився: `a3` і `a3.5` — різні
  диски, і сплутати їх програма не може.
- **Кнопка «Відкрити папку» справді відкриває папку.** Вона мовчала: браузер
  блокує відкриття диска зі сторінки, і робив це без жодного повідомлення.
  Тепер папку відкриває сама програма, а якщо не вдалося — про це видно
  повідомлення, а не тиша.

### Змінено
- **З картки клієнта прибрано підвал «Інші папки».** На ранковій видачі
  звіряють коронку з моделлю за рядком роботи, а чужі папки лише відвертали
  увагу. Папка клієнта цілком лишається одним кліком.
## [0.3.25] — 2026-08-28
### Змінено
- **Видача відкривається на останньому дні, а не на всіх одразу.** Так і йде
  робота: печі відкриваються вранці, і видають те, що відфрезерували вчора.
  Раніше екран показував усі минулі дні разом — 262 клієнти й тисячі папок,
  які треба було перечитати на сховищі ще до того, як з'явиться перший рядок.
  Тепер це десятки папок.
- **Старіші дні нікуди не поділись.** У шапці написано, скільки робіт
  лишилось на інших днях, і цей напис веде на повний список. Чіп «усі» і
  чіпи днів — на місці, як були.
## [0.3.24] — 2026-08-27
### Виправлено
- **Видача більше не змушує чекати.** Цього разу причину не вгадали, а
  виміряли просто на робочому сховищі: кожне звертання до папки в мережі
  коштує час очікування, і сотні таких звертань програма робила одне за
  одним, стоячи в черзі сама до себе. Тепер вона читає папки паралельно —
  вчетверо швидше. А головне: цю роботу тепер робить фон кожні дві хвилини,
  ще до того, як хтось відкрив екран. Оператор приходить на готове. Перше
  відкриття одразу після запуску програми ще може зачекати — далі ні.
- **Екран «Клієнти» відкривається миттєво.** Він перечитував усе сховище
  цілком — десятки тисяч папок — хоча йому потрібен лише список імен папок
  клієнтів, а це один-єдиний запит. Плюс кожен клієнт звірявся з кожним
  рядком таблиці, хоча різних написань імені всього кілька сотень на тисячі
  рядків.
- **Обидва екрани тепер записують свій час у журнал.** Наступну скаргу на
  повільність можна буде перевірити цифрою, а не здогадкою.
## [0.3.23] — 2026-08-27
### Виправлено
- **Видача більше не перечитує роками накопичені папки.** Журнал показав
  справжній масштаб: програма читала **46 148 папок** на мережевому сховищі
  за одне відкриття — по 176 партій на кожного з 262 клієнтів, тобто все, що
  накопичилось за роки. А на екрані роботи лише за останні 30 днів. Тепер
  стара партія пропускається одразу, без заглядання всередину: сховище й так
  повідомляє дату створення разом зі списком папки. Межа рахується від
  найстарішої показаної роботи з тижневим запасом — файли не можуть лежати
  в папці, створеній до появи самої роботи.

## [0.3.22] — 2026-08-27
### Виправлено
- **Видача і Клієнти відкриваються за частку секунди.** Це справжня причина,
  через яку сторінки не відкривались, — і вона була не в мережевому сховищі,
  як здавалось спершу. Програма звіряє ім'я кожного клієнта з назвами тек, і
  робила це найдорожчим можливим способом: навіть знайшовши потрібну теку
  точним збігом, вона все одно порівнювала клієнта з усіма іншими сотнями тек
  — заради двох рядків підказки «схоже також на…». Плюс одні й ті самі імена
  перераховувались мільйони разів за один запит. Тепер очевидно несхожі назви
  відкидаються одразу, а результат обчислюється один раз і запам'ятовується.
  На тестових даних: 1.7 секунди замість хвилин, і 0.15 при повторному
  відкритті. Логіка зіставлення не змінилась — ті самі теки знаходяться так
  само.

## [0.3.21] — 2026-08-27
### Виправлено
- **Клієнти більше не задвоюються.** На екрані «Клієнти» кожне ім'я стояло
  двічі — 458 карток замість половини. Причина: коли дві сторінки зверталися до
  програми одночасно, обидві бачили «такого клієнта ще нема» й обидві його
  заводили. Розтягнута видача робила це майже неминучим: поки вона хвилину
  читала сховище, вистачало відкрити «Клієнтів» у сусідній вкладці.
  При оновленні дублі зливаються автоматично: залишається та картка, де вже
  вписані телефон, пошта чи нотатки — введене не губиться. Заразом
  підчищаються зайві пробіли в іменах, з яких дублі й починалися. Повторно
  створити однакового клієнта тепер неможливо.

## [0.3.20] — 2026-08-27
### Виправлено
- **Ранкова видача більше не читає все сховище.** Головне виправлення з
  трьох. Попередні дві версії робили обхід папки `export` дешевшим, але
  програма все одно щоразу переглядала ВСІ теки на Synology — сотні, — щоб
  показати десяток клієнтів. Тепер вона питає лише список імен тек (це один
  запит) і зазирає всередину тільки тих клієнтів, які справді на екрані.
  Якщо обрано конкретний день — тим паче: читаються лише його клієнти.
- **У журнал додано час обходу сховища.** Рядок виду «Handout export scan:
  12 клієнтів на екрані, 340 тек у сховищі, 47 записів, 1.20с» у
  `kuubmill.log`. Коли сторінка знову почне гальмувати, причина буде
  видна одразу, а не через кілька спроб навмання.

## [0.3.19] — 2026-08-27
### Виправлено
- **Видача відкривається швидше вчетверо.** Продовження попереднього
  виправлення. Папка `export` лежить на мережевому сховищі Synology, а
  програма опитувала його надто дрібно: окремий запит на кожну теку й
  кожен файл. Тепер вона питає одразу про всю теку — сховище й так віддає
  всю потрібну інформацію однією відповіддю. Мережевих звернень стало
  приблизно вчетверо менше, і саме вони й були тим часом очікування.
  Разом із памʼяттю з 0.3.18: перше відкриття помітно швидше, наступні
  миттєві.

## [0.3.18] — 2026-08-27
### Виправлено
- **Ранкова видача знову відкривається.** Сторінка могла вантажитись
  хвилину й довше, а часто не відкривалась узагалі. Причина: перед показом
  вона щоразу обходила всю папку `export` на мережевому диску — теку за
  текою, клієнт за клієнтом. Вибір дня не допомагав, бо обхід ішов до
  фільтрації. Тепер результат обходу тримається в памʼяті: перше відкриття
  після запуску програми ще чекає, усі наступні — миттєві. Коли оператор
  приймає лист і файли переїжджають у `export`, памʼять скидається одразу,
  тож нові роботи видно без затримки.

## [0.3.17] — 2026-08-27
### Додано
- **Часткову видачу нарешті видно.** Кнопка «Видати» раніше зʼявлялась лише
  тоді, коли знайдено ВСІ роботи клієнта. Але пічки відкриваються тричі на
  день, тож роботи одного клієнта фізично виходять партіями — і клієнт із
  півсотнею робіт міг не отримати цю кнопку жодного разу за день. Тепер вона
  зʼявляється, щойно знайдено хоч одну, і каже скільки піде: «Видати 3 з 12».
  Решта лишається в картці й чекає наступної пічки.
- **Кабінет оператора.** Своя літера для колонки «Прорахував», зміна пароля,
  «Закінчити зміну».
- **Журнал дій і «Крок назад».** Кожна дія з таблицею — Sum3D, статус,
  оператор, видалення — записується і скасовується. Поруч «Крок вперед», щоб
  повернути скасоване, і попап «Останні дії» з переходом до потрібної роботи.
- **Колонка «Оператор» у черзі.** Редагується прямо в рядку й одразу пише в
  стовпець «Прорахував» (М) таблиці. Хрестик очищає.
- **Клієнти: режим «Майстер».** Список ліворуч, картка праворуч. Картки
  заводяться самі з робіт, у списку видно, чи прив'язана папка.
- **Прив'язка папки клієнта прямо в його картці**, а ім'я клієнта відкриває
  папку в Провіднику.

### Змінено
- **Матеріал і колір — одне маркування.** Замість бейджа й окремого чіпа
  тепер `Zr │ моно A3.5`. Назва матеріалу більше не дублюється двічі, а
  колонка вирівняна в стовпчик. У панелі «Вигляд» зʼявився перемикач
  «Колір роботи»: «Маркування» або «Технічний код» (моноширинний, коди
  виробника читаються першими). Видача тепер показує матеріал так само,
  як черга — раніше там був сирий текст із таблиці.
- **Відмітку «знайдено» на видачі збільшено** з 26 до 40 пікселів. Це
  найдорожча дія в програмі, а була найменшою ціллю свого екрана.
- **Видача: варіант «Черга»** — порядок фрезерування як хребет екрана,
  три дати в пейджері замість однієї, банер замість десятків однакових
  попереджень.
- **Менше миготіння.** На черзі одночасно крутились три нескінченні
  анімації; лишився один маячок «черга жива».

### Виправлено
- **Кнопка видалення рядка не працювала взагалі** — вона жила в прихованій
  колонці «Статус» і фізично не існувала на екрані. Повернена в комірку
  наряду.
- **Колір відтінку розбирався неправильно.** `с2 транс` показувалось як
  «с» + «2 транс», `пмма в1` як «пмма в» + «1». Тепер шкала Vita (A/B/C/D
  і BL) розпізнається обома абетками.
- **Видача й пошук відкривались повільніше, ніж мали.** Кожен рядок робив
  власний запит по матеріал.
- **Контрасти.** Титан на прострочених рядках, межа й підказка поля Sum3D,
  межа неактивного чіпа дня — усе не дотягувало норми й погано читалось
  при денному світлі. Поля «Оператор» і «Коментар» отримали підкреслення,
  бо без нього виглядали як звичайний текст.
- **Бічна панель більше не зʼїдає колонки.** На вужчому екрані вона
  ховала «Технік», «Коментар», «Sum3D ID» і «Оператор» — тепер складається
  під таблицю.
- **Повідомлення більше не накриває заголовок.**

## [0.3.16] — 2026-08-25
### Виправлено
- **Готові до взяття роботи знову видно.** Критично: якщо Sum3D прибрали в
  таблиці (роботу повернули в чергу), CRM не звільняла її — вона застрягала у
  «В роботі» й зникала з «Можна брати». Тепер порожній Sum3D у таблиці одразу
  повертає роботу в доступні. Таблиця знову головна для Sum3D.

### Додано
- **Літера оператора в «Прорахував».** Коли оператор вписує Sum3D через портал,
  його літера (Р, К, СТ…) сама лягає в колонку «Прорахував» — М для звичайної
  роботи, Х для переробки. Літери задає адмін у Налаштуваннях → Оператори.
  Доки літеру не призначено — нічого не пишеться, все як раніше.

## [0.3.15] — 2026-08-25
### Виправлено
- **Sum3D взятої роботи більше не зникає.** Якщо запис ID у таблицю не проходив
  (звʼязок із таблицею моргнув) або клітинку очистили — робота могла тихо
  «розібратись» назад у «можна брати», і її міг схопити другий оператор. Тепер
  ID тримається; таблиця може його заповнити чи змінити, але не стерти.
- **Sum3D і позначки лягають у правильний рядок навіть після перебудови
  таблиці.** Якщо хтось саме видалив рядок вище, запис більше не потрапляє в
  чужу роботу — система звіряє наряд і знаходить потрібний рядок.
- **Робота без наряду більше не стає «фейковим клієнтом» і не зникає.** Коли
  технік вносить роботу до присвоєння наряду, вона тепер показується як
  лабораторна робота без наряду (а не клієнт, названий за видом роботи), і
  спокійно набуває наряд згодом.

## [0.3.14] — 2026-08-25
### Виправлено
- **Видалення клієнтської роботи прибирає синю заливку повністю.** Раніше
  рядок лишався синім (біла була лише клітинка з іменем) — тепер увесь блок
  стає білим, як у нового рядка.
- **Після ручного додавання ви лишаєтесь на місці у списку** — черга більше
  не прокручується на початок.

### Змінено
- **Стовпець «Кількість»** центровано — число під серединою заголовка.

## [0.3.13] — 2026-08-25
### Змінено
- **Черга: лабораторні роботи зверху, клієнтські знизу** — як у таблиці, коли
  не задано фільтр джерела/готовності й ручне сортування. Раніше вони йшли
  впереміш.
- **Сума одиниць** у шапці черги, поруч із лічильником — рахує кількість по
  ввімкнених фільтрах.
- **Стовпець «Кількість»** вирівняно під заголовок (не зсунуто вправо).

### Виправлено
- **Порожній коментар — порожньо.** Більше немає сірого «коментар…» на кожному
  рядку без коментаря; підказка з'являється лише коли клікнути в поле.
- **Папка з роботою відкривається нормальним вікном**, а не згорнутим.

## [0.3.12] — 2026-08-25
### Додано
- **Іконка в треї Windows.** Order Desk висить біля годинника, поки працює.
  Правий клік → **Відкрити Order Desk**, **Синхронізація таблиці на паузі**
  (перемикач), **Вийти** (коректне вимкнення).
- **Пауза синхронізації таблиці.** На паузі система не читає й не пише таблицю —
  адміни можуть спокійно правити її вручну. Дії, що пишуть у таблицю (взяти в
  роботу, змінити статус, видати, видалити, додати), на паузі чемно
  відхиляються з підказкою, тож нічого не розходиться. Зняв паузу — одразу
  читається свіжа таблиця. Керується з трею і з бічної панелі черги (адмін).

## [0.3.11] — 2026-08-25
### Виправлено
- **Видалення в таблиці одразу доходить у CRM через «Синхронізувати зараз».**
  Раніше щойно додану роботу, видалену з таблиці за кілька хвилин, ручна
  синхронізація не прибирала (спрацьовував захисний інтервал проти збоїв). Тепер
  ручний синк прибирає її негайно; автоматичний — як і раніше, трохи згодом.

## [0.3.10] — 2026-08-25
### Виправлено
- **Робота більше не показується «наполовину чужою».** Якщо рядок створили
  через CRM, видалили, а потім у те саме місце вписали іншу роботу — в черзі
  з'являвся гібрид зі старих і нових даних. Тепер рядок повністю заміщується
  новою роботою. Уже зіпсовані рядки самі виправляться при наступній
  синхронізації.
- **Видалення кількох робіт доходить повністю.** Якщо видалити з таблиці
  кілька робіт одразу, у черзі інколи лишалась одна з них. Тепер зникають усі.
  Уже «застряглі» роботи приберуться при наступній синхронізації.

## [0.3.9] — 2026-08-25
### Додано
- **Позначка «технік змінив роботу».** Якщо технік виправив рядок у таблиці
  після того, як робота потрапила в чергу (не той колір, інша папка), рядок
  підсвічується і показує, що саме змінилось і коли. Для вже взятих у роботу
  (є Sum3D ID) позначка червона — саме там ризик зробити брак за старими
  даними. Знімається вашим кліком, не зникає сама. Плюс спливаюче
  сповіщення — помітите, навіть якщо не дивитесь на екран.

### Змінено
- **Сповіщення оновлюються швидше** — за тим самим темпом, що й черга. На
  режимі «Турбо» попередження про зміну від техніка приходить за ~5 секунд.

## [0.3.8] — 2026-08-25
### Виправлено
- **Робота додається у відкритий день — з будь-якої вкладки.** У 0.3.7 це
  працювало лише коли день обрано в стрічці дат; з вкладок «Завтра» / «Вчора»
  робота все одно йшла в сьогоднішній день.
- **Видалений у таблиці рядок доходить у CRM, навіть якщо він був останнім.**
  Коли після видалення день у таблиці лишався порожнім, портал вважав це
  збоєм зв'язку і не прибирав роботу з черги. Завтрашній день зазвичай має
  один-два рядки, тож видалення з нього не спрацьовувало ніколи.
- **Повторна спроба після помилки не втрачає день.** Якщо додавання відхилили
  (не заповнене поле), наступна спроба писала в сьогоднішній день і викидала
  на початок черги.

## [0.3.7] — 2026-08-25
### Виправлено
- **Видалена робота більше не повертається.** У 0.3.6 робота, видалена з черги,
  за кілька секунд з'являлася знову: рядок у таблиці очищається у фоні, а
  синхронізація встигала прочитати його раніше й вважала роботу живою. Тепер
  робота повертається лише тоді, коли в рядок вписали **іншу** роботу.
- **Робота додається у вкладку, яку ви дивитесь.** Якщо додати роботу,
  переглядаючи завтрашній день, вона записувалась у сьогоднішній — і на екрані
  не з'являлась. Тепер потрапляє в той день, який відкрито.

## [0.3.6] — 2026-08-25
### Виправлено
- **Роботи з таблиці більше не «зникають».** Якщо рядок раніше видалили з
  черги, а потім у нього вписали нову роботу, вона не показувалась: портал
  оновлював дані, але лишав роботу в «Архіві». Тепер заповнений рядок
  повертає роботу в чергу, а в її історії лишається запис про це.

### Додано
- **Видалення роботи прямо з черги** — кнопка-кошик у рядку, без заходу в
  картку роботи. З'являється при наведенні на рядок і завжди перепитує перед
  видаленням. Робота йде в «Архів» (історія й коментарі зберігаються), рядок у
  таблиці очищається. Після видалення ви лишаєтесь на тій самій вкладці дня, з
  тими ж фільтрами й на тому ж місці списку.

## [0.3.5] — 2026-08-25
### Виправлено
- **Ручне додавання більше не затирає попередню роботу.** Якщо лабораторну
  роботу додали без номера наряду, наступне додавання лягало в той самий
  рядок таблиці і перезаписувало її. Тепер рядок вважається зайнятим і за
  кількістю чи видом роботи, а не лише за нарядом.
- **Enter у формі додавання відправляє її.** Раніше після натискання Enter
  нічого не відбувалося — доводилось цілитись мишкою в кнопку.
- **Після додавання залишаєтесь на місці** — та сама вкладка дня і ті самі
  фільтри, замість викидання на початок черги.

## [0.3.4] — 2026-08-25
### Додано
- **«Перевірити вагу таблиці»** в розділі «Стан системи» — рахує правила
  умовного форматування. Коли вкладку-день роблять копією вчорашньої, Google
  дублює правила поклітинково, і кожне звернення до таблиці починає платити за
  них (на тестовій було 105 тис. правил і 612 МБ метаданих). Кнопка лише читає
  й каже, чи повільність саме через це.

## [0.3.3] — 2026-08-25
### Додано
- **Видалення роботи з черги** — кнопка в картці роботи. Робота переходить в
  «Архів» (історія й коментарі зберігаються), а рядок у Google Таблиці
  **очищається**. Запис у таблицю фоновий — чекати не треба.

### Виправлено
- **Видалений у таблиці рядок більше не псує сусідні роботи.** «Видалити рядок»
  у Google зсуває все нижче вгору, і кожна робота під ним тихо перезаписувалась
  даними сусіда, а в архів ішла не та — видалена робота при цьому лишалась у
  черзі, показуючи чужі дані. Тепер роботи звіряються за нарядом (або
  клієнт+матеріал), і зсув розпізнається правильно.
- Тривалість запису в таблицю тепер видно в логах по фазах — щоб діагностувати
  повільне додавання, а не гадати.

## [0.3.2] — 2026-08-24
### Додано
- **Спливаючі сповіщення** з власним розділом у Налаштуваннях: вигляд (скляна або
  картка), позиція на екрані (4 варіанти) і перелік системних подій, які можуть
  спливати. Критичні не зникають самі — їх закриває оператор.
- **Системні тригери**: втрачено зв'язок із застосунком, Google Таблиця чи пошта
  не відповідають, зв'язок відновлено, нові роботи, нові листи, доступне
  оновлення. Кожен вмикається окремо; повідомлення показується лише на **зміні**
  стану, не повторюється щохвилини.
- **Підказки в згорнутому лівому меню** — раніше в режимі іконок пункти
  доводилось вгадувати.

### Змінено
- Перемикач скачування вкладень («лише довірені / усі підряд») став **скляним
  сегментним** — і в Налаштуваннях, і в шапці «Нові з пошти».
- Ліве меню: під курсором їде м'яка пляма світла, активний пункт отримав
  глибину, його іконка трохи більша.

### Виправлено
- **Лічильник нових листів більше не пульсує безкінечно.** Ліве меню видно на
  кожному екрані цілий день — постійний рух там втомлює. Увага тримається на
  числі.
- Лічильник більше не конкурує з активним пунктом меню (був другою бірюзовою
  плямою поруч).
- Спливаючі повідомлення більше **не накривають паспорт роботи** — праві позиції
  автоматично зсуваються, коли він відкритий.
- У налаштуваннях Google: блок OAuth показувався навіть у режимі сервісного
  акаунта; кнопки секції зібрані в один ряд замість трьох.

## [0.3.1] — 2026-08-24

### Додано
- Екран **«Стан системи»**: жива карта конвеєра (пошта → спул → черга → архів) і
  самоперевірка 7 пунктів (Таблиця, пошта, папки, диск, копія, оновлення) зі звітом
  у буфер обміну.
- **Командна палітра** `Ctrl+K` — швидкий перехід до будь-якого налаштування.
- Адреса **сервісного акаунта** прямо на екрані Google Таблиці з кнопкою «Копіювати» —
  видно, кому відкривати доступ до таблиці.
- Позначка **«змінено ДД.ММ.РР»** біля збережених секретів — коли востаннє їх міняли.
- Цей журнал змін у розділі «Про застосунок».

### Змінено
- Налаштування перебудовано в **бічну консоль**: розділи в лівому меню, одна секція
  на екран замість довгого списку.
- Збереження налаштувань більше **не перекидає** на інший екран — показує тост і
  лишає на місці.

### Виправлено
- Поле таблиці приймає **ціле посилання** з адресного рядка, а не лише голий ID.
- Помилки доступу до таблиці тепер кажуть, **що саме робити** (ID не знайдено /
  відкрити доступ сервісному акаунту / перевірити ключ), а не одне загальне
  «немає доступу».

## [0.3.0] — 2026-08-17

### Додано
- **Двосторонній запис** у Google Таблицю: ID проєкту Sum3D, статуси й коментарі
  повертаються в таблицю, не перезаписуючи ручні значення.
- **Retention 30 днів** і екран **«Архів»** (Хроніка) — старі роботи не зникають,
  а їдуть в архів.
- **Розпакування ZIP/RAR** з поштових вкладень.

### Виправлено
- **Watchdog поштового синку**: підвислий IMAP більше не блокує кнопку «вже
  виконується» до перезапуску.

## [0.2.0] — 2026-08-12

### Додано
- Читання **пошти (IMAP)**, екран **«Нові з пошти»**, ручне прийняття листів у чергу.
- Інсталятор **Windows**, автозапуск при вході й **автоматичне оновлення**.

## [0.1.0] — 2026-08-12

### Додано
- Перше підключення до **Google Таблиці**, екран **черги робіт**, вхід операторів.
