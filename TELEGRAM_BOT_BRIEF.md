# Бриф: двосторонній Telegram-бот KuubMill

Складено 10.09.26 з розмови з Ромою. Будувати в НОВОМУ чаті (CLAUDE.md §15:
один чат — один блок). Спершу прочитати цей файл і `app/services/telegram.py`.

## Мета

Рома бачить стан цеху з телефона, не відкриваючи KuubMill: меню кнопок у чаті
(пічки, роботи, верстати, Sisma) і сповіщення про події, які чекати біля
екрана немає сенсу.

## Вирішено (власник, 10.09.26)

- **Бот — наявний бот іншого проєкту.** (10.09.26 з'ясувалось, що його вхідні
  забирає інша програма, — KuubMill отримав окремого бота.) Він лише НАДСИЛАЄ (збірки APK іншого GitHub-
  проєкту шлють Рому через нього); вхідних ніхто не слухає, тож long polling
  з KuubMill ні з ким не конфліктує. Токен і chat_id уже в налаштуваннях
  (розділ «Зворотний зв'язок», зашифровано), `discover_chat_id` уже вміє.
- **Отримувач — лише Рома** (його приватний чат). Групи й інших людей — ні.
- **Двосторонній:** не лише пуш, а й меню з відповідями на запит.

## Перша версія — що саме

Меню (inline-кнопки, одна відповідь = одне повідомлення, яке оновлюється
`editMessageText`, а не сиплеться новими):
- 🔥 **Пічки** — стан кожної (гріє / охолоджує / вільна), коли відкриття
  (час київський). Джерело: `app/services/furnace.py` (`FurnaceState`,
  `FurnaceCard`); показувати лише печі З ДАНИМИ, як віджет.
- 🧾 **Роботи сьогодні** — лабораторія / пошта: скільки «можна брати»,
  «в роботі», «не готово» (§5, готовність із `job_code` + `sum3d_id`). Лише
  ЧИСЛА.
- ⚙️ **Верстати** — хто фрезерує (% і, якщо прив'язано, наряд), хто без
  зв'язку. Джерело: `app/services/machines.py` (`MachineCard`).
- 🖨 **Sisma** — шар N/M, прогноз кінця. Джерело: `app/machine_sisma.py`
  (`SismaReading`).

Сповіщення (сам пише):
- 🔥 пічка **закрилась** (цикл пішов) і **відкрилась / можна відкривати**;
- 🖨 **Sisma: друк закінчився**;
- 💬 **нове звернення зворотного зв'язку** — ВЖЕ є (`services/feedback.py` →
  `telegram.send_feedback`, з ретраєм); лише перевести на спільну чергу
  відправки, не дублювати.

## Архітектура (пропозиція)

- **Вхідні — long polling `getUpdates`** у фоновому воркері, як
  `_mail_sync_worker` у `web.py` (застосунок на 127.0.0.1 за TLS-проксі цеху,
  вебхук неможливий). `offset` зберігати в базі, щоб після рестарту старі
  натискання не виконувались удруге. Сесія — `new_legacy_session` (проксі
  цеху рве звичайну).
- **Лише один chat_id.** Будь-яке оновлення з іншого чату — ігнор, без
  відповіді (бот знайдуть сторонні; відповідь «не маєте доступу» — уже витік).
- **Вихідні — через одну чергу (outbox-таблиця)**: подія пише рядок, воркер
  відправляє з ретраєм. Той самий принцип, що з Sum3D 10.09.26: база — правда,
  Telegram — best-effort, збій не губить повідомлення мовчки.
- **Сповіщення — на ПЕРЕХІД стану, не на стан**: пічка «гріє→охолоджує»,
  Sisma «шар N/M → звіт/кінець». Дедуплікація ключем (пристрій + подія +
  цикл), щоб рестарт застосунку не слав «закрилась» удруге. Частота —
  `app/log_throttle.py`-подібний дросель.
- Роути не імпортують `web`; логіка — `app/services/telegram_bot.py`.

## Пастки (перевірити до «готово»)

- **Один слухач на бота.** При старті — `getWebhookInfo`: якщо вебхук
  стоїть (хтось колись увімкнув), long polling отримає 409 — показати це в
  налаштуваннях словами, не падати й не знімати чужий вебхук мовчки.
- **Дані пацієнтів / імена клієнтів у Telegram — НІ** (глобальні правила):
  лише числа, стани, номери нарядів максимум (уточнити в Роми, чи наряд
  допустимий). Telegram — чужий хмарний сервіс.
- **Токен не в лог і не в текст помилки** — `_net_error` уже маскує; нові
  виклики мусять іти через нього.
- **Хибне сповіщення гірше за жодне** (правило печей): статус печі — лише з
  голосування двох сигналів, як на екрані; сумнів = мовчати.
- Бот живе, поки працює KuubMill на робочому ПК; вимкнений ПК = тиша. Сказати
  це Ромі в меню («дані станом на HH:MM»).
- У тому ж приватному чаті приходять APK-збірки іншого проєкту — повідомлення
  KuubMill мати помітний префікс (🏭 KMill), щоб не губились.

## Перевірка

- Юніт: розбір апдейтів, фільтр chat_id, дедуп переходів, outbox-ретрай.
- **Наживо — лише з телефоном Роми** (бот справжній): без цього «НЕ
  ПЕРЕВІРЕНО» окремим рядком (CLAUDE.md §15). Перехід пічки/Sisma — на
  записаних кадрах (фікстури є), не лише «стан».

## Відкрите

- Чи можна в повідомленнях номер наряду (не ім'я клієнта)?
- Меню Верстати/Sisma — у першій версії чи другою хвилею?
