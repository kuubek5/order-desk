# Бриф: що далі після MCP

Складено 12.09.26 з розмови про діагностичний доступ. Для нового чату: прочитати
цей файл, далі за кроками. Журнал фіч — `HISTORY.md`, інваріанти — `CLAUDE.md` §14.

## Де ми зараз

Випущено (тег `v0.16.0` ще НЕ запушений, див. «Незакрите» нижче):

- `POST /mcp` у самому застосунку, шість інструментів **лише читання**:
  `kmill_health`, `kmill_queue`, `kmill_order`, `kmill_sync_journal`,
  `kmill_quota`, `kmill_log`. Гейт — loopback.
- Окремий слухач на 8011 (`app/services/mcp_gateway.py`) для запиту з ІНШОЇ
  машини: перемикач у Налаштуваннях → «Доступ по мережі», межа — токен.
- Екран віддає **один готовий рядок** `http://<адреса>:8011/mcp?t=<токен>` і
  готову команду брандмауера (`netsh`, на всі профілі). Інструкція —
  `MCP_ACCESS.md`.

Перевірено живими викликами на запущеному сервері (не лише тестами): 12 з 12 —
рядок з екрана працює, чужий токен `401`, перевипуск убиває старий, вимикання
закриває порт.

## Мережа: як влаштована дорога

Цеховий ПК САМ дзвонить додому — програма WireGuard на ньому, удома MikroTik із
«Back to Home». У цеху на роутері міняти не треба нічого. Удома
`192.168.88.0/24` (домашній ПК `.2`, шлюз `.1`), у цеху `192.168.1.x` —
підсітки не конфліктують.

**Стукати треба на адресу з тунелю** (поле `Addresses` у програмі WireGuard),
не на `192.168.1.x`. Цехова підсіть не потрібна: верстати й печі опитує сама
CRM.

## Крок 1. Підключитись (чекає на Рому в цеху)

1. Увімкнути перемикач, скопіювати рядок, надіслати.
2. Один раз виконати команду брандмауера з екрана.
3. Якщо зʼєднання рве після простою — `PersistentKeepalive = 25` у конфіг
   тунелю.

Одразу після підключення: `kmill_quota` за завантажений ранок — це й є вимір
запасу перед другим оператором (ліміт Google 60/хв, гальмо гарячого тіку 45).

## Крок 2. Кадри й зони через MCP (~день)

Навіщо: вузьке місце навчання розпізнавання — не алгоритм, а **дістати кадр і
знати, що на ньому написано**.

Додати інструменти: список кадрів пристрою; **сам кадр картинкою** (повний або
виріз зони); що читається в кожній зоні зараз і на чому спіткнулось («зона
обрізає символ», «немає еталона»); стан печей і верстатів з віком кадру.

**Важливий факт про сховище:** історії кадрів НЕМА. `save_frame(target.key, …)`
(`app/services/furnace.py:287`, `app/services/machines.py:612`) тримає ОДИН файл
на пристрій і перезаписує його. Виміряно 12.09.26: печі — 14 файлів, 0,28 МБ,
кадр ≈20 КБ; верстати — 3 файли, 0,27 МБ, кадр до 271 КБ. Тому «переглянути
сотні кадрів» сьогодні неможливо — лише найсвіжіший.

Навчання лишається ЗАПИСОМ і живе в репозиторії (`scripts/furnace_glyphs.py
learn`, `scripts/newgen_glyphs.py learn`) з тестом на тому ж кадрі. Через MCP
еталони не додаються: один кривий еталон псує читання назовсім і видно це не
одразу.

## Крок 3. Скринька невідомих екранів (~два дні)

Зчитувач сам помічає, що не зрозумів (статус не склався двома голосами; бітмапа
цифри без еталона; розклад екрана незнайомий) → кадр у скриньку. Рома
розмічає одним рядком на окремому екрані, плюс кнопка «це неважливо — не питай
більше». Його підпис — **дані, не правило**: я з нього роблю зону/еталон/правило
статусу з тестом.

**Дублі — умова існування фічі, не оптимізація.** Кадр раз на 6 с = 600/год на
пристрій, тобто гігабайти за добу. Тому відпечаток (хеш зменшеного
чорно-білого кадру), один запис на відпечаток, лічильник «бачено N разів,
уперше/останній раз». Межі: ~20 різних екранів на пристрій, зменшена копія
повного кадру + виріз зони, витіснення найстаріших, стеля в МБ. Оцінка:
печі ≈2 МБ, верстати ≈14 МБ у найгіршому разі.

Запобіжник: лічильник витіснених + рядок у лог через `app/log_throttle.py`
(перший раз, далі раз на годину) — інакше стеля спрацює тихо.

Захоплення не має гальмувати опитування: відпечаток із уже розібраного кадру,
запис лише на НОВИЙ відпечаток (урок зі стуку в порти — послідовний прохід
старить решту пристроїв).

## Крок 4. Другий оператор у браузері (ROADMAP хід 17)

Те саме, що MCP, але для всього інтерфейсу: один ПК = сервер із базою й
воркерами, другий заходить браузером.

Що вже є після 0.16.0: зразок «перемикач у налаштуваннях + сторож, що тримає
слухача», токен, правило брандмауера готовим рядком.

Чого бракує:
- **Головний застосунок не можна перепривʼязати без рестарту** — адресу
  `uvicorn` вибирає раз на старті (`app/windows_launcher.py:315`). Для другого
  оператора це разова дія в неробочий час, але перемикач мусить чесно казати
  «застосунок перезапуститься».
- Постійна адреса сервера + правило брандмауера на 8000 (лише приватний
  профіль/джерело).
- «Відкрити теку» — єдина операторська дія, замкнена на loopback: по мережі
  віддавати копіювання шляху в буфер (усі теки мережеві, шлях вставиться).
- Перед підключенням — поміряти квоту (`kmill_quota`): до 25/хв запас є, від 35
  спершу знизити темп.
- Пастка з ROADMAP #26: «Вийти» в треї на ПК-сервері вимикає застосунок і
  другому оператору — потрібен чесний текст підтвердження.

НЕ можна: другий KuubMill на спільному файлі бази через мережеву шару (SQLite у
WAL цього не вміє) і два синки в ті самі клітинки.

## Незакрите на момент брифу

- **Тег `v0.16.0` не запушений:** `git push` заблокував класифікатор дозволів.
  На `resilience/etap-1` лежать три локальні коміти: `f7a9f05` (фіча),
  `259d1a9` (bump) і `ed38c38` (спрощення + цей бриф). Спрощення пішло ПІСЛЯ
  бампа, тож тег мусить стояти на HEAD, а не на коміті бампа — інакше в
  інсталятор не потрапить половина фічі. Порядок, коли тести зеленітимуть:
  `ruff` → `pytest -q` → `pytest tests/test_version_sync.py` → тег на HEAD →
  `git push origin resilience/etap-1` → `git push origin v0.16.0`.
- **Набір тестів не може бути зеленим у вихідні.** 12.09.26 (субота, після
  07:30) падали 23 тести в шести файлах: вони будують назву вкладки з
  `business_today()`, а черга й синк дивляться на `business_tab_today()` — у
  вихідні це пʼятниця. Поведінка застосунку ПРАВИЛЬНА, ламались саме тести.
  Виправлення — пін годинника (`app.business_day.business_now`) у тестах, без
  жодної правки `app/`. CI гоняє `pytest` як ворота, тож без зеленого інсталятор
  не публікується.
- **НЕ ПЕРЕВІРЕНО:** запит із домашнього ПК через справжній тунель і правило
  брандмауера — лише на цеховому ПК.
- Заготовлене раніше й досі чекає: тип архіву з листа визначати за байтами, а не
  за розширенням (`app/archive_extract.py:67`).
