# KuubMill — контекст проекту

Постійний контекст проекту. Codex читає цей файл автоматично на старті кожної сесії.

## 1. Що це і навіщо

Фрезерний центр дентальної лабораторії: ~10 верстатів imes-icore, фрезерують коронки, абатменти, каркаси з цирконію, ПММА, титану. Робота приходить з двох джерел, зараз обидва канали зводяться вручну в Google Таблицю. KuubMill — окремий сервіс (не модуль існуючого CNC Tracker), який об'єднує обидва джерела в одну робочу чергу, автоматизує синхронізацію з таблицею і закриває найболючіше ручне місце — ранкову видачу готових робіт.

## 2. Робочий процес (як є зараз)

**Джерело 1 — внутрішня лабораторія.** Лабораторія моделює роботу, скидає файли на сервер, пише рядок у Google Таблицю: номер наряду, колір/матеріал, кількість одиниць, вид конструкції, дедлайн. Оператор копіює номер роботи в Sum3D (CAM-програма) для прорахунку.

**Джерело 2 — пошта з усієї країни (ukr.net).** Клієнти шлють файли листом, у тексті листа — матеріал, фрезерування/на фрезерування, колір. Зараз оператор вручну скачує вкладення і вручну дописує рядок у ту саму таблицю (клієнт, колір, матеріал, кількість одиниць).

**Взяття в роботу.** Коли оператор бере роботу, він прораховує її в Sum3D і вписує в таблицю ID проекту з Sum3D — щоб потім можна було передивитись проект і як він фрезерувався.

**Ранкова видача (найболючіший ручний етап).** Наступного дня відкривають вчорашню вкладку таблиці, дивляться імена клієнтів, у папці `export` (відсортованій по датах створення підпапок) шукають фізичні роботи цих клієнтів, звіряють. Коли всі роботи клієнта знайдені й видані — рядки в таблиці відмічають кольором.

Хто ще дивиться в таблицю: логісти, адміністратори, техніки — вони теж пишуть туди зауваження. Таблиця залишається спільною мовою для всіх, портал її не замінює, а синхронізується з нею в обидва боки.

## 3. Структура Google Таблиці

- Один файл Google Sheets, окрема вкладка на кожен день, назва вкладки = дата у форматі `22.07.26` (день.місяць.рік, двозначний рік).
- Заголовки колонок — приблизно рядок 4, дані починаються приблизно з рядка 7 (потребує перевірки наживо — перший крок реалізації читає й друкує сирі заголовки, щоб звірити це напевно, а не покладатись на фото).
- Колонки (за фото реальної таблиці):
  - № (порядковий)
  - Номер наряду (напр. `24122`)
  - Кількість (одиниць)
  - Колір роботи (напр. `пмма A2`, `моно А3.5`, `титан корея`, `500`, `800`)
  - Вид роботи (напр. `анатомія`, `анатомія гвинтова`, `абатмент`, `каркас міст 13-23`, `вкладка`)
  - Здати до — три підколонки `14:00` / `16:00` / `09:00`, потрібна колонка позначена `x`
  - Номер роботи — формат `2026-07-21_00016-007` (ідентифікатор роботи, використовується для зіставлення при синхронізації)
  - Ім'я техніка
  - Коментар для CAM-оператора (що саме фрезерувати)
  - ID (проекту Sum3D) — заповнює оператор після взяття в роботу
  - Прорахував / Відфрезерував — позначки + час
  - Дата останнього фрезерування
  - Який раз фрезерується (для повторних/переробок)
  - БРАК (переробки): хто винен — обладнання / технік / адміністратор / клієнт — і кількість одиниць переробки
  - Рядки підсвічуються кольором вручну (напр. помаранчевий = очікує щось, зелений = певний стан) — правила кольорів ще не формалізовані, уточнити з користувачем при потребі.

## 4. Структура папки export (для ранкової видачі)

```
export/<ім'я клієнта>/<нова папка (N)>/<матеріал-колір, напр. mono a3>/<файл>
```

- Проміжна папка — стандартна Windows-папка `нова папка (N)`, номер system ставить сам, сенсу не несе.
- Номер роботи в шляху не фігурує. Ручне зіставлення зараз спирається на дату й час створення папки.
- Імена клієнтів у таблиці й у назвах папок можуть незначно відрізнятись (одруківки, регістр, пробіли) → потрібне нечітке зіставлення (fuzzy matching, напр. `rapidfuzz`), з накопичувальним словником підтверджених пар "ім'я в таблиці" ↔ "назва папки", щоб з часом система запитувала підтвердження все рідше.
- Логіка зіставлення для видачі: клієнт + матеріал/колір + дата створення папки. Якщо збіг однозначний — прив'язати автоматично; якщо кілька кандидатів — показати всі з часом створення і дати оператору вибрати.

## 5. Статуси / життєвий цикл роботи

```
нове → прийнято → прораховано → у фрезеруванні → відфрезеровано
     → знайдено при видачі → видано
```

Окремі стани: проблема (вільний коментар), переробка (n-й раз, причина браку + винуватець — обладнання/технік/адміністратор/клієнт).

## 6. Технічний стек (рішення вже прийняте)

- Бекенд: Python + FastAPI
- База: SQLite + SQLAlchemy (досить із запасом для 20–40 робіт/день; міграція на Postgres тривіальна завдяки SQLAlchemy, якщо колись знадобиться)
- Фронтенд: Jinja2 + HTMX (без React/SPA — сервер рендерить HTML-фрагменти, HTMX підмінює їх без перезавантаження сторінки)
- Синхронізація з таблицею: `gspread` + сервісний акаунт Google
- Пошта: IMAP через `imap-tools` (сервер `imap.ukr.net`, порт 993, окремий "пароль для програм", не основний пароль)
- Основний деплой: standalone Windows 10/11 x64 на одному робочому ПК, локальний `127.0.0.1`, PyInstaller onedir + Inno Setup, автозапуск при вході користувача; Docker лишається додатковим варіантом для розробки/майбутнього сервера
- Окремий сервіс від CNC Tracker (свідоме рішення — різні цикли оновлень, ізоляція збоїв), спільним може бути хіба стиль інтерфейсу

## 7. Вимога: секрети через інтерфейс, не тільки .env

Замість того щоб тримати всі секрети в `.env`-файлі на диску, потрібен екран "Налаштування" (доступ лише для адміна), де вводяться:

- Google Sheet ID
- Google service-account JSON (вміст або шлях)
- IMAP логін і пароль для програм
- Шлях до папки `export`

Ці значення зберігаються зашифрованими в базі, а не текстом. У `.env` лишається тільки один секрет — ключ шифрування самої бази. Це і безпечніше (секрети не лежать читабельним текстом у файлі проекту), і зручніше для користувача (не треба лізти редагувати файли руками при зміні пароля).

## 8. Дизайн-напрям (стартовий, буде уточнюватись)

Дуже сучасний, анімований, технологічний вигляд, зовсім не типова admin-панель за замовчуванням. Стартовий референс — стиль лендінгу Hex.tech (знайдений через Mobbin):

- Темний, майже чорний фон з тонкою сіткою-«блюпринтом» на задньому плані (перегукується з темою точної механіки/фрезерування)
- Теплий кремово-рожевий (blush) акцент замість холодного неонового "кіберпанк"-техно кольору — м'якший, комфортніший для очей при цілоденному використанні
- Великий округлий жирний шрифт для заголовків, контраст з дрібним акуратним текстом решти інтерфейсу
- Заокруглені картки/панелі з тонкою межею
- Чіп-теги зі знімним хрестиком для фільтрів — добре лягає на показ матеріал+колір роботи (напр. `моно A3.5 ✕`) у черзі
- Моноширинний шрифт для технічних ідентифікаторів — номерів робіт, ID проекту Sum3D, часу
- Анімації — стримані й функціональні: нова робота елегантно з'являється в черзі, зміна статусу — коротка мікроанімація підтвердження; не декоративні ефекти заради ефектів

Важливо: користувач надсилатиме додаткові референси в процесі роботи — дизайн-система має бути легко доопрацьовуваною (CSS custom properties / дизайн-токени в одному місці), а не захардкодженою по всіх шаблонах.

## 9. Екрани

0. Вхід в систему — логін/пароль, окремий обліковий запис на кожного оператора (див. п.10 `User`); без валідного входу — нічого не видно
1. Черга робіт (основний, 90% часу) — усі незавершені роботи обох джерел незалежно від дня (не тільки "сьогодні"), таби/фільтр по періоду: Сьогодні / Вчора / Завтра / Раніше (архів, включно з виданими); сортування за терміновістю, прострочені (день вкладки вже минув, а робота не готова) підсвічені; фільтри-чіпи, лічильники дедлайнів; дії: взяти в роботу / копіювати шлях-ID для Sum3D (кнопка, рівень 1 — просто clipboard; рівень 2, пізніше — кастомний `sum3d-open://` протокол-хендлер для автозапуску Sum3D з проєктом, якщо Sum3D підтримує відкриття через CLI-аргумент, це ще не перевірено) / скачати файли (zip) / змінити статус
2. Нові з пошти (тріаж) — розпізнані поля з листа на перевірку/виправлення перед прийняттям у чергу
3. Картка роботи — повний паспорт, хронологія статусів (з іменем оператора, хто саме що зробив), коментарі (двосторонні з таблицею), файли, історія переробок
4. Ранкова видача — роботи вчорашнього (і старіших невиданих) дня, згруповані по клієнтах, зі зіставленими шляхами з `export`; відмітка "знайдено" по кожній роботі, автозакриття клієнта, коли зібрано все
5. Статистика — одиниці/роботи по періодах, брак по причинах і винуватцях, середній час "нове → відфрезеровано"
6. Налаштування (адмін) — секрети, див. пункт 7; керування обліковими записами операторів

## 10. Модель даних (орієнтовна)

`User` (логін, хеш пароля, ім'я, роль — адмін/оператор) · `Order` (джерело, номер наряду, номер роботи, клієнт, матеріал, колір, одиниці, вид, дедлайн, sum3d_id, статус) · `StatusEvent` (хто/коли/що — `actor` прив'язаний до конкретного `User`, не просто текст) · `Comment` (двосторонній, sheet↔portal) · `EmailMessage` + `Attachment` · `ReworkRecord` (причина, винуватець, який раз) · `ClientNameAlias` (навчений словник зіставлення імен таблиця↔export) · `SyncLog`.

Важливо: в історії роботи (картка роботи, п.9.3) завжди має бути видно, який саме оператор що зробив — це вимога, не деталь реалізації.

## 11. Дорожня карта

- v0.1 — тільки читання: підключення до тестової таблиці, знаходження сьогоднішньої вкладки за датою, друк черги в консоль. Нічого не пише, нічого не може зламати. (готово)
- v0.2 — читання пошти (IMAP), екран "Нові з пошти", ручне прийняття в чергу
- v0.3 — запис у таблицю: статуси, ID Sum3D, коментарі (двосторонньо). Реалізовано точковий запис без перезапису рядка, імпорт коментарів у історію та захист портал-специфічних статусів; лишилась перевірка повного сценарію наживо на тестовій таблиці
- v0.3.5 — автентифікація: сторінка входу, облікові записи операторів (`User`), прив'язка дій (взяв у роботу / змінив статус) до конкретного оператора в історії
- v0.4 — сканер `export`, нечітке зіставлення клієнтів, екран "Ранкова видача"
- v1.0 — production target змінено на standalone Windows; реалізуються Windows runtime/installer, first-run setup, локальні міграції й автозапуск. Docker лишається необов’язковим додатковим способом
- пізніше, за потреби — Tauri-обгортка того самого веб-застосунку для системних плюшок (трей-іконка, нотифікації, sum3d-протокол-хендлер); React/SPA свідомо не розглядається, HTMX вистачає

## 12. Тестове середовище (вже готове)

- Тестова копія Google Таблиці: ID `1IIEkBnPoDcxgo3-41IdbJu6FZXNawYX9UNdoekFDPbs`
- Сервісний акаунт Google створено, доступ "Читач" до тестової таблиці наданий
- Фантомна скринька на ukr.net створена, IMAP увімкнено, пароль для програм згенеровано
- Тестова структура папки `export` підготовлена

Секрети (JSON-ключ, IMAP-пароль) в цей файл і в git не потрапляють — вводяться локально через `.env` на етапі v0.1–v0.2, пізніше через екран налаштувань (п. 7).

## 13. Що ще НЕ вирішено / уточнити по ходу

- ~~Точний рядок заголовків і початку даних у вкладці~~ — вирішено в v0.1: заголовки рядки 2-6, дані з рядка 7
- ~~Формальні правила кольорів рядків~~ — вирішено: колір/підсвітка в порталі задається логікою застосунку (не ручним копіюванням паперової практики). "Проблема" і "переробка" мають власні бейджі (коралово-червоний і теплий оранж відповідно), прострочені роботи (день вкладки минув, робота не готова) підсвічуються теплим червонуватим відтінком рядка
- Остаточна дизайн-система — це стартовий напрям, буде ітеративно уточнюватись новими референсами від користувача
- Чи Sum3D підтримує відкриття проєкту через аргумент командного рядка (`Sum3D.exe "шлях"`) — потрібно для sum3d-протокол-хендлера (п.9, екран 1, рівень 2) — на паузі
- ~~Ролі операторів для v0.3.5~~ — вирішено (наразі): застосунок орієнтований лише на операторів, без розділення технік/cam-оператор; лишається тільки поділ адмін/оператор, що вже реалізовано

## 14. Поточний стан (01.08.26)

- Локальна реалізація зведена в цілісний незакомічений шар; 217 тестів проходять, шаблони/OpenAPI/compile/diff-check валідні.
- v0.3: точковий write-back Sum3D ID, статусних маркерів і коментарів; живі ручні значення таблиці не перезаписуються, portal-only статуси захищені від downgrade. Live write-тест ще не виконано, бо тестовий service account має доступ «Читач».
- Пошта: IMAP автоматично перевіряється кожні 2 хвилини й вручну з тріажу; читаються всі листи за останні 30 днів незалежно від прапорця «прочитано», mailbox flags не змінюються, є timeout/lock/UID-дедуплікація та очищення нових файлів при rollback. Станом на 01.08.26 у локальному тріажі 6 листів. MIME filenames і export paths захищені від traversal/overwrite; partial move вкладень відкочується, невдалий export не приховує лист із тріажу.
- Черга: необроблені листи закріплені окремим teal-блоком над Google-чергою незалежно від фільтрів; показуються відправник, тема, розпізнані поля, час і кількість вкладень. Є дії «Обробити» та «Відкрити файли»; остання через authenticated localhost-only POST відкриває Windows Explorer лише для строго перевіреного шляху всередині mail spool/export (traversal, symlink/junction і зовнішні шляхи блокуються). Після прийняття email-роботи датуються за київською бізнес-датою та мають окремий teal-колір/бейдж і фільтр джерела. Додано дедлайн/сортування за терміновістю, готовність визначається фактично знайденою папкою; path-ID копіюється в clipboard замість ненадійного `file://` переходу.
- Швидкодія: GET-сторінки з синхронним SQL винесені з event loop у threadpool, N+1 на статистиці усунуто через `selectinload`, SQLite працює в WAL з `busy_timeout`, запити довші секунди логуються.
- Windows: основний target — один робочий Windows 10/11 x64 ПК, bind лише `127.0.0.1`, дані в `%LOCALAPPDATA%\KuubMill`, DPAPI master key, PyInstaller onedir + Inno Setup, first-run admin без стандартного пароля, локально vendored HTMX, graceful shutdown. Реальний onedir та `dist-installer/KuubMill-Setup-0.1.0.exe` зібрані; інсталятор перед upgrade автоматично викликає старий `KuubMill.exe --shutdown`, тому оновлення не показує діалог locked files. Пройдено smoke-test install/upgrade/shutdown/uninstall зі збереженням даних. Див. `WINDOWS_INSTALL.md`.
- Google Sheets у Windows UI: додано onboarding після створення адміна, зашифроване збереження конфігурації, ручну кнопку та дію «Зберегти й синхронізувати», безпечні повідомлення результату/помилки й атомарний імпорт датованих вкладок. На встановленому екземплярі тестову таблицю підключено; повторний імпорт ідемпотентний за `sheet_tab + row_number`, дублів немає; візуально перевірено desktop/mobile.
- Поштова черга: прийняті листи створюють `Order(source="email")` і відображаються на головній разом із табличними роботами; є незалежний фільтр джерела, бірюзовий фон, SVG-бейдж «Пошта» та ім’я клієнта. Після прийняття листа відкривається відфільтрована поштова черга.
- Docker: лишається додатковим варіантом; non-root UID 10001, healthcheck, persistent DB, fail-closed Alembic baseline/legacy guard.
- Найближчі кроки: налаштувати IMAP і робочі шляхи `export`/файлів техніків на цьому Windows-ПК; реалізувати recovery секретів/backup для перенесення ПК; надати service account роль «Редактор» тестової таблиці й провести live write smoke-test; після smoke-test розбити великий diff на змістовні коміти.
