> ⛔ ВІДКИНУТО 23.09.26 (власник, на dev): «забагато інформації на одній
> сторінці, потрібно повертати назад». Реалізацію повністю відкочено. Вкладки
> лишаються. Наступна спроба покращити accept-потік мусить ТРИМАТИ вкладки/
> компактність і лише прибрати подвійне введення полів, а не згортати все в
> довгу сторінку. Файл лишено як історію напряму, що не спрацював.

# Бриф: карта листа в 3 кроки (лист → файли → прийняти)

Вихід дизайн-чату 23.09.26 (Рома). Блок 2 роботи з поштою; блок 1 (ім'я
відправника + груповий Sum3D) — `MAIL_UX_BRIEF.md`, окремий коміт. Читати
разом із `CLAUDE.md` §14 «Пошта», не замість. Тест — локальний dev :8002.

## Задача (слова власника)

«Першим пунктом ознайомитися з листом. Другим — перевірити, чи файли
скачались; якщо ні — скачати й правильно розмістити. Останнім — прийняти в
роботу, щоб автоматично додалося в чергу.»

Бекенд усе це **вже вміє**; проблема в тому, як карта це показує. Рішення
власника: **лишити логіку як є, модифікувати карту.** Перенос файлів спул →
export лишається **при прийнятті** (на ньому тримається відкат листа).

## Що є зараз — 6 стопів замість 3

`_mail_detail_panel.html` (364 р.) — `.mail-seg`, чотири рівноправні вкладки
([рядки 30–40](app/templates/_mail_detail_panel.html:30)):

| Вкладка | pane | Що там |
|---|---|---|
| Лист | :46 | текст, метабар, кнопки «Скачати вкладення →» (:71/:77 → стрибок у Файли), навчання фільтра |
| Файли | :121 | `files-summary` ok/warn (:125), скачати (:176) / наново (:198) / розпакувати (:210), рядки посилань `_mail_link_row.html` (:250) |
| Прийняти | :293 | `#mail-wizard` → `_mail_wizard.html` |
| Статус | :299 | історія/стан |

Липка смуга внизу (:360) — «Прийняти →» лише **перемикає вкладку**
(`data-seg-go`), сам submit — у кроці 3 візарда.

`_mail_wizard.html` (211 р.) — ще 3 кроки всередині вкладки «Прийняти»:
- крок 1 (:26) — клієнт / матеріал / вид / к-сть (дублює розпізнані поля з
  вкладки «Лист»);
- крок 2 (:91) — тека: `folder_pick` (:119), `folder_new` (:131),
  `material_folder` (:140), прев'ю шляху (:103–113); кожен контрол
  `hx-post /mail/{id}/wizard` → перемальовує весь візард;
- крок 3 (:158) — підсумок + приховані поля (:168–170) + помилка (:161) +
  **`.btn.apex`** (:205) → `hx-post /mail/{id}/accept`.

Роути: `mail_wizard` [mail.py:713](app/routers/mail.py:713) →
`_wizard_context` (:749); `accept_email` (:1029), `_accept_failed` (:1069)
повертає крок 3 з помилкою у `#mail-wizard`; контекст карти —
`_mail_panel_context` (:428): `att_count`, `undownloaded_links`,
`missing_count`. Готовність — `triage_status.triage_readiness`.

`mail.js`: пам'ять активної вкладки `mailSeg` (:68–107), живий міні-підсумок
у липкій смузі з полів візарда (:110–128), подія `mailFilesChanged` (:181)
оновлює лічильники, гарячі клавіші (:231).

## Цільовий вигляд — 3 вертикальні кроки в одній карті

Вкладки й візард зникають. Карта = три блоки `.mstep` поспіль, кожен із
заголовком (номер · назва · **статус-чіп**) і тілом. Усе видно одразу,
скролиться як один документ.

**Крок 1 · Лист.** Від кого (показне ім'я + адреса — блок 1), текст листа,
навчання фільтра. **Розпізнані поля редагуються ТУТ** (клієнт / матеріал /
вид / к-сть — це колишній крок 1 візарда; підказки матеріалу
`material_cands` і пам'ять відправника `sender_hint` лишаються).
Чіп: `прочитано` / `не розпізнано: клієнт, матеріал`.

**Крок 2 · Файли.** Вкладення з перевіркою «на диску», посилання, архіви,
кнопки скачати / наново / розпакувати — як зараз. Плюс **«куди покласти»**
(колишній крок 2 візарда): тека клієнта (наявна / нова), тека матеріалу,
прев'ю `export / <клієнт> / <дата> / <матеріал>`.
Чіп: `✓ 5 на диску` · `⬇ не скачано` · `⚠ 2 з 5` · `🔗 1 за посиланням`.

**Крок 3 · Прийняти в чергу.** Лише підсумок: N файлів → така тека, робота
така (клієнт · матеріал · к-сть). Помилка прийняття — тут, під підсумком.
**Замкнений**, поки крок 2 не зелений (файли за посиланням не скачані —
серверний гейт уже є, тепер він видимий: кнопка неактивна + текст чому).
Після прийняття — робота **сама в черзі** (уже так), там шпилька → груповий
Sum3D (блок 1). Кілька кольорів — часткове прийняття як зараз (`HX-Redirect`
на `/mail?open=id`, карта відкривається знову з рештою файлів у кроці 2).

**Липка смуга** лишається — саме в ній живе **єдина `.btn.apex`** «Прийняти
в чергу» (справжній submit, не перемикач вкладки) + міні-підсумок. У тілі
кроку 3 другої залитої кнопки НЕМАЄ (§14: одна залита кнопка на екран).

**«Статус»** — не крок; згорнутий `<details>` «Історія листа» під кроком 3.

## Як зшити (пропозиція, не догма)

- **Одна форма** `#mail-accept-form` обіймає поля кроків 1–2 і submit у
  смузі → `hx-post /mail/{id}/accept` з **тими самими іменами полів**
  (`client_name, material_color, kind, quantity, folder_pick, folder_new,
  material_folder, attachment_ids, accept_anyway`). Роут `accept_email` і
  `accept_letter` **не змінюються**.
- Контроли теки перемальовують **лише прев'ю шляху**, а не всю карту:
  `/mail/{id}/wizard` лишається (шлях у знімку роутів той самий), але з
  `hx-target` на блок прев'ю; `_wizard_context` віддає той самий `preview`.
  Або новий партіал `_mail_path_preview.html`, включений і в карту, і у
  відповідь роуту.
- `_accept_failed` рендерить **крок 3 карти** з помилкою (той самий
  контекст, що й карта), а не `_mail_wizard.html`. Значення полів не
  губляться — вони в тій самій формі на сторінці.
- `files_ready` для замка кроку 3 — рахувати в `_mail_panel_context` з уже
  наявних `att_count / missing_count / undownloaded_links /
  attachments_status`, **не дублюючи** `triage_readiness`.
- `mail.js`: прибрати перемикання вкладок; `data-seg-go` → плавний скрол до
  кроку; живий міні-підсумок і `mailFilesChanged` лишаються (тепер читають
  поля з кроку 1). `node --check` + `test_frontend_assets`.
- Зняте: `_mail_wizard.html` стає непотрібним — прибрати разом із CSS
  `.wiz-*`/`.wp-*` (grep перед видаленням: хто ще посилається).

## Інваріанти (§14 «Пошта») — не зламати

- Спул → export **лише при прийнятті**; відкат (`restore_email` /
  `_unaccept_email`) повертає файли у спул — не чіпати.
- Гейт «не можна прийняти з нескачаними посиланнями» — на сервері, UI лише
  показує.
- `.btn.apex` — одна на екран, лише прийняття.
- Водяний знак `since` у списку; `get_mail` і `_mail_triage_list.html`
  правляться разом (список цим блоком не чіпаємо).
- Жодних анімацій-петель у тріажі.
- Зомбі-фетч (`zombie_fetch_blocks_files`) блокує прийняття — лишається.

## Перевірка (§15, змагальна)

1. Справжній лист у фантомну скриньку (§12): відкрити → крок 1 показує
   ім'я/поля → крок 2 «не скачано» → скачати → чіп зелений → крок 3
   відімкнувся → прийняти → файл **фізично** в `export/<клієнт>/<дата>/<мат>/`,
   робота в черзі, рядок-нотатка в тестовій таблиці.
2. Лист із файлом **за посиланням**: крок 3 замкнений з поясненням, доки не
   скачано.
3. **Два кольори** в одному листі: перше прийняття часткове → карта знову
   відкрита, у кроці 2 решта файлів, чіп каже скільки лишилось.
4. **Помилка прийняття** (напр., тека недоступна): лишаємось на карті, поля
   цілі, помилка під кроком 3 — не редірект зі стертими полями.
5. **Відкат** прийнятого з Архіву: файли повернулись у спул, лист знову «нове».
6. Сторожі: `route_inventory` (без нових роутів; `/wizard` лишається),
   `frontend_assets`, `test_mail_wizard_errors.py` (перечитати — він тестує
   крок 3 візарда, адаптувати на крок 3 карти, **не видаляти** сценарії).

## Поза межами

- Список тріажу, `from_name`, груповий Sum3D — блок 1, уже зроблено.
- Авто-прийняття довірених відправників — лишається як є.
- Реорг `routers/mail.py` (1738 р.) — окремо.
- Розміщення файлів у export ДО прийняття — власник відхилив.
