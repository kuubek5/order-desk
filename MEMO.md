# Памʼятка Ромі — KuubMill і робота з Claude

Одна сторінка. Оновлюй, коли щось змінюється.

## 1. Як вести чати (простими словами)

### Головне, що треба зрозуміти

У Клода є **стіл**. На стіл кладеться все: твої повідомлення, його відповіді, кожен файл, який він відкрив, кожна команда й помилка. Стіл **не безмежний**.

Коли стіл заповнюється — Клод змітає старе в коробку і працює далі з коротким переказом. Деталі губляться. Саме тоді він починає «тупити»: перепитує те, що ти вже казав, забуває домовленості.

**Новий чат = чистий стіл.** Тому нові чати — це добре, а не «я щось роблю не так».

Але: щоразу, коли ти відкриваєш новий чат, на стіл **одразу кладеться** `CLAUDE.md` + памʼять. Раніше вони займали чверть столу ще до першого слова. Ми їх постригли вдвічі — тепер місця більше.

### Правило №1: один чат — одна справа

Не тягни в одному чаті все підряд. Як зошит: окремий розділ на окремий предмет.

**Погано:** обговорили архів → зробили копії таблиці → поправили дизайн → зробили аудит → ще й код обговорили. (Це був наш чат. Стіл змітався кілька разів.)

**Добре:**
- Чат 1 — «обговорити, що робити з архівом» → у кінці Клод пише короткий підсумок
- Чат 2 — «зроби ось це» (даєш підсумок з чату 1)
- Чат 3 — «поправ дизайн ось тут»

Закінчив справу → скажи **«закоміть і запиши в памʼять»** → закривай чат.

### Правило №2: останній рядок — це команда

Диктуй голосом як зручно, думай уголос скільки хочеш. Але **останнім рядком** напиши, чого ти хочеш. Інакше Клод гадає і витрачає хід на перепитування.

Найкорисніші фрази:

| Пишеш | Що робить Клод |
|---|---|
| `Тільки поясни, без коду` | відповідає словами, нічого не чіпає |
| `Тільки план, без коду` | розписує кроки, чекає твого «так» |
| `Зроби` | будує |
| `Спершу покажи, що зміниш` | показує перелік, потім робить |
| `Закоміть` / `Не комітити` | явно про збереження в git |

`Тільки поясни, без коду` — найощадніша фраза. Коли ти ще думаєш — завжди її.

### Правило №3: не змушуй його читати зайве

Клод читає файли — і кожен лягає на стіл. Якщо сказати «розберись у проєкті» — він прочитає пів проєкту.

**Погано:** «подивись, як там усе працює з поштою»
**Добре:** «у якому файлі приймається лист із пошти?»

Що конкретніше питання — то менше він відкриє.

### Правило №4: два чати одночасно — тільки в різних папках

Якщо два чати правлять **той самий проєкт одночасно** — вони наступають один одному на руки. У нас так і сталося: другий чат переробляв Архів, і в мене посипались тести, яких я не ламав. Пів години на розбір.

Треба паралельно — попроси Клода:

> зроби окрему копію проєкту для цієї задачі

(він виконає `git worktree add ../CRM_Laba-<назва>`)

Або просто **не роби двох справ одночасно**. Це простіше.

### Правило №5: скріншот — твій, не його

У Клода є свій браузер, але він часто прихований, і тоді Клод чекає по 30 секунд намарно. **Якщо треба показати, як щось виглядає — кинь свій скріншот.** Швидше й точніше.

### Правило №6: модель під задачу

- **Fable / Sonnet** — щоденна робота, дизайн, CSS, дрібні правки. Дешевше й швидше.
- **Opus** — складні баги (щось «іноді» ламається), шифрування, міграції бази, планування великої фічі.

Перемикається через `/model`.

### Правило №7: CLAUDE.md — це інструкція, а не щоденник

`CLAUDE.md` вантажиться в **кожен** чат. Тому туди — лише правила й граблі, на які можна наступити знову.

«Що ми зробили в серпні» → `HISTORY.md` (ніхто не вантажить, читається за потреби).

Якщо Клод пропонує дописати в CLAUDE.md опис готової фічі — скажи: **«це в HISTORY.md»**.

### Коли пора закривати чат

Будь-яке з цього:
- справа закінчена й закомічена
- Клод почав перепитувати те, що ти вже казав
- переходиш на іншу тему
- чат іде довше ніж пів дня

Перед закриттям: **«закоміть і запиши handoff у памʼять»**.

### Дизайн-правки

Перед серією правок вигляду: `/impeccable hooks off`
Наприкінці один раз: `/impeccable audit`

(Інакше після кожної правки Клоду прилітає простирадло перевірок — і воно теж на столі.)

## 2. Дві справи одночасно — гілки й копії проєкту

### У чому проблема

Проєкт — це **одна папка**. Клод у чаті відкриває й править файли прямо в ній.

Два чати одночасно = двоє людей правлять ті самі папери на одному столі. Один щось поміняв — у другого «раптом» посипались тести, які він не ламав. У нас так і було: другий чат переробляв Архів, а я пів години шукав, чому падають чужі тести.

### Що таке «гілка» (branch)

Гілка — як **чернетковий зошит**.

Головний зошит — `master`, там усе робоче й перевірене. Береш чернетку, пишеш у ній що хочеш — головний зошит не змінюється. Готово → переписуєш у головний (це називається **мерж**).

Якщо чернетка не вдалася — просто викидаєш, головний цілий.

### Що таке «копія папки» (worktree)

Гілка сама по собі не рятує від двох чатів: гілки перемикаються **в тій самій папці**, тобто стіл усе одно один.

`worktree` — це **друга папка** з тим самим проєктом, але на своїй гілці. Два столи. Кожен чат порається на своєму.

```
P:\AI-Projects\CRM_Laba            ← головна папка, гілка master
P:\AI-Projects\CRM_Laba-archive    ← копія, гілка feat/archive
```

Це не копіювання руками — git робить це сам, і потім легко зливає назад.

### Як це робити (покроково)

**Крок 1. Створити копію.** У першому чаті просто скажи:

> зроби окрему копію проєкту для роботи над архівом

Клод виконає:
```bash
git worktree add ../CRM_Laba-archive -b feat/archive
```

**Крок 2. Відкрити другий чат на цій копії.** У новому чаті першим повідомленням:

> працюй у папці P:\AI-Projects\CRM_Laba-archive

**Крок 3. Працювати.** Два чати не бачать правок один одного — і це саме те, що треба.

**Крок 4. Закінчив у другому чаті** → «закоміть».

**Крок 5. Забрати роботу в головний проєкт.** У першому чаті:

> злий гілку feat/archive у master

**Крок 6. Прибрати копію** (коли вже не потрібна):

> прибери копію проєкту CRM_Laba-archive

### Що памʼятати

- **Дев-сервер (8002) один на всіх.** Якщо обидва чати його перезапускають — плутанина «чому мої зміни не видно». Домовляйся сам із собою: дев тримає **один** чат, другий перевіряє тестами.
- **Не запускай два релізи одночасно.**
- **Забув про копію і працював двома чатами в одній папці?** Не смертельно, але тести можуть падати «нізвідки». Тоді просто закрий один чат і скажи другому: «перевір, що з тестів моє, а що чуже».
- Якщо не впевнений — **не роби двох справ одночасно**. Це найпростіше рішення, і воно завжди правильне.

## 3. Що вимагати від Клода (щоб потім не переробляти)

Клод старається, але може сказати «готово», маючи на увазі «має працювати». Різниця велика. Кілька фраз, які економлять тобі години:

| Пишеш | Навіщо |
|---|---|
| `Ти це перевірив чи припускаєш?` | відсіює «має працювати» |
| `Перевір наживо, не тільки тести` | тести можуть проходити, а кнопка не натискатись |
| `Покажи доказ` | скріншот, вивід команди, а не слова |
| `Це на 8000 чи 8002?` | щоб не шукати фічу не там |
| `Спершу поміряй, потім прав` | на скарги «повільно» — інакше правитиме навмання |
| `Звір з кодом, не з памʼяті` | памʼять могла застаріти |
| `Спершу покажи, що зміниш` | перед великою правкою |

**Окремо про «не бачу фічу»:** спершу глянь **версію** внизу меню й **порт**. Дев (8002) отримує нове раніше за прод (8000). Половина «багів» — це просто не той порт.

## 4. Де що лежить

| Що | Де |
|---|---|
| Правила для Claude, пастки | `CLAUDE.md` §14 (пастки), §15 (процес) |
| Журнал змін «що і чому» | `HISTORY.md` |
| Прод / Dev | `127.0.0.1:8000` (встановлений `KuubMill.exe`) / `127.0.0.1:8002` (`.\dev_restart.ps1`) |
| База проду | `%LOCALAPPDATA%\KuubMill\kuubmill.db` |
| Місячні копії БД | `…\KuubMill\backups\monthly\orderdesk-YYYY-MM.db` (авто) |
| Переносний бекап (на інший ПК) | Налаштування → Резервна копія (з паролем) |
| **Копії Google-таблиці** | Налаштування → **Копії Google-таблиці**; файли `…\KuubMill\backups\sheets\*.csv` |
| Реліз на прод | `/kuubmill-release` у чаті Claude |

## 5. Копії Google-таблиці — як це працює

- Раз на 6 год (можна змінити) застосунок зберігає CSV кожної денної вкладки.
- **Порожні вкладки не зберігає** — лише з роботами. Свіжі 4 дні перезнімає, поки наповнюються.
- Обрізане читання (проксі віддав менше рядків) **не затирає** повну копію.
- Вкладку прибрали з Google → копія лишається, зʼявляється бейдж «зникла з Google».
- Кнопка «Зняти копію вкладок» — знімок зараз; перший раз тягне всю історію (може хвилину подумати — квота Google).
- Файли STL сюди **не** входять — вони на мережевому сховищі.

## 6. Аварія з Google-таблицею — що робити

1. Налаштування → Копії Google-таблиці → скачай день, місяць (ZIP) або всю таблицю (ZIP). Файли названі як вкладки: `22.07.26.csv`.
2. У Google Sheets: **Файл → Імпорт → Завантажити** → обери CSV.
3. У вікні імпорту: **«Вставити нові аркуші»** — кожен файл стане вкладкою під своєю датою.
4. Звір із живою таблицею, прибери зайве. Дані CRM не чіпаються — це відновлення саме Google.

Формули й ручні кольори заливок не переносяться — лише значення клітинок.

## 7. Реліз (порядок)

1. Гілка фічі → мерж у `master` (`git merge feat/<фіча>`).
2. У чаті: `/kuubmill-release` — bump версії, тести, тег, CI, перевірка.
3. Прод сам підтягне оновлення (авто-апдейт). Перший реліз після перейменування — інсталятор руками, один раз.

## 8. Якщо щось «зникло»

- **«Не бачу фічу»** → спершу звір версію (`v0.8.xx` внизу рейки) і порт. Фіча є на dev (8002) раніше, ніж на проді (8000).
- **Лист «зник» після рестарту dev** → інша БД (dev = папка проєкту), не втрата. Перечитай IMAP.
- **500 скрізь після переїзду теки** → змінився ключ шифрування; `/license` дає ввести заново.
- **Синк повільний** → спершу лог і профіль (`/diag/perf`), потім правки. Не релізити навмання.

## 9. Аудит 05.09.26 — що ми ним зробили

Повний аудит (UX усіх екранів, порівняння з ринком, код-рев'ю) — `design/audit_2026-09-05/AUDIT.md`.
Живий трекер із галочками й журналом — `design/audit_2026-09-05/PLAN.md`.

### Найкоротше

Аудит знайшов **48 пунктів**. З них зробили **44**, чотири зняли свідомо (нижче — які й чому).
Це був один робочий блок 06.09.26. Тестів у проєкті стало **1950**, усі зелені.

Уяви, що CRM — це майстерня. Аудит пройшовся по ній із ліхтариком і склав список: тут
дошка тріщить, тут два вимикачі роблять одне й те саме, тут інструмент лежить не там.
Ми пройшли список зверху вниз.

### Що це дало для щоденної роботи

**Дані перестали губитись.**
Раніше, якщо база спотикалась посеред прийняття листа, файли вже переїхали в `export`,
а роботи в CRM не було — файли ставали сиротами, і ніхто про це не знав. Тепер таке
прийняття **відкочується саме**: файли повертаються в спул, оператор бачить зрозумілу
помилку, а не тишу. Перевірено не на словах: ми навмисно зламали код рівно в тому місці
й подивились, що станеться.

**Портал перестав завмирати.**
Раніше «Видати 8 з 8» могло заморозити застосунок на дві хвилини — він ходив у Google
по одній роботі за раз, прямо в момент кліку. Тепер уся видача клієнта — одне завдання
у фоні. Те саме зі статусами, Sum3D і «Скасувати».

**Ми більше не пишемо в чужий рядок таблиці.**
Якщо технік видалив рядок вище, наш збережений номер рядка починав показувати на сусідню
живу роботу. Тепер перед КОЖНИМ записом позиція звіряється, і якщо не збіглась — запис
пропускається. Неперевірений рядок гірший за пропущений запис. Це теж перевірили живцем:
підмінили номер рядка на чужий, і CRM сама виявила підміну й записала куди треба.

**Синк перестав ховати робочі дні.**
Якщо Google віддавав неповний список вкладок, CRM могла вирішити, що день зник, і
відправити його в архів. Тепер є запобіжник: масове зникнення = сигнал про збій, а не
команда архівувати.

**Вхід і коди стали нормальними.**
Пауза після 5 невдалих спроб, пароль від 10 символів, технічна сторінка `/docs` закрита.

**Робочий день замість календарного.**
О другій ночі файли лягали в теку одного дня, а рядок у таблиці — в інший. Тепер скрізь
одна доба, що починається о 07:30.

### Що змінилось на екранах

Тут те саме — «було → стало».

**Подвійні кліки.** Було: натиснув, нічого не сталося, натиснув ще раз — і дія пішла двічі.
Стало: будь-яка кнопка, що чекає відповіді, тьмяніє й другого кліку не приймає.

**STL.** Було: відкривався маленьким, кожного разу треба розгортати руками.
Стало: відразу на весь екран, і памʼятає твій вибір.

**Видача — дрібне тертя.** Було: згорнув картку — після першої ж галочки вона розгорталась
назад; клієнта шукав очима по всьому списку; «Прив'язати папку» викидало на список усіх днів.
Стало: згорнуте лишається згорнутим, є пошук клієнта, повернення в той самий день.

**Видача — галочки.** Було: зібрав лоток клієнта на 30 робіт — ставиш 30 галочок поспіль.
Стало: кнопка **«Усі знайдено»** одним кліком. «Видати N з M» лишилась **окремою** кнопкою —
і це навмисно: «я тримаю коронку» і «я віддав її логісту» різні рішення, а часткова видача
у вас норма (з трьох пічок роботи виходять у різний час).

**Видача — звірка.** Було: нічого не було.
Стало: є **чеклист із трьох звірок** перед «знайдено» — і він **вимкнений**. Вмикається в
Налаштуваннях → «Видача», якщо колись вирішиш, що повернення того варті. Поки увімкнений,
кнопка «Усі знайдено» ховається: вона просто обійшла б звірку.

**Стан синку.** Було: «таблиця не оновлюється» видно лише на Черзі — на видачі чи в пошті
ти цього не бачив узагалі.
Стало: кружок у рейці на **кожному** екрані, а клік по ньому відкриває новий екран
**«Журнал синку»** з усією історією.

**Печі й верстати.** Було: видно лише «зараз» — поточна температура, поточний стан.
Стало: у картці розкривається **доба історії**: у печей крива температури (видно, коли
вийшла на полицю й коли пішла на охолодження), у верстатів стрічка «фрезерував / стояв».
Пропуски в даних заштриховані з підписом, а не замальовані прямою лінією — краще чесна
дірка, ніж намальована температура, якої не було на табло.

**Дрібне.** Статус роботи ставиться прямо з рядка черги (меню «⋯»), архівні роботи в пошуку —
лише перегляд, архів памʼятає місяць і день в адресі, а «Встановити оновлення» перепитує
й каже, скільки операторів зараз працює.

### Що всередині (тобі не видно, але важливо)

Чотири величезні функції розібрані на частини: найбільша була 456 рядків в одному місці.
Екран налаштувань був одним файлом на 2280 рядків — став пакетом із тематичних модулів.
Чотири різні перевірки «це адмін» стали однією, і тепер тест не дасть новому роуту
зʼявитись без гейта. Це не «краса заради краси»: саме в таких купах і сиділи дві
найважчі помилки, які ми полагодили.

### Що НЕ робимо — і чому (твої рішення 06.09.26)

- **Не зливаємо ПІН «Виробітку» і блокування розділів.** Це різні замки для різного.
- **Не зливаємо Статистику і Виробіток.** Вони рахують по-різному навмисно: Виробіток —
  це зарплата, у нього своя логіка. Об'єднання ризикує зламати гроші.
- **Не робимо планшетну верстку видачі.** Планшета в цеху немає. Верстатка «на майбутнє» —
  це код, який ніхто не відкриє, але який ламатиметься при кожній правці екрана видачі,
  і ніхто не помітить.
- **Не повертаємо «читання чанками»** — той відкат був правильний, причина була в сірих
  СЛМ-рядках, а не в проксі.

### Що лишилось

- Два останні пункти плану (палітра Ctrl+K і збережені вигляди черги) — в роботі.
- **Реліз.** Прод (`127.0.0.1:8000`) досі на **0.9.0**, а це лише фази 0 і 1. Усе інше,
  описане вище, лежить у `master` і на робочий ПК ще не поїхало. Скажеш — женемо реліз.

## Меню налаштувань — що змінилось 06.09.26

- Меню тепер описане **одним файлом** (`app/services/settings_nav.py`). Хочеш новий розділ або переставити пункт — правиться один рядок, а не сім шаблонів.
- Групи: **Моє робоче місце · Джерела робіт · Обладнання · Люди й доступ · Обслуговування**. Ти бачиш 21 пункт, оператор — 14.
- У меню нарешті потрапили **Швидкодія** і **Ліцензія** — раніше до них не було як дійти.
- Пункт зі **стрілкою ↗** = відкриється окремою сторінкою (перезавантаження). Без стрілки — перемкнеться на місці.
- **Оператор тепер редагує «Джерела робіт» і «Обладнання»** — так, як ти сказав, включно з паролями пошти й Google та пристроями. Одне лишив адмінським: кнопка перевірки шляху, яка ПИШЕ файл-маркер у теку (нею можна було б писати в будь-яку мережеву шару). Скажеш — відкрию і це, це один рядок.
- Перевірити «нічого не зникло» тепер можна командою, не очима:
  `python scripts/settings_baseline.py --out C:	mpfter` і `python scripts/settings_baseline_diff.py design/settings-redesign C:	mpfter`.
- **Глянь очима на 8002**: відступи, стрілка ↗ у темі forge, чи не тісно в групі «Джерела робіт» (там 7 пунктів). Пейн браузера в тій сесії був прихований, скріншотів немає.


## 10. Аудит 06.09.26 (другий) — що зроблено і що далі

Звіт: `design/audit_2026-09-06/AUDIT.md`, трекер `design/audit_2026-09-06/PLAN.md` (читати першим
у наступному чаті по цій темі). Усі виправлення — у гілці **`audit/2026-09-06`** в окремій копії
проєкту `P:\AI-Projects\CRM_Laba-audit` (бо в головному дереві в цей час працював інший чат).

**Що перевірити руками — dev 8002 ЗАРАЗ показує код гілки** (сервер запущено з копії проєкту;
`.\dev_restart.ps1` з головного дерева поверне на 8002 старий master):
1. Черга → «Раніше»: під таблицею кнопка «Показати ще 200»; лічильник «616 у вигляді» — по всіх.
2. Налаштування → «Робочий процес → Ранкова видача»: перемикач QC-чеклиста; увімкни → на видачі
   перед «знайдено» три галочки; вимкни → один клік.
3. Налаштування → «Про застосунок»: 8 релізів і кнопка «Показати всі N релізів».
4. Рейка: «Звернення» замість «Вхідні».
5. Оператором: Налаштування → Фільтри пошти — додати/вимкнути правило БЕЗ 403.
6. `/license` без входу → на логін (коли ліцензія активна).

**Як влити в master (коли інший чат закрито):**
```
cd P:\AI-Projects\CRM_Laba
git status            # має бути чисто; інакше спершу закомітити/відкинути чужі файли
git merge audit/2026-09-06
.\dev_restart.ps1
git worktree remove ..\CRM_Laba-audit
```
Далі — звичайний реліз (`/kuubmill-release`), запис у CHANGELOG уже є.

## 11. Повне код-ревʼю 07.09.26 і лад у гілках

**Гілки.** `audit/2026-09-06` влито в master (`d30b90f`), 24 злиті локальні гілки й 6
залишених agent-worktree прибрано, upstream master → `origin/master`. Лишилась лише `master` (worktree `../CRM_Laba-audit` видалено після мержу). Віддалену `work-2026-08` GitHub не дає
видалити — **protected branch**: Settings → Branches → зняти правило, потім
`git push origin --delete work-2026-08`.

**Код-ревʼю.** Звіт `design/audit_2026-09-06/CODE_REVIEW.md` (одним абзацом — §1, рахунок — §2).
Сім детальних звітів `review_full_*.md` поруч. Виправлено одразу (з тестами): 3 CRITICAL синку,
4 CRITICAL пошти, 2 CRITICAL видачі/undo, 1 CRITICAL SISMA, CSRF/Origin, токен Telegram, ще ~10.
Що лишилось і в якому порядку — `PLAN.md`, розділи **S2 / M / K / D / T / U2**. Три кроки
позначені ⚠ (бекап усіх таблиць, підпис інсталятора, PRAGMA foreign_keys, TLS агента) — спершу
спитати.

**Що перевірити руками після оновлення (8002 уже з цим кодом):**
1. Видача → клієнт із сьогоднішньою роботою → «Усі знайдено» / «Видати» бере і її.
2. Черга → «Скасувати» через 6 хвилин після дії → «Вікно скасування минуло».
3. Нові з пошти → прийнятий і відфрезерований лист → «Повернути в тріаж» → відмова з поясненням.
4. Статистика о 00:30 → «Сьогодні» = вчорашня робоча доба.
5. Dev 8002 у сусідній вкладці → спроба POST на прод 8000 → 403 (Origin-стіна).

**Реліз:** CHANGELOG уже містить записи; `/kuubmill-release` → 0.11.3.

## 12. Блоки B / S2 / M / K / D / T / U2 — зроблено 07.09.26

**Стан.** master `c1af8c4`, **28 комітів поверх релізу 0.11.3** — тобто все нижче в прод ще НЕ
поїхало. 2271 тест зелений. Наступний реліз — 0.11.4 (`/kuubmill-release`).

**Що це дає простими словами.**

1. **Дані не зникають.** Копія бази охоплює всі таблиці (раніше половину: печі, верстати,
   матеріали, весь Виробіток лишались поза копією). Перед КОЖНИМ оновленням програма сама робить
   повний зліпок у `backups/pre-update/`. Після оновлення «Стан системи» каже: усе на місці або
   чого бракує.
2. **Стерте повертається.** Вміст рядка таблиці зберігається ДО стирання, у «Журналі синку» є
   кнопка «Відновити рядок». Плюс стеля — не більше 20 стирань за годину.
3. **Черга не бреше.** Роботи без наряду більше не залипають в архіві назавжди. Робота більше не
   переписується чужими даними, коли рядок зсунувся. Стерти матеріал заради виправлення одруківки
   більше не викидає роботу з черги (раніше вона верталась «новою», без історії й Sum3D).
4. **Видача не зависає.** Партія на 50 робіт — приблизно три звернення до Google замість сотень.
5. **Пауза = пауза.** Раніше вона зупиняла лише запис, а читання далі рухало чергу.
6. **Пошта не губить файли.** Моргання мережевої шари більше не сприймається як «файл видалили»
   (лист позначався прийнятим, а робота лишалась у спулі). Зависла перевірка не пускає другу в ту
   саму теку.
7. **Замок на місці.** Секрети й керування людьми — лише з робочого ПК. Зміна пароля тепер вибиває
   старі сесії. Кожна зміна налаштувань лишає слід «хто і що» (без самих паролів). Код Виробітку
   зберігається необоротно.
8. **Одна піч не гасить усе.** Збій пристрою чи бібліотеки = «немає кадру» для нього, решта живе.
   Статус печі вимагає, щоб ДВА сигнали збіглись (раніше вистачало одного — перевірка була вдаваною).
9. **Реліз не падає через дрібницю.** Імʼя інсталятора береться з тегу, а не вписане шість разів
   руками. Заборонені оновлення бібліотек, які точно зламають печі й запис у таблицю.

**Що ЗАЛИШИЛОСЬ** — `design/audit_2026-09-06/PLAN.md`, 29 рядків. Коротко:
- *можна робити без тебе (18):* швидкодія `/?period=earlier`, `/clients/{id}/pane`, пагінація
  пошуку й журналу; дрібниці пошти C.1/C.2; звести гейти прав; LOW-список бекенду; кольори в
  `settings.css`;
- *потрібне твоє око (7):* ширина «вузьких» екранів, шапка standalone-карток, 403 шаблоном, бейдж
  «5/5», обрізаний заголовок на 900 px, затримка PIN-клавіатури, палітра Ctrl+K і SVG-спрайт;
- *потрібен ти фізично (4):* **B.5 прод**, D.1 Go-агент (перевстановлення на ПК верстатів),
  D.6 SISMA (потрібні свіжі кадри), P.5 перевірка «змінено після взяття» на проді.

**ОБОВʼЯЗКОВО перед релізом — прогнати на тестовому сервері (див. §13).**

## 13. Що прогнати руками на 8002 перед релізом 0.11.4

Порядок такий, бо кожен наступний спирається на попередній.

1. **Копія й відновлення.** Налаштування → Резервна копія → створити. Потім відновити її ж і
   переконатись, що числа зійшлись (розходження = відкат, а не напівжива база).
2. **Оновлення.** Підмінити версію → перезапустити → «Стан системи» показує зелений рядок звірки.
   Перевірити, що в `backups/pre-update/` зʼявився файл.
3. **Стирання й повернення.** Видалити тестову роботу → «Журнал синку» → кнопка «Відновити рядок»
   → рядок повернувся в таблицю. Другий раз кнопка не працює (і не має).
4. **Черга.** Стерти матеріал у клієнтському рядку тестової таблиці → синк → робота лишилась на
   місці з історією. Повернути матеріал.
5. **Видача.** Клієнт із кількома роботами → «Видати N з M» → заливка знялась, у журналі один
   запис на роботу.
6. **Пауза.** Поставити паузу → переконатись, що черга НЕ рухається (раніше рухалась).
7. **Пошта.** Прийняти лист → відкат «Повернути в тріаж» → файли повернулись у спул БЕЗ суфікса
   «(2)» в імені.
8. **Права.** Зайти оператором: тека кадрів верстатів має давати 403; налаштування пошти — теж.
   Змінити оператору пароль адміном → його стара вкладка має викинути на логін.
9. **Печі й верстати.** `/furnaces` і `/machines` відкриваються, статус печі або збігається з
   табло, або показує «?» — але не бреше.
10. **Друк.** Паспорт роботи → Ctrl+P → на аркуші лише зміст, без темної рейки.

Якщо будь-який пункт поводиться інакше — не релізити, писати в новий чат що саме.

## 14. Результат прогону §13 на dev — 07.09.26 (вечір)

Пройдено на 8003 (порт 8002 досі тримає процес-привид, там старий код).

| # | Пункт | Що вийшло |
|---|---|---|
| 1 | Копія й відновлення | **знайдено баг, полагоджено** — див. нижче |
| 2 | Оновлення | зелений рядок «0.11.3 → 0.11.4: усе на місці»; копія в `backups/pre-update/` (2,2 МБ) |
| 3 | Стирання й повернення | рядок повернувся; друга спроба каже «вже відновлено» |
| 4 | Черга без матеріалу | покрито тестом; жива перевірка за тобою — стерти колір у клієнтському рядку |
| 5 | Видача | «знайдено» + «Видати» пройшли; один запис у таблицю на партію, інший день не зачепило |
| 6 | Пауза | синк не читає і не пише, видалення відмовляє; знято паузу — ожило |
| 7 | Пошта | прийняття перенесло файли в export, «Повернути в тріаж» повернуло їх у спул тими самими іменами, без «(2)» |
| 8 | Права | оператору 403 на теку кадрів і `calibration.zip`; зміна пароля адміном вибиває його вкладку на логін |
| 9 | Печі й верстати | обидва екрани живі, кажуть «немає зв'язку» замість вигаданих цифр |
| 10 | Друк | паспорт узагалі без рейки, в архіві рейка схована `@media print` |

**Баг, який ловив саме живий прогін.** Відновлення з резервної копії падало 500 на щойно
створеній власній копії. `orders.source_email_id` і `email_messages.order_id` посилаються одна
на одну — при перевірці зовнішніх ключів по рядку жоден порядок вставки не проходить. Блок B
(повна копія) і блок K (`foreign_keys=ON`) поодинці правильні, разом дали зламане відновлення.
Лікування — `PRAGMA defer_foreign_keys=ON`: перевірка переїжджає на COMMIT, коли обидві таблиці
вже заповнені. Тести модуля працювали БЕЗ увімкнених FK, тому дірки не бачили; додано тест, який
вмикає їх явно.

**Поправка до §13 пункт 8.** Очікування «налаштування пошти дають оператору 403» застаріле:
за рішенням від 06.09 пошта — це «джерело робіт», оператор редагує її нарівні з адміном
(`edit_roles=None`). Адмінське там інше — тека кадрів верстатів і `calibration.zip`.

**Слід від перевірки.** Проба прав перезаписала логін/пароль IMAP у тестовій базі; відновлено
з `backups/kuubmill_20260907_131317.db` (перенесено зашифровані рядки), зʼєднання перевірено.

## 15. Що зроблено після релізу 0.11.4 — 07.09.26 (ніч)

Реліз **0.11.4 опубліковано** (інсталятор і контрольна сума в релізах, smoke-тест пройшов).
Пам'ятай: після встановлення всі залогінені входять заново — разово.

**Робочий каталог переїхав на `D:\KuubDesk`.** Шара лишилась як резервна копія (`git remote share`).
Тести стали 100 с замість 160, `git status` — 0,6 с замість секунд. Правило: працювати ТІЛЬКИ з
`D:\KuubDesk`, інакше два дерева розійдуться гілками.

**Швидкодія.**
- Картка клієнта читала 2321 роботу на кожен клік — тепер лише роботи цього клієнта (10 мс).
- Черга «Раніше» сканувала папки для 616 рядків — тепер для 200 показаних (утричі менше звернень
  до Synology; удома різниці не видно, виграш саме на робочому ПК).
- Пошук: сторінка 50 рядків замість сотні (~1 МБ → 515 КБ), і відбір більше не тягне всю таблицю
  робіт у пам'ять. Журнал синку: 150 записів замість 400 (~200 КБ → 98 КБ). В обох є «Показати ще».

**Безпека й порядок.**
- Два роути синку (повний імпорт історії, зняття запобіжника масової архівації) обіцяли в
  докстрінгу «лише з цього ПК», а перевірки не було. Тепер є.
- Десять рукописних гейтів «адмін + цей ПК» зведено до спільного помічника.
- Самоперевірка тепер СПРАВДІ пробує запис у теку export (рядок так і зветься), а для теки
  техніків проби немає — звідти лише читають.

**Пошта й файли.**
- Прийняття листа відкочує перенесені файли на БУДЬ-ЯКІЙ помилці, не лише файловій.
- Відмова від уже прийнятого листа тепер відмовляється з підказкою: відкат уміє «Повернути в тріаж».
- Вікно пошуку листів рахується від робочої доби (о 00:05 стрибало на добу раніше).
- Скачування за посиланням не лишає файл-сироту в спулі при невдалому коміті.

**Дрібниці, які тихо переставали працювати:** обмежувач спроб входу більше не росте вічно; тека
калібрувальних кадрів пише в лог, коли викидати нічого; скарга на зсунуті заголовки зникає разом
із вкладкою; «Оновити зараз» на верстатах має паузу 3 с.

**Перевірено наживо (8003, після всіх правок):** 2287 тестів зелені, ruff чистий, усі 25 екранів
віддаються за 0,008–0,31 с, оператор має 403 там, де треба, анонім — редірект на логін,
самоперевірка 7/7, копія бази знімається й відновлюється (3452 роботи до і після), у лозі сервера
жодної помилки.

**Цікаве спостереження на потім:** один рядок черги важить ~10 КБ HTML — здебільшого через
випадайку статусів, яка малюється для кожного рядка. Саме це робить сторінку на 200 рядків
двомегабайтною.
