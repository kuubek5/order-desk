# ROADMAP_IDEAS.md — перспективний план ідей KuubMill

Свіжі пропозиції прискорити роботу цеху, виведені з наявного коду (не загальний
список CRM-фіч). Кожна каже: **що болить → ідея → що вже написано → чого
бракує → орієнтовна ціна**. Номер = рекомендований порядок, НЕ жорсткий.

Складено 02.09.26 після наскрізного аудиту (23 екрани, 13 роутерів, 8 воркерів,
1396 тестів). Це план-нагадування, а не зобов'язання — обговорюється перед
будь-якою реалізацією.

> Живий візуальний артефакт цього плану:
> https://claude.ai/code/artifact/52dfc752-4944-441f-9f7f-cba041178b8e
> Супутній аудит «розрив до преміуму»:
> https://claude.ai/code/artifact/2101f75e-23fd-4017-9a8e-299930fce6e5

---

## Рівень A — прибрати ручні кроки (потрібні шматки вже в коді)

### 1. Сторож теки Sum3D — ID заповнюється сам ⭐ (В РОБОТІ)
- **Болить:** оператор створює проєкт у Sum3D, читає хвіст часу з назви
  (`17-55-28`) і вручну переписує його в CRM. Найчастіша дія дня, кожна —
  ризик одруківки.
- **Ідея:** CRM стежить за текою проєктів Sum3D. Новий проєкт → CRM бачить час
  у назві → пропонує вставити ID у потрібний рядок.
- **Факт про назву теки (від власника):** назва = `YYYY-MM-DD_HH-MM-SS`, БІЛЬШЕ
  нічого. Ключа за вмістом (наряд/клієнт) немає → зіставлення лише за наміром
  (пін) + часом. Timestamp зашитий у назві, тож затримка сканування не спотворює
  момент.

**Прийняті дизайн-рішення (4 стани одного механізму, макети зроблено):**
1. **Привид у полі** — закріплена робота, порожнє поле → бляклий найновіший ID
   з ↵, Enter/клік записує. Артефакт ee426f28…
2. **Заміна ⇄** — закріплена, поле вже має ID, з'явився новіший → «⇄ замінити?»,
   старий закреслено, клік замінює (лог + «Крок назад»). Артефакт 3d50d1b4…
3. **Моно-лоток у шапці** — незакріплені/пачка: найновіший ID у моно-полі +
   копія, «+N» на решту (панель із «прибрати» — слайс 2). Артефакт d2b4868d…
4. **Ручна вставка** — нічого не закріплено → копія + Ctrl+V у будь-який рядок
   (§2: рішення за оператором). Артефакт 251bc4c3…
   - Сигнал = токен **`--sig`**, адаптивний до теми: бурштин у Бірюзі,
     прохолодний у Amber Forge (бо там бурштин — сам акцент). Запис завжди
     логується, ніколи не авто.

**Стан реалізації:**
- ✅ **Слайс 1 (02.09.26, master):** налаштування шляху Cam-work
  (`sum3d_projects_path`, /settings/check-path kind=sum3d), сервіс захоплення
  `app/services/sum3d_capture.py` (парсинг назв + скан), моно-лоток у шапці
  `_sum3d_tray.html` (найновіший ID + копія + «+N»), роут `GET /sum3d/tray`
  (самополл 60с), токен `--sig` у tokens.css + theme-forge.css. Перевірено
  наживо на 8002 з тестовою Cam-work: лоток показав `18-04-12` +2.
- ⏳ **Слайс 2:** привид у закріпленому рядку + заміна ⇄ (потребує зв'язку
  пін↔найновіший ID); повна панель лотка з «прибрати» (потрібен стан, що
  переживе полл — клас на body, як у смузі печей).
- ⏳ **Слайс 3:** перевірка одруківки (вписаний ID звіряється з реальними
  теками → «проєкт не знайдено»).
- **Пастка на майбутнє:** офіційна переробка (брак) пише ID у колонку W (rework),
  а НЕ перезаписує L — заміна ⇄ лише для «передумав у тому самому прорахунку».

### 2. Лічильник STL — ловить розбіжність кількості до фрезерування
- **Болить:** кількість одиниць вписують руками; помилка спливає аж на видачі,
  коли диск уже відфрезеровано. Брак впливає на гроші й звітність.
- **Ідея:** порівняти кількість із таблиці з кількістю STL у теці. Розійшлось —
  тихий чіп у рядку «STL: 6 ≠ 4», ще ДО постановки диска.
- **Вже є:** `list_stl_files()` рахує файли в теці (для прев'ю); сканер export
  ходить по тих самих теках.
- **Треба:** порівняння + чіп. Межа: одиниця ≠ завжди файл (міст одним файлом),
  тому попередження, не блокування.
- **Ціна:** ~день.

## Рівень B — нові можливості на вже початому

### 3. Верстат ↔ наряд: жива черга фрезерування (унікальне на ринку)
- **Болить:** щоб знати, що на верстаті й скільки лишилось — треба піти й
  подивитись. 10 верстатів = 10 походів.
- **Ідея:** агент уже бачить `.iso` й смугу %. Зв'язати ім'я з нарядом → рядок
  сам каже «фрезерується на 350i · 77% · ~12 хв».
- **Вже є:** Go-агент, екран «Верстати», живі кадри, конвеєр еталонів цифр.
- **Треба:** OCR % й `.iso` (= Фаза 2 агента) + поле зв'язки. Чекає
  повноекранні кадри 3 станів. Цього немає в жодної дентальної CRM.
- **Ціна:** Фаза 2 агента (запланована).

### 4. Планувальник пічок — «коли вийде»
- **Болить:** роботи клієнта в різних пічках, виходять у різний час; тримають у
  голові; клієнту «коли готово?» відповідають на око.
- **Ідея:** «до закладки о 17:00 готово 23 роботи цирконію» (завантаження) +
  «ця вийде завтра о 9:15» (видача).
- **Вже є:** каталог матеріалів (що цирконій), статус «відфрезеровано»,
  `done_at` уже рахується й показується в смузі пічок.
- **Треба:** зведення «готові до закладки» + прогноз відкриття на роботах.
- **Ціна:** 2–3 дні.

### 5. Ярлик на пакетик замість ручки
- **Болить:** на видачі підписують пакетик ручкою щоразу; друку в застосунку
  нема (0 правил `@media print`).
- **Ідея:** ярлик (клієнт, к-сть, дата, роботи). Побічний ефект важливіший:
  ярлик створює спільний ключ, якого фізично не існує — на майбутнє.
- **Вже є:** групування по клієнтах, «видати групу», дані робіт.
- **Треба:** шаблон друку + ярликовий принтер. Заразом закриває друк списку
  видачі й паспорта для рекламації.
- **Ціна:** ~2 дні + принтер.

### 6. Telegram-бот у два боки ✅ (зроблено 10.09.26, v0.14.0)
- **Стан:** меню (пічки, верстати, Sisma, роботи й видача сьогодні/вчора),
  сповіщення про пічки й Sisma, учасники за одноразовим запрошенням, окремі
  меню власника й операторів. Що далі — пункт 28.
- **Болить:** 9 типів сповіщень описані, але тост живе лише поки вкладка
  відкрита; вночі/у вихідні не знає ніхто; адміністратор дзвонить оператору по
  статус і відриває його.
- **Ідея:** назовні — критичне на телефон (технік змінив узяту роботу, синк ліг,
  пічка стала); всередину — `/де 24122` → статус у відповідь.
- **Вже є:** перелік подій із рівнями (`NOTIFY_EVENTS`), пульс синку, логіка
  статусів.
- **Треба:** лише канал доставки. Найзвичніша труба в українському цеху.
- **Ціна:** 1–2 дні.

## Рівень C — гіпотези (спершу перевірити на реальних даних)

### 7. Номери зубів з імен STL — карта замість звірки оком ⭐ (дешева перевірка)
- **Болить:** видача найповільніша — оператор звіряє форму з STL-прев'ю оком;
  спільного ключа немає.
- **Ідея:** дентальні CAD часто називають файли номером зуба (`13.stl`,
  `crown_23.stl`). Якщо так — на видачі карта зубів «13, 14, 23»: шукати за
  числом незрівнянно швидше.
- **Вже є:** перелік файлів, прев'ю, зіставлення клієнт↔тека.
- **Треба:** СПЕРШУ подивитись реальні імена у теці export (10 хв). Ризик —
  борг із покрученими іменами файлів за посиланням. Сміттєві імена → ідея
  відпадає безкоштовно; чисті → найбільший виграш зі списку.
- **Ціна:** перевірка 10 хв, далі ~день.

### 8. Авто-детект повтору (переробки)
- **Болить:** «який раз фрезерується» + винуватця браку вписують руками — там,
  де дані впливають на гроші.
- **Ідея:** той самий клієнт + той самий зуб/вид + <30 днів = ймовірна
  переробка; CRM підказує, не вирішує.
- **Вже є:** записи переробок із винуватцем, історія, нечітке зіставлення, архів
  30 днів.
- **Треба:** правило + підказка в картці. Сильніше разом із ходом 7 (номер зуба
  = точний ключ повтору).
- **Ціна:** ~день.

### 9. Прогноз завантаження дня
- **Болить:** «чи встигнемо» — відповідають відчуттям.
- **Ідея:** рядок у шапці «за поточним темпом черга закінчиться о 19:40».
- **Вже є:** середній час «нове → відфрезеровано» вже рахується в статистиці.
- **Треба:** винести цифру з статистики в чергу, перерахувати на залишок дня.
- **Ціна:** ~день.

## Рівень C — відібрано власником 08.09.26 (обговорити перед реалізацією)

Шість пунктів із сесії 08.09.26. Рома позначив «цікаво, потім розберемо» —
це фіксація, не зобов'язання. Порядок = порядок називання, не пріоритет.

### 10. STL-прев'ю на другий монітор
- **Болить:** прев'ю ділить екран зі списком клієнтів; на весь екран — список
  зникає, і оператор гортає назад-вперед.
- **Ідея:** прев'ю відкривається окремим вікном на другому моніторі. Список
  лишається на першому, форма звіряється на другому, перемикань нема.
- **Вже є:** повноекранне прев'ю (`stl-preview.js`, `stl-gallery.js`), другий
  монітор на видачі вже стоїть (розділ «Залізо»).
- **Треба:** Window Management API (`getScreenDetails`) + запасний варіант
  `window.open` на екран за замовчуванням, якщо дозвіл не дали.
- **Обмеження:** лише Chrome/Edge — прод це Windows + вбудований браузер, тож
  прийнятно. Дозвіл на розташування вікон питається один раз.
- **Ціна:** ~день.

### 11. Бейдж у заголовку вкладки
- **Болить:** нові листи й термінові роботи видно лише на своєму екрані; вкладка
  згорнута — сигналу нема.
- **Ідея:** `(3) KuubMill` у `document.title` = 3 нових з пошти. Зникає, коли
  тріаж порожній.
- **Вже є:** лічильник тріажу вже рахується для бейджа в меню.
- **Треба:** один запис у title з наявного числа; повертати початковий заголовок
  при виході з екрана.
- **Ціна:** ~година.

### 12. «Хто чіпав рядок» — diff знімків таблиці
- **Болить:** логісти, адміністратори й техніки правлять таблицю руками; тиха
  правка кольору чи кількості після взяття в роботу випливає аж на видачі.
- **Ідея:** на картці роботи рядок «колонку Колір змінили в таблиці 14:32, після
  взяття в роботу».
- **Вже є:** копії вкладок (`app/sheet_backup.py`) — знімки за 4+ дні вже лежать.
- **Треба:** порівняння двох знімків по рядку + показ у хронології картки.
- **Пастка:** копія НЕ перезаписується при різкому падінні рядків (обрізане
  читання) — diff мусить це врахувати, інакше покаже фантомні зміни.
- **Ціна:** ~день.

### 13. 500 і падіння синку в Telegram
- **Болить:** прод стоїть у чужому цеху. `_settings_check_result.html` випав і
  дев'ять днів віддавав 500 без жодного видимого сліду (CLAUDE.md §14).
- **Ідея:** хендлер логера на `ERROR`/необроблений виняток шле в наявний бот.
- **Вже є:** `app/services/telegram.py` (бот KuubMill, токен у налаштуваннях,
  `new_legacy_session` крізь TLS-проксі цеху), фоновий ретрай.
- **Треба:** logging.Handler + дедуп однакових помилок (інакше цикл синку заллє
  чат) + перемикач у налаштуваннях.
- **Пастка:** у тексті помилки можуть бути шляхи з іменами клієнтів — фільтрувати
  перед відправкою назовні.
- **Ціна:** ~пів дня.

### 14. View Transitions на свапах htmx
- **Болить:** рядки черги й видачі стрибають при свапі; полл смикає око.
- **Ідея:** `hx-swap="… transition:true"` — браузер сам робить плавний перехід,
  без власного JS і без бібліотек.
- **Вже є:** htmx 1.9.10 підтримує `transition:true`; свапи вже точкові.
- **Треба:** увімкнути на `#queue-rows` і рядках видачі, звірити з
  `prefers-reduced-motion`.
- **Пастка:** §8 — анімації стримані й функціональні. Порядок списку видачі
  фіксований, перехід не має створювати враження пересортування.
- **Ціна:** ~пів дня.

### 15. three.js 0.128 → 0.16x
- **Болить:** рушій прев'ю старий на п'ять років; великі мости (13-23) крутяться
  ривками.
- **Ідея:** оновити three.js і `STLLoader` до сучасної гілки.
- **Вже є:** `three-0.128.0.min.js`, `STLLoader-0.128.0.js`, власний
  `stl-render-core.js`.
- **Треба:** перевірити зміни API між 0.128 і цільовою версією (освітлення,
  кольоровий простір, назви класів), прогнати на реальних STL з `export`.
- **Пастка:** прев'ю — головний інструмент звірки на видачі. Зламане прев'ю
  зупиняє ранкову видачу, тому оновлення — окремий блок із перевіркою на
  справжніх файлах, не «заодно».
- **Ціна:** ~день з перевіркою.

### 16. Кнопка RustDesk у смузі верстатів
- **Болить:** щоб зазирнути в софт верстата, оператор шукає RustDesk, згадує ID
  або клацає по списку підключень. Смуга верстатів показує стан, але з неї
  нікуди не перейти.
- **Ідея:** клік по верстату відкриває підключення до нього. RustDesk має власну
  URL-схему: `rustdesk://<ID>` (для власного сервера — `?server=…&key=…`).
- **Чому це дешево і чому працює по мережі:** схему обробляє ОС того, хто
  клікнув, а не сервер. На відміну від «Відкрити теку» (Провідник відкриває
  сервер, тому гейт `is_loopback_request` її ріже), кнопка RustDesk однаково
  спрацює і на ПК-сервері, і на ПК другого оператора. Гейт не потрібен.
- **Вже є:** модель `Machine` дзеркалить `Furnace` (app/models.py:937) — поле,
  форма в Налаштуваннях і місце в смузі лягають за наявним зразком.
- **Треба:** поле RustDesk ID у `Machine` + міграція, поле у формі верстата,
  кнопка в `_machine_strip.html` і на картці.
- **Пастка 1 — пароль:** у посилання НЕ класти. Він осяде у вихідному коді
  сторінки і в історії браузера. Постійний пароль зберігається в самому
  RustDesk на кожному робочому ПК; посилання несе лише ID.
- **Пастка 2 — кіоск:** якщо колись зʼявиться екран-кіоск на стіні (перегляд без
  входу), кнопки підключення там бути НЕ повинно.
- **Перед роботою:** формат URI в документації RustDesk описаний уривчасто.
  Перевірка на пʼять хвилин — вставити `rustdesk://<ID>` реального верстата в
  адресний рядок. Спрацювало — робимо.
- **Побічне:** це той самий механізм, що відкладений `sum3d-open://` (CLAUDE.md
  §13). Верстати — дешевший і безпечніший пілот, ніж одразу Sum3D.
- **Гігієна:** у RustDesk цього року була вразливість з обходом автентифікації
  (CVE-2026-30797). Не привід відмовлятись, привід тримати клієнти оновленими.
- **Ціна:** ~пів дня.

## Рівень D — двоє операторів і день власника (консультація 08.09.26)

Розмова без коду: як підключити другого оператора і що додати з огляду на
робочий день. Ходи 17-19 — інфраструктура, 20-24 — фічі поверх неї.
Порядок усередині кожної групи — рекомендований.

### 17. Мережевий доступ для другого оператора ⭐ (фундамент решти)
- **Болить:** двоє операторів, а застосунок слухає лише `127.0.0.1`
  (`windows_launcher.py:315`) — другий ПК підключитись не може.
- **Ідея:** один ПК = сервер із базою й усіма воркерами; другий заходить
  браузером на його адресу. Це стандартна архітектура CRM, не компроміс.
- **Чого робити НЕ можна:** другий KuubMill на спільному файлі бази через
  мережеву шару. SQLite у WAL так не працює (потрібна спільна памʼять `-shm`),
  плюс два синки таблиці писали б у ті самі клітинки. База лишається на
  ЛОКАЛЬНОМУ диску сервера (`%LOCALAPPDATA%\KuubMill`, `runtime.py:57`).
- **Треба:** прив'язка до `0.0.0.0` перемикачем у Налаштуваннях (типово ВИМК),
  та сама адреса в пробі порту (`windows_launcher.py:138`), показ готової
  адреси для другого ПК, правило брандмауера (вхідний TCP 8000, лише приватний
  профіль), постійна IP-адреса сервера.
- **Квота Google не зростає:** клієнт Google лишається ОДИН, другий ПК з Google
  не говорить. Але темп ДІЙ подвоюється, а кожен запис коштує ще й читання для
  звірки рядка (`sheet_writer.py:85`). Перед підключенням поміряти пік
  `api_calls_last_minute` у завантажений ранок: до 25 — запас є, від 35 — спершу
  знизити (поодинокі галочки через пакетний шлях, синк подалі від видачі).
- **Рівність функцій:** з 13 дій, замкнених на `is_loopback_request`, 12
  адмінські. Операторська одна — «Відкрити теку» (пошта, панель STL, подвійний
  клік у черзі). Обхід: по мережі копіювати мережевий шлях у буфер + тост,
  ~пів години. Усі теки в обох ПК мережеві, тож шлях вставляється і працює.
- **Ціна:** ~1 день коду + ~1 день налаштування в цеху.

### 18. Сервер у шафі + запуск службою
- **Болить:** ПК-сервер стоїть на столі оператора — сон, перезавантаження,
  вихід із Windows знімають систему в другого. Автозапис у `HKCU\...\Run`
  (`installer/KuubMill.iss:42`) стартує при ВХОДІ користувача, не при вмиканні.
- **Ідея:** дешевий міні-ПК у шафі, який ніхто не чіпає, + запуск службою
  Windows при старті машини. Обидва оператори стають рівними клієнтами.
- **Плата:** значок у треї при службі не працює (потрібен сеанс користувача) —
  для машини в шафі трей і не потрібен.
- **Ціна:** гроші на міні-ПК + ~день.

### 19. Екран-кіоск моніторингу на стіні
- **Болить:** стан печей і верстатів видно лише тому, хто сидить за CRM.
- **Ідея:** телевізор або Raspberry Pi показує печі/верстати на весь екран.
- **Чому дешево:** печі й верстати опитує СЕРВЕР (кадр раз на 6 с), сторінки
  показують готові числа. Екрану не потрібні ні паролі печей, ні доступ до них;
  скільки завгодно екранів не додають VNC-сеансів (контролери часто пускають
  лише одного глядача).
- **Треба:** посилання-кіоск (довгий ключ, ЛИШЕ перегляд, лише з локальної
  мережі, відкликається з Налаштувань) + вигляд на весь екран без меню, великі
  цифри, помітний знак «дані застаріли».
- **Пастка:** екран бачать техніки й логісти — на ньому лише обладнання, без
  списків робіт клієнтів. І без кнопки RustDesk (хід 16).
- **Ціна:** ~день, після ходу 17.

### 20. Смуга тривог — показувати ВІДХИЛЕННЯ, а не стан
- **Болить:** система показує стан; відхилення оператор досі ловить головою.
- **Ідея:** кілька правил на наявних полях, тихий рядок угорі лише коли
  спрацювало. Наприклад: «на швидку» відфрезеровано, а до закладки 17:00
  лишилась година; взято в роботу, а Sum3D ID порожній 3+ години; лист прийнято,
  а рядок у таблиці не зʼявився; часткова видача, а решта не відфрезерована.
- **Треба:** список правил складає ВЛАСНИК, не розробник.
- **Ціна:** 1-2 дні залежно від кількості правил.

### 21. Ранкове зведення в Telegram (08:30)
- **Болить:** який ранок попереду, дізнаєшся вже відкривши екран.
- **Ідея:** одне повідомлення до приходу: скільки робіт до видачі, скільки
  клієнтів, скільки «на швидку», які печі відкрились.
- **Вже є:** бот KuubMill і `services/telegram.py`, усі дані в базі.
- **Ціна:** ~пів дня.

### 22. Присутність на видачі — хто біля якого клієнта
- **Болить:** щойно операторів двоє, зʼявляється нова помилка — обоє беруть один
  лоток.
- **Ідея:** відкрив картку клієнта — інший бачить тонку позначку «Рома тут».
  Без блокувань і підтверджень, лише сигнал.
- **Вже є:** канал «мої зараз» у черзі (`OrderFocus`) — механізм копіюється.
- **Коли:** у день, коли другий оператор реально сяде за роботу, не раніше.
- **Ціна:** ~пів дня.

### 23. Екран логіста (лише перегляд)
- **Болить:** логіст питає оператора або дивиться таблицю; оператор працює
  довідковим бюро.
- **Ідея:** третя роль і один екран на телефоні: у кого все знайдено, у кого
  часткова видача.
- **Треба:** третя роль (зараз лише адмін/оператор).
- **Ціна:** ~день, після ходу 17.

### 24. Фото пакетика на видачі
- **Болить:** суперечка «ви надіслали не ту коронку» вирішується словами проти
  слів, а брак впливає на гроші.
- **Ідея:** одне фото підписаного пакетика з телефона, привʼязане до видачі.
- **Вже є:** робота з картинками у зміні (`shift_images.py`).
- **Проти:** зайвий рух на видачі, де правило — один клік. Робити ЛИШЕ якщо такі
  суперечки бувають реально, а не теоретично.
- **Ціна:** ~день.

### 25. Трей: індикатор стану, а не просто ярлик ⭐ (погоджено 08.09.26)
- **Болить:** підказка при наведенні — статичний рядок «KuubMill», іконка одна
  на всі стани. Щоб дізнатись, чи працює синк, треба ВІДКРИТИ застосунок. Тобто
  трей не робить своєї головної роботи — не показує стан.
- **Чому саме зараз:** дані вже є. Аудит 08.09.26 навчив систему розуміти власні
  поломки (`/health` перевіряє базу й вік тіків, `SyncHeartbeat.last_success_at`
  знає час останнього УСПІХУ, `error_message` доїжджає до інтерфейсу). Лишилось
  донести це до трею — найбільша віддача на зусилля з усього списку нижче.
- **Ідея:** жива підказка («синк: помилка · пошта востаннє о 9:40») + стан на
  іконці (норма / попередження). Наведення миші відповідає на питання без
  жодного кліку.
- **Вже є:** `app/windows_launcher.py:255` (`pystray.Icon(...)`, третій аргумент —
  підказка, зараз стала), `assets/kuubmill.ico`, `sync_heartbeat.heartbeats`.
- **Треба:** `icon.title` оновлювати з пульсу; другий-третій варіант іконки;
  `icon.update_menu()` уже викликається на паузі, поруч і оновлення підказки.
- **Ціна:** ~день.

### 26. Трей: «Вийти» надто легкий ⭐ (погоджено 08.09.26)
- **Болить:** пункт одразу зупиняє сервер, без питання, і сховати вікно
  неможливо — єдиний вихід з трею зупиняє все.
- **Три наслідки, і всі загостряться:**
  1. З ДРУГИМ ОПЕРАТОРОМ (ROADMAP хід 17) «Вийти» на ПК-сервері вимкне
     застосунок і йому. Людина натисне звичний пункт, не знаючи, що вбиває
     чужу роботу.
  2. Незаписані дії: пул запису в таблицю не дренажиться при вимкненні
     (RESILIENCE_PLAN.md, етап 3.1 — черга в базі це закриє, але поки ні).
  3. Немає «згорнути» — лише «зупинити все».
- **Треба:** підтвердження з чесним текстом («застосунок перестане працювати
  для ВСІХ, хто підключений»), окремий пункт «Сховати вікно», і перевірка
  незавершених записів перед виходом.
- **Ціна:** ~пів дня.

### 27. Трей: чотири дрібниці, яких бракує до продакшн-рівня
- **Версія просто в меню** — питання «яка в тебе версія» зараз вимагає відкрити
  застосунок. Один рядок.
- **«Відкрити теку логів»** — коли щось не так, людина має знати куди дивитись;
  зараз шлях треба памʼятати.
- **«Перевірити оновлення зараз»** — автоперевірка раз на добу є, ручної немає.
- **Сповіщення бульбашкою** — застосунок згорнутий, і саме тоді найцінніше
  сказати «пошта не відповідає 10 хв» або «оновлення готове». Пряме
  продовження аудиту: система навчилась розуміти поломки, але мовчить про них
  у трей.
- **Автозапуск перемикачем** (зараз лише при встановленні, `KuubMill.iss:42`).
- **Ціна:** ~день на всі разом.

**Чого в трей свідомо НЕ додавати:** налаштувань. Вони вже є на своєму екрані, а
дубль розійдеться з оригіналом. Трей відповідає на два питання: «як воно там» і
«відкрий мені».

### 28. Налаштування Telegram-бота — друга хвиля (записано 10.09.26)
Рішення власника: нічого не будувати, доки бот не попрацює тиждень у цеху;
тоді брати те, що справді заважає. Поточних налаштувань (токен, чат, тест,
вимикач, стан, учасники) для старту досить.
- **Найімовірніше знадобиться:**
  1. **Тихі години** (напр. 23:00–07:00). Закладки о 22:00 і 03:00 — «пічка
     закрилась» будитиме вночі. 🔔 вимикає все, а потрібна середина.
  2. **Вибір подій** галочками: «закрилась» / «можна відкривати» / «Sisma
     закінчила». Спершу загальні, потім — якщо попросять — на кожну людину.
     Очікування: власнику потрібне лише «можна відкривати».
  3. **Журнал надісланого** — коли, кому, що, дійшло чи ні. Дані вже є в
     `TelegramOutbox`, бракує лише списку на екрані. Потрібен з першого
     «мені не прийшло».
- **Можливо, пізніше:** окремий розділ «Telegram-бот» у меню налаштувань
  (зараз живе в «Зворотному зв'язку», де його неочевидно шукати; новий розділ
  = рядок у `settings_nav.NAV` + партіал + плита, CLAUDE.md §14); змінити
  підпис учасника після запрошення; кнопка «тестове сповіщення всім»;
  налаштування нових подій, якщо з'являться (час ранкового зведення — п. 21,
  поріг «обрив зв'язку довше N хв»).
- **Не потрібно:** строк дії запрошення (24 год норм), часовий пояс (Київ
  свідомо, як у печах), вебхук (у цеху неможливий), ролі учасників (адмін лише
  власник), мова/аватар/опис бота (це @BotFather).
- **Ціна:** 1–3 — ~день разом.

## Залізо (не софт) — прискорює руки, а не екран
- **Ножна педаль** на видачі: руки зайняті коронкою й пакетиком, педаль натискає
  «видано» ногою. Вмикається як клавіша — працює одразу з гарячими клавішами.
  ~600–1200 грн.
- **Ярликовий принтер** (під хід 5). ~2500–4000 грн.
- **Другий екран на видачі:** STL-прев'ю на весь екран перестає ділити місце зі
  списком клієнтів. Наявний монітор.

## Свідомо НЕ пропоную (це рішення власника)
- Заміна Google-таблиці — спільна мова цеху.
- Відмітка «втрачено» — тримається поза системою.
- Рушій дедлайнів «Здати до» — свідомо на паузі.

---

**Що взяв би першим:** хід 1 (сторож Sum3D — прибирає найчастішу ручну дію,
використовує наявне, без API/заліза), хід 7 (перевірка імен STL — дешева
перевірка дорогої гіпотези), хід 3 (верстат↔наряд — унікальне, чекає кадри).
