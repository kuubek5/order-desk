# Бриф: скринька невідомих екранів (крок 3 з `MCP_NEXT_BRIEF.md`)

Складено 12.09.26, коли крок 2 (кадри й зони через MCP) уже випущено. Тут —
рішення до початку роботи, щоб не переписувати їх двічі.

## Задача одним реченням

Зчитувач сам помічає, що не зрозумів кадр, і кладе його в скриньку; Рома
розмічає одним рядком («це екран помилки», «тут нічого важливого»); я з його
підпису роблю зону, еталон або правило статусу — **з тестом і в репозиторії**.

Підпис Роми — **дані, не правило**. Через екран нічого не вчиться.

## Що вже є (не писати заново)

| Що | Де | Чого бракує |
|---|---|---|
| Відбір кадрів за НЕСХОЖІСТЮ (мініатюра 64×48, поріг 6.0, витіснення найзайвішого) | `machines.collect_calibration_frame_timed` | вмикається галкою руками, не звʼязане з «читач не зрозумів» |
| Кадр «екран JOBS видно, назву не прочитано» | `machines._report_unread_screen` | один файл на верстат, без причини в базі й без екрана |
| Причина відмови зони | `furnace_ocr.read_zone` → `Field(clipped/unknown/raw)` | нікуди не потрапляє, крім лога |
| Статус двома голосами | `furnace_ocr.read_status` | розбіжність голосів ніде не лишає сліду |
| Кадр і розбір назовні | `device_diag` + `kmill_frame` / `kmill_zones` (крок 2) | лише найсвіжіший кадр |

## Рішення

**Дублі — умова існування фічі.** Кадр раз на 6 с = 600/год на пристрій.
Відпечаток (хеш зменшеної чорно-білої копії), ОДИН рядок на відпечаток,
лічильник «бачено N разів, уперше/востаннє».

**Таблиця `screen_puzzles`** (міграція 0061): вид (піч/верстат), ключ і назва
пристрою (назва — знімком, пристрій можуть перейменувати), відпечаток,
причина кодом, пояснення текстом, `first_seen_at` / `last_seen_at` /
`seen_count`, імена файлів кадру й вирізу зони, підпис Роми з автором і часом,
ознака «не питай більше».

**Межі:** ~20 відпечатків на пристрій, зменшена копія повного кадру + виріз
зони. Витіснення — як у калібруванні: спершу позначені «неважливо», далі
найрідше бачені. Лічильник витіснених + рядок у лог через `app/log_throttle.py`
(перший раз, далі раз на годину) — інакше стеля спрацює тихо.

**Захоплення не гальмує опитування:** відпечаток рахується з УЖЕ розібраного
кадру; запис на диск і в базу — лише на НОВИЙ відпечаток; повтор оновлює
лічильник не частіше раза на хвилину (памʼять процесу). Урок зі стуку в порти:
послідовний прохід старить решту пристроїв.

**Один кадр — одна загадка.** Кадр із чужого екрана провалить усі зони одразу;
писати чотири рядки на один кадр означало б забити скриньку одним випадком.
Береться найважливіша причина за пріоритетом: незнайомий розклад екрана →
розбіжність голосів статусу → обрізана зона → немає еталона → не той шаблон.

**Куди підчеплятись:** `furnace.poll_target` після `read_panel`,
`machines._report_unread_screen` і гілка «кадр є, а читати з нього нічого».

**Екран** — окремий пункт рейки (`_topbar_nav.html`), не розділ налаштувань:
це робота, а не конфігурація. Рядок = кадр + причина + «бачено N разів» +
поле підпису + кнопка «неважливо». Підпис нічого не вмикає й не вимикає.

**MCP:** `kmill_screen_puzzles` (список із підписами) і кадр загадки картинкою
через `kmill_frame` за ключем загадки. Саме так підпис Роми доїжджає до мене,
не лишаючи цеховий ПК.

## Порядок робіт

1. Модель + міграція 0061.
2. `app/services/screen_inbox.py`: відпечаток, дедуплікація, витіснення,
   лічильники; жодного винятку назовні (захоплення — зручність, не робота).
3. Гачки в опитуванні печей і верстатів.
4. Екран і рейка.
5. Інструменти MCP.
6. Тести: дублі не ростуть, витіснення викидає позначене «неважливо» першим,
   лічильник оновлюється не частіше раза на хвилину, захоплення не кидає на
   битому кадрі.
