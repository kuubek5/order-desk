# Бриф: пошта — «Стрічка» + «Конвеєр» (A + B)

Вихід дизайн-чату 25.09.26 (Рома). Наступний блок роботи з поштою після
0.20.31 + 4 незапушених комітів 25.09 (STL drag, компактний майстер, гейт
«не всі файли», mypy). Читати разом із `CLAUDE.md` §14 «Пошта», не замість.
Тест — ЛИШЕ локальний dev :8002, акаунт `claude_test`. Прод не чіпати.

Візуал (артефакти, приватні):
- сторінка з прокруткою — https://claude.ai/artifact/4B6kfU87oqGNwpjjXXe9Qp
- канвас з артбордами — https://claude.ai/artifact/LQBgJMLt5qJM9Wm4UUdLvk
- джерела мокапів — scratchpad `mailredesign/project/*.dc.html` (не в git)

## ⚠ Урок, який не можна повторити

`MAIL_CARD_STEPS_BRIEF.md` («карта в 3 кроки») власник ВІДКИНУВ 23.09.26 після
повного білду: «забагато інформації на одній сторінці, потрібно повертати
назад». Там три ПОВНИХ блоки з усіма контролями стояли один під одним — довга
сторінка.

Варіант A — НЕ це. Різниця, що робить його прийнятним:
1. **Один екран без прокрутки** на 1280×800. Правило жорстке: якщо картка
   при білді вилазить за екран — це провал етапу, не «додамо скрол».
2. **Первинне на видноті, вторинне за розкриттям.** На картці лише те, що
   потрібно, щоб ПРИЙНЯТИ типовий лист: 2 рядки тексту, мініатюри файлів
   з одним чіпом статусу, чотири поля в ряд, один рядок шляху, одна кнопка.
   Скачати/наново/розпакувати, посилання Drive, навчання фільтра, історія,
   зміна теки — за `<details>`, меню «⋯» або кліком по мініатюрі.
3. **Етапність з підтвердженням.** Кожен етап показати власнику на dev і
   дочекатись «ок» ДО наступного. Не будувати все й показувати в кінці.

## Слова власника (25.09.26)

«Обробка листів захаращена. Мета — спростити візуально прийняття, але не
загубити функціонал. Візуальний контроль: бачити й розуміти, що замовник
пише, що ми качаємо і куди файл дівається. Кілька листів обробляти в один
захід.» Обрано **A + B**. Доповнення: «Роботи, які йдуть у чергу з пошти,
мають бути знизу, як зараз, видимі — дзеркало звичайної черги. Повний
контроль над тим, що робиш.»

## Інвентар функцій — НЕ ЗАГУБИТИ

| Функція | Зараз | У A+B |
|---|---|---|
| Текст листа від замовника | вкладка «Лист» | картка, 2 рядки + «розгорнути» |
| Файли + STL-прев'ю з обертанням | вкладка «Файли + STL» | мініатюри на картці; клік — велике прев'ю (stl-gallery.js, drag уже є) |
| Скачати вкладення / за посиланням (Drive, ukr.net) / наново / розпакувати | там же | чіп статусу файлів → розкриття з кнопками |
| Клієнт, матеріал, к-сть, вид | майстер Крок 1 | 4 поля в ряд на картці; підказки `materialsuggest.js`, `sender_hint`, `material_cands` лишаються |
| Тека клієнта / нова / підпапка матеріалу | майстер Крок 2/3 (`dir_editor`) | рядок шляху + «змінити» → розкриває `dir_editor` (макрос уже є) |
| Часткове прийняття (2 кольори) | чекбокси файлів Крок 1 | чекбокси на мініатюрах |
| Гейт «не всі файли скачані» / посилання | Крок 3 warn + `accept_anyway` | той самий warn над кнопкою; сервер не міняти |
| Відхилити / у фільтр / у папку «оброблено» / відкат | низ панелі + рядок списку | меню «⋯» біля кнопки; з рядка списку ПРИБРАТИ дублі |
| Бейдж готовності | рядок списку (`triage_readiness`) | лишається, у B блокує чекбокс |
| Історія/статус листа | вкладка «Статус» | `<details>` унизу картки |
| **Прийняте з пошти (дзеркало черги)** | низ екрана | **ЛИШАЄТЬСЯ як є** — власник |

## A · Стрічка — картка одного листа

Замість трьох вкладок і майстра на 3 кроки — одна картка, блоки згори вниз:

1. **Шапка**: ім'я відправника + чіп матеріалу (`mail_material_badge`) +
   адреса/час; праворуч чіп «постійний · N робіт · export/<тека>»
   (sender_hint, компактний — зроблено 25.09).
2. **Що пише замовник**: `body_text`, обрізано до 2 рядків, «розгорнути ↓».
3. **Файли**: мініатюри STL (72–92px) + імена + чіп `✓ скачано N з M` /
   `⬇ не скачано` / `🔗 K за посиланням`. Клік по чіпу — розкриває блок
   кнопок (скачати / наново / розпакувати / рядки посилань). Клік по
   мініатюрі — велике прев'ю. Чекбокс на мініатюрі — часткове прийняття.
4. **Поля в ряд**: Клієнт · Матеріал·колір · К-сть · Вид (grid 1.4/1.2/0.5/1.2).
5. **Низ**: ліворуч рядок `export / клієнт / дата / матеріал ↺ наявна`
   + «змінити» (розкриває `dir_editor`); праворуч «⋯» · Відхилити ·
   **✓ Прийняти в чергу** (`.btn.apex`, єдина залита).
6. Warn «не всі файли» + `accept_anyway` — над низом, лише коли є що казати.
7. `<details>` «Історія листа» — під усім, згорнуто.

## B · Конвеєр — кілька листів разом

- У рядку списку — чекбокс. Заблокований (сірий, підказка) коли
  `triage_readiness` ≠ ready (нема файлів / не скачано / 3D-друк).
- Перший чекбокс → панель праворуч перемикається з картки на **таблицю
  батчу**: рядок на лист: ▸ · Клієнт · Матеріал · К-сть · Файли (чіп) ·
  Ляже у (шлях). Значення — ті самі здогади, що пішли б у картку; клітинки
  редагуються. ▸ розгортає рядок: текст листа 2 рядки + мініатюри.
- Кнопка **«✓ Прийняти N в чергу»** + підпис «N робіт, M файлів переїдуть».
- «Обрати всі готові» зверху списку.
- Зняти всі чекбокси → панель повертається до картки.

## Як зшити — бекенд НЕ міняти

- Картка шле ту саму форму, що майстер: `client_name, material_color, kind,
  quantity, folder_pick, folder_new, material_folder, attachment_ids,
  accept_anyway` → `POST /mail/{id}/accept`. `accept_email` і
  `accept_letter` не змінюються; `_accept_failed` рендерить картку з
  помилкою (той самий контекст), поля не губляться.
- Прев'ю шляху при зміні полів — `POST /mail/{id}/wizard` лишається, але
  `hx-target` на блок шляху, не на всю картку (партіал
  `_mail_path_preview.html` або `dir_editor` як є).
- **Батч**: один новий роут `POST /mail/accept-batch` (знімок
  `route_inventory` — свідомо), тіло = список `{email_id, поля…}`; сервер —
  цикл `accept_letter` по кожному під своїм локом листа; результат —
  список ok/помилка по листу; помилка одного НЕ відкочує інших (кожен лист
  — власна транзакція, як зараз). Відповідь — таблиця з рядками-статусами,
  успішні зникають зі списку, невдалі лишаються з текстом.
- Стан «обрані листи» — лише в браузері (JS), не в БД.
- `mail.js`: прибрати перемикання вкладок; лишити `mailFilesChanged`,
  гарячі клавіші, живий міні-підсумок (тепер із полів картки).
- Зняти: `_mail_wizard.html` кроки 1–3 → лишити ЛИШЕ макрос `dir_editor`
  (винести в `_mail_dir_editor.html`); CSS `.wiz-*` — grep перед видаленням.
- Список: з рядка прибрати «У папці / ↩ / ✕» (переїжджають у «⋯» картки);
  `get_mail` і `_mail_triage_list.html` правити разом (§14).

## Етапи (кожен — показати на dev, дочекатись «ок»)

1. **Картка A замість вкладок+майстра.** Один лист, усе на екрані. Без B.
   Здача: справжній лист у фантомній скриньці → відкрити → прийняти → файл
   фізично в `export/…`, робота в черзі, рядок у тестовій таблиці.
2. **B — чекбокси + таблиця батчу + `/mail/accept-batch`.** Здача: 3 листи
   одним натиском; один з помилкою — двоє прийнято, третій лишився з текстом.
3. **Полірування**: прибрати дублі з рядка списку, `<details>` історія,
   гарячі клавіші, видалити мертвий код майстра.

## Інваріанти (§14 «Пошта») — не зламати

- Спул → export ЛИШЕ при прийнятті; відкат повертає у спул.
- Гейти на сервері (посилання, нескачані вкладення, `pending`, зомбі-фетч);
  UI лише показує.
- `.btn.apex` — одна на екран. Жодних анімацій-петель у тріажі.
- Водяний знак `since`; `get_mail` ↔ `_mail_triage_list.html` разом.
- `export_folder_path` (Частина A, 0074) пишеться при прийнятті — батч
  теж мусить (іде через `accept_letter`, тож автоматично).

## Перевірка (§15, змагальна)

1. Лист → картка → прийняти → файл на диску, черга, таблиця.
2. Лист за посиланням: warn + кнопка тримається до скачування/галочки.
3. Два кольори: часткове → картка знову з рештою файлів.
4. Помилка прийняття: картка на місці, поля цілі, текст помилки.
5. Батч 3 листи, один зламаний: 2 ok + 1 з помилкою, список коректний.
6. Відкат з Архіву: файли у спул, лист «нове».
7. Ширина 1280×800: картка БЕЗ вертикального скролу на типовому листі.
8. Сторожі: `route_inventory` (+1 роут свідомо), `frontend_assets`,
   `test_mail_wizard_errors.py` (перечитати — тестує майстер), повний прогін.

## Не робити

- Не чіпати `accept_letter`, `mail_export`, синк, дзеркало папок.
- Не міняти «Прийняте з пошти» внизу.
- Не робити C (три колонки) — відкладено.
- Не ховати warn про файли за розкриттям — він завжди на видноті.
