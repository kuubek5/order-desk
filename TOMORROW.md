# План на робочому ПК

Два завдання, обидва підготовлені 03.09.26 увечері. Нічого встановлювати чи
налаштовувати не треба — усе вже в коді.

**Спершу оновити прод** до збірки з цими змінами, інакше жодного з інструментів
на робочому ПК не буде.

---

## 1. Швидкодія: знайти, де затуп

### Що робити

Просто працювати як зазвичай **10-15 хвилин**: поперемикати вкладки періодів,
відкрити видачу, пошту, картку роботи, натиснути кілька кнопок. Саме те, що
здається повільним — те й робити.

Потім відкрити **`/diag/perf`** (лише адмін) → кнопка **«Скопіювати як текст»**
→ надіслати блок мені.

### Що там буде видно

Кожен рядок — одна взаємодія, найповільніші зверху:

| Стовпець | Що означає |
|---|---|
| **Відчутно** | від кліка до намальованого — те, що чекає оператор |
| **Сервер** | скільки з цього зайняв застосунок |
| **Після сервера** | свап HTMX + перемальовка браузером |
| **Розкладка** | по фазах: SQL, мережева шара, рендер шаблону, гейт ліцензії, «решта (незаміряне)» |

Ключове: **«Відчутно» і «Сервер» — різні числа**. Якщо затуп у другому
стовпці — лікуємо сервер; якщо в «Після сервера» — лікуємо розмітку й обсяг
таблиці. Досі ми не могли їх розрізнити взагалі.

«Решта (незаміряне)» показується **явно** — це не помилка, а чесна відповідь
«сюди інструмент ще не дотягнувся». Якщо саме вона виявиться найбільшою,
наступний крок — заміряти всередині неї.

### Уже видно на dev (для орієнтиру)

- рендер `_queue_rows.html` на **105 рядках — 0.40 с**. На робочому ПК рядків
  у кілька разів більше, тобто це вже секунди чистого малювання шаблону;
- «решта (незаміряне)» на черзі ~0.3 с навіть на трьох рядках.

Це здогадки, не висновки. Числа з робочого ПК їх або підтвердять, або ні.

### Дрібниці

- Кнопка **«Очистити»** — перед заміром, щоб у буфері були лише сьогоднішні дані.
- Буфер тримає останні **400** взаємодій і живе лише до перезапуску застосунку.
- Розкладка також лягає в заголовок `Server-Timing` — видно у вкладці «Мережа»
  браузера, якщо захочеш подивитись сам.

---

## 2. Спостерігач за верстатами: навчити читати підпис

### Навіщо

Відсоток зараз береться з **геометрії** смуги (частка заливки) — і це вже
сім разів ламалось об чергове синє щось на екрані. Надійніший сигнал — сам
**підпис усередині смуги** («43%»): це те, що верстат сам про себе пише.

Читається шаблонами цифр, знятими з реальних кадрів. Малювати їх шрифтом не
можна — перевірено на печах, жоден системний не збігається.

### Що вже є

- цифри **1**, **0** і знак **%** — зняті з реального кадру 100%;
- бракує: **2, 3, 4, 5, 6, 7, 8, 9**.

### Порядок

**Крок 1 — зібрати кадри** (потрібна одна програма, що фрезерується):

```bash
python scripts/machine_collect_frames.py <адреса-верстата> <ТОКЕН> --minutes 40
```

Скрипт зберігає кадр **лише коли змінився відсоток**, тож за одну програму
назбирується майже весь набір цифр. Кадри лягають у `calibration_frames/<адреса>/`.

**Крок 2 — навчити** (на кожен кадр із новим числом):

```bash
python scripts/machine_glyphs.py learn calibration_frames/<адреса>/pct-43-1.png 43
```

Число береться з імені файлу — воно і є те, що написано на кадрі. Скрипт сам
скаже, яких цифр ще бракує. Запускати двічі на тому самому кадрі безпечно.

**Крок 3 — перевірити:**

```bash
python scripts/machine_glyphs.py check calibration_frames/<адреса>/*.png
```

Має бути `підпис 43%  =  геометрія 43%`. Знак `≠` означає, що геометрія
промахується — і саме заради цього все й робилось. Знак `?` — цифру ще не
навчено.

Якщо кадр не читається взагалі:

```bash
python scripts/machine_glyphs.py show <кадр.png>
```

— вирізає підпис збільшено у файл, видно, що саме там намальовано.

### Чому потрібні РІЗНІ відсотки, а не один кадр

Підпис стоїть по центру смуги, тому на частковому прогресі він **розрізаний
межею заливки**: ліва половина біла на синьому, права темна на світлому. Код
це вже враховує, але одна й та сама цифра на межі та цілком на заливці — два
різні накреслення, і обидва треба показати.

### Що зміниться після навчання

Підпис стає **головнішим** за геометрію. Доки цифру не навчено — працює як
раніше, тобто зіпсувати нічого не може. Розбіжність підпису й геометрії
пишеться в лог: якщо вона систематична, значить межі смуги знаходяться
неправильно, і це видно тільки так.
