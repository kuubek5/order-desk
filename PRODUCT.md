# PRODUCT.md — Order Desk

Продуктова правда для дизайн-роботи. Візуальні рішення тут не фіксуються — їх власник DESIGN.md.

## Що це

Order Desk — робоча черга фрезерного центру зуботехнічної лабораторії. ~10 верстатів imes-icore фрезерують коронки, абатменти, каркаси з цирконію, ПММА, титану. 20–40 робіт на день.

Унікальний механізм: зводить два потоки робіт (внутрішня лабораторія через Google Таблицю + клієнтська пошта з вкладеннями) в одну чергу і закриває ранкову видачу — найболючіший ручний етап, де оператор шукає фізичні роботи в папці `export` за іменами клієнтів.

## Хто і де

Оператор CAM за робочим ПК у виробничому приміщенні фрезерного центру. Windows 10/11, локальний `127.0.0.1:8000`, один монітор, цілий робочий день. Поруч працюють верстати. Той самий екран паралельно дивляться логісти, адміністратори й техніки.

Робота йде не «сесіями», а безперервно: оператор тримає чергу відкритою весь день і повертається до неї десятки разів.

## Головна задача

Черга робіт — 90% часу. Все інше обслуговує її.

Оператор має за секунди побачити: що прострочено, що здавати сьогодні, що вже прораховано, а що чекає. Далі — взяти роботу, скопіювати шлях-ID у Sum3D, вписати ID проєкту назад, змінити статус.

## Реальні дані (не вигадувати інші)

**Наряд** — `24122`. **Номер роботи (шлях-ID)** — `2026-07-21_00016-007`. **Матеріал/колір** — `пмма A2`, `моно А3.5`, `титан корея`, `500`, `800`. **Вид роботи** — `анатомія`, `анатомія гвинтова`, `абатмент`, `каркас міст 13-23`, `вкладка`. **Коментар для CAM-оператора** — `покрити опаком`, `Чекаємо Скани!!!!`, `Якщо можно на швидку, я сам закрию`. **Дедлайн** — `14:00` / `16:00` / `09:00`. **Вкладка таблиці** — дата `22.07.26`.

## Статуси

`нове` → `прийнято` → `прораховано` → `у фрезеруванні` → `відфрезеровано` → `знайдено при видачі` → `видано`. Окремо: `проблема`, `переробка`.

Прострочене = день вкладки минув, а робота не в фінальному статусі.

## Екрани

1. **Черга робіт** — головна. Фільтри: період (сьогодні/вчора/завтра/раніше), готовність, джерело (лабораторія/пошта), прострочені. Сортування по колонках. Колонки: наряд, коментар, матеріал/колір, вид роботи, кількість, шлях, Sum3D ID, технік, статус.
2. **Нові з пошти** — тріаж розпізнаних полів листа перед прийняттям у чергу.
3. **Картка роботи** — паспорт, хронологія статусів з іменем оператора, коментарі, файли, історія переробок.
4. **Ранкова видача** — вчорашні роботи по клієнтах, зіставлені зі шляхами з `export`.
5. **Клієнти** · **Пошук** · **Статистика** · **Налаштування** (адмін) · **Акаунт**.

## Функції, які справді працюють

Двостороння синхронізація з Google Таблицею (точковий write-back Sum3D ID, статусних маркерів, коментарів). IMAP-перевірка пошти кожні 2 хвилини. Клік по шляху-ID копіює його в буфер для Sum3D, подвійний клік відкриває папку. STL-превʼю вкладень. Нечітке зіставлення імен клієнтів із назвами папок `export`. Облікові записи операторів — кожна дія в історії підписана конкретним оператором.

## Тон

Спокійна сила. Це високотехнологічне виробництво з мікронними допусками, а не стартап. Без хайпу, без пафосу, без святкування дій користувача.

## Заборони бренду (від власника)

Фіолетові градієнти. Глянсові 3D-кулі. Усмішки й емодзі. Тотальне заокруглення. Сітка іконок з фічами. Inter і системні шрифти. Райдужна палітра.

## Що не вигадувати

Ціни, кількість клієнтів, назви реальних лабораторій, бенчмарки, інтеграції, яких немає. Демо-дані черги — авторські й позначені як демонстраційні.
